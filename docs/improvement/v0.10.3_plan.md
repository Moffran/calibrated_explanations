# v0.10.3 Release Task Implementation Plan

This plan captures tasks completed for v0.10.3 (originally targeted for v0.11.0).

## Release tasks covered

## 1) ADR-008: Domain model authority (Explanation/FeatureRule)

**Goal:** Make the ADR-008 domain model authoritative in runtime flows (not just
as an adapter), fix legacy round-trips, enrich calibration/model metadata, add
golden fixtures, and harden `_safe_pick`.

### 1.0 Relevant references

- **[RELEASE_PLAN_v1.md](RELEASE_PLAN_v1.md)** — v0.11.0 task definition and release gates.
- **[adrs/ADR-008-explanation-domain-model-and-compat.md](adrs/ADR-008-explanation-domain-model-and-compat.md)** — authoritative domain model and compatibility rules.
- [adrs/ADR-005-explanation-json-schema-versioning.md](adrs/ADR-005-explanation-json-schema-versioning.md) — schema v1 contract, additive evolution rules, provenance/metadata extension surface.
- [adrs/ADR-002-validation-and-exception-design.md](adrs/ADR-002-validation-and-exception-design.md) — validation expectations and exception taxonomy.
- [../standards/STD-003-test-coverage-standard.md](../standards/STD-003-test-coverage-standard.md) — fixture/testing expectations.
- [tests-guidance.md](https://github.com/Moffran/calibrated_explanations/blob/main/.github/tests-guidance.md) — test-writing rubric.

### 1.1 Current anchors in code

- Domain model: `src/calibrated_explanations/explanations/models.py`.
- Legacy collection implementation: `src/calibrated_explanations/explanations/explanations.py`.
- Serialization helpers: `src/calibrated_explanations/serialization.py`.

### 1.2 Key gaps to close (from release plan wording)

- Runtime flows still primarily operate on legacy dicts/lists, not domain objects.
- Legacy → domain → legacy round-trips are not guaranteed.
- Missing structured metadata (calibration/model provenance) on the domain object.
- `_safe_pick` in `from_legacy_dict` is tolerant but not robust/deterministic.
- Golden fixtures for domain-model parity are missing.

### 1.3 Implementation steps

1. **Enforce the conversion boundary.**
      - The canonical conversion boundary is **immediately after any legacy
         explanation dict is produced**.
      - Add a `convert_legacy_to_domain(legacy_dict)` helper and require all call
         sites to call it at the production point.
      - From that point onward, the **domain model is authoritative**. Legacy
         dicts must not be mutated downstream (scoring, aggregation,
         post-processing, telemetry, serialization).
      - Add a lightweight lint/CI check (or debug-only assertion) that flags code
         paths which mutate legacy dicts instead of domain objects after the
         boundary.

2. **Fix round-trips and support conjunctive rules.**
      - Audit `from_legacy_dict(...)` in `explanations/models.py` and define the
         minimum supported legacy keys.
      - Support conjunctive rules as an additive JSON v1 minor:
         `rules[*].feature: integer | array[integer]`.
      - Domain model behavior:
         - Normalize `FeatureRule.feature` to `List[int]` for internal logic
            (singletons become length-1 lists).
         - Expose an `is_conjunctive` boolean (or equivalent) for clarity.
      - Serialization behavior:
         - Emit `feature` as an integer for single-feature rules and an array for
            conjunctive rules (avoid unnecessary churn for scalar-only consumers).
      - Add fixtures and round-trip tests for factual, alternative, and
         conjunctive cases.

3. **Replace `_safe_pick` with deterministic parsing.**
      - Replace the current heuristic fallback with `deterministic_pick(container,
         idx_or_key=None)`.
      - Deterministic behavior (no silent fallbacks):
         - `None` containers raise `ValidationError`.
         - mappings return the requested key (or `'predict'` when no key provided),
            else raise `ValidationError`.
         - sequences return `container[idx]` (validated) or `container[0]` when no
            index is provided; empty sequences raise `ValidationError`.
         - scalars return the scalar when `idx` is `None` or `0`, else raise.
      - Do not mask `TypeError`; raise `ValidationError` per ADR-002.
      - Replace all uses of the old `_safe_pick` and add unit tests covering:
         scalars, mappings, sequences, empty sequences, invalid index/key types.

4. **Attach runtime parameters and minimal default provenance.**
      - Add a typed `Explanation.runtime_parameters` (or
         `Explanation.calculation_context`) as the **authoritative** location for
         behaviour-changing parameters (thresholds, bins, conditionals,
         preprocessing map pointer, calibration pointer).
      - Compute and store a canonical parameter manifest and record
         `provenance.parameters_hash = "sha256:<hex>"` where `<hex>` is the sha256
         of the canonicalized manifest bytes.
      - Canonicalization and hashing MUST follow the global rules above.
      - The parameter manifest artifact MUST include the global artifact fields
         (`schema_version`, provenance fields, and `checksums`).
      - Persist the canonical manifest as an immutable artifact keyed by the hash
         and (where permitted) record a pointer in `metadata.audit.config_artifact`
         (or `metadata.audit.run_id`).
      - Populate these default fields unless caller explicitly supplies values or
         policy prohibits:
         - `provenance.library_version` (literal library version string)
         - `provenance.calibration_version` (artifact content id if available,
            else `null`)
         - `provenance.parameters_hash` (required when runtime parameters are
            material)
      - Ensure `provenance` and `metadata` remain **non-authoritative** for
         computation; they identify state, they do not change interpretation.
      - Do not write raw training data or PII into manifests or public payload
         fields.

5. **Golden fixtures and acceptance-focused tests.**
      - Add golden fixtures for:
         - factual explanation
         - alternative explanation
         - conjunctive rule case
      - Add round-trip tests for legacy↔domain↔JSON parity.
      - Add parameter-hash verification tests that fetch a manifest (fixture or
         fake artifact store) and re-hash it to confirm equality with
         `provenance.parameters_hash`.

### 1.4 Decisions (v0.10.3)

- **Conversion boundary:** Convert legacy dicts → domain objects immediately
   after legacy explanation dict production; domain model is authoritative.
- **Parameters & reproducibility:** Attach authoritative
   `runtime_parameters`/`calculation_context` to the domain model and record a
   canonical `provenance.parameters_hash` computed as sha256 of a canonical
   manifest; persist the manifest as an immutable artifact and reference it from
   `metadata.audit` where permitted.
- **Provenance defaults:** Populate minimal, non-sensitive defaults:
   `library_version`, `calibration_version` (if available), `parameters_hash`.
- **Conjunctive rules:** Support `rules[*].feature: integer | array[integer]`
   as an additive JSON v1 minor; normalize domain `feature` to `List[int]` and
   expose `is_conjunctive`.
- **Parsing hardening:** Replace `_safe_pick` with `deterministic_pick` and raise
   `ValidationError` on structural incompatibility; no `arr[-1]` fallbacks.

### 1.5 Verification checklist

- Conversion boundary enforced (tests and/or CI check confirm early conversion;
   downstream code does not mutate legacy dicts).
- Golden fixtures exist for factual, alternative, and conjunctive rule cases.
- Round-trip parity tests pass for legacy↔domain↔JSON.
- JSON v1 schema and serialization support `feature` as int or array[int].
- `provenance.parameters_hash` is populated (when required) and hash
   verification tests re-hash the canonical manifest and assert equality.
- Canonical manifests are rejected (as `ValidationError`) when they contain
  sensitive keys.
- `deterministic_pick` tests cover scalars, dicts, sequences, empty sequences,
   and invalid cases; failures raise `ValidationError` per ADR-002.
- Run the test suite: `pytest -q`.

## 2) ADR-005: Strict payload validator + fixture coverage

**Goal:** Close payload validation gaps by ensuring schema validation and
semantic invariants are reliably enforced, and add fixture coverage.

### 2.0 Relevant references

- **[RELEASE_PLAN_v1.md](RELEASE_PLAN_v1.md)** — v0.11.0 task definition and release gates.
- **[adrs/ADR-005-explanation-json-schema-versioning.md](adrs/ADR-005-explanation-json-schema-versioning.md)** — schema versioning and strictness expectations.
- [adrs/ADR-002-validation-and-exception-design.md](adrs/ADR-002-validation-and-exception-design.md) — validation/exception expectations.
- [adrs/ADR-008-explanation-domain-model-and-compat.md](adrs/ADR-008-explanation-domain-model-and-compat.md) — domain model ↔ serialization compatibility requirements.
- **[../standards/STD-003-test-coverage-standard.md](../standards/STD-003-test-coverage-standard.md)** — fixture coverage requirements.

### 2.1 Current anchors in code

- JSON schema: `src/calibrated_explanations/schemas/explanation_schema_v1.json`.
- Schema helper: `src/calibrated_explanations/schema/validation.py`.
- Serialization invariants: `src/calibrated_explanations/serialization.py`.

### 2.2 Key gaps (from release plan wording)

- Schema validation is optional when `jsonschema` is not installed.
- Schema currently enforces `explanation_type` ∈ {factual, alternative}; ensure
   this matches runtime behavior and fixtures.
- Fixture coverage needs to exercise schema and invariants.

### 2.3 Implementation steps

1. **Make validation strict (without forcing heavy deps).**
    - Keep `jsonschema` optional, but add a minimal built-in validator that
       checks:
       - required keys,
       - allowed `explanation_type`,
       - `rules[*].feature` shape,
       - payload-level types for core fields.
    - When `jsonschema` is available, run full schema validation.

2. **Align schema with runtime rule shapes.**
    - If conjunctive rules are to be supported in JSON, update schema to allow
       `feature` as `integer | array[integer]` (additive).
    - Otherwise, explicitly reject conjunctive rules in serialization with a
       clear error and document the limitation.

3. **Add fixtures + tests.**
    - Add valid/invalid payload fixtures.
    - Tests must assert failures are raised (not silently skipped).

### 2.4 Verification checklist

- `serialization.to_json()` fails fast on invalid payloads even without
   `jsonschema` installed.
- `schema.validate_payload()` provides full-schema enforcement when `jsonschema`
   is installed.
- Tests cover both modes.

## 3) ADR-010: Core-only dependency clarity

**Goal:** Verify that core-only installs do not require matplotlib at import time.

### 3.0 Relevant references

- **[RELEASE_PLAN_v1.md](RELEASE_PLAN_v1.md)** — v0.11.0 task definition and release gates.
- **[adrs/ADR-010-core-vs-evaluation-split-and-distribution.md](adrs/ADR-010-core-vs-evaluation-split-and-distribution.md)** — core-only import constraints and optional dependency policy.

### 3.1 Current anchors in code

- Lazy imports: `src/calibrated_explanations/__init__.py` uses lazy loading for optional dependencies.
- Test: `tests/test_core_import_no_matplotlib.py` verifies matplotlib is not imported.

### 3.2 Implementation steps

1. **Core-only import verification.**
    - Ensure lazy imports prevent matplotlib from being loaded on core package import.
    - Add a unit test that simulates a clean import environment and asserts `matplotlib` is not in `sys.modules` after importing `calibrated_explanations`.
    - CI should run this test to enforce ongoing compliance.

### 3.3 Decision (v0.11.0)

- Implement verification via unit test and lazy imports.

### 3.4 Verification checklist

- A clean environment without matplotlib can import `calibrated_explanations` without loading matplotlib.
- Unit test passes and is included in CI.

## 4) Standard-004: Plugin authoring + override guidance

**Goal:** Publish comprehensive plugin authoring and override guidance for all plugin types (explanation, interval calibrator, and plot plugins) consistent with the audience-hub documentation structure.

### 4.0 Relevant references

- **[RELEASE_PLAN_v1.md](RELEASE_PLAN_v1.md)** — v0.11.0 task definition and release gates.
- **[../standards/STD-004-documentation-audience-standard.md](../standards/STD-004-documentation-audience-standard.md)** — required documentation structure and audience hubs.
- [adrs/ADR-013-interval-calibrator-plugin-strategy.md](adrs/ADR-013-interval-calibrator-plugin-strategy.md) — interval calibrator plugin architecture.
- [adrs/ADR-014-plot-plugin-strategy.md](adrs/ADR-014-plot-plugin-strategy.md) — plot plugin architecture.
- [adrs/ADR-015-explanation-plugin.md](adrs/ADR-015-explanation-plugin.md) — explanation plugin architecture.
- [adrs/ADR-007-visualization-abstraction-layer.md](adrs/ADR-007-visualization-abstraction-layer.md) — visualization abstraction constraints.
- [adrs/ADR-016-plot-spec-separation.md](adrs/ADR-016-plot-spec-separation.md) — PlotSpec separation guidelines.
- [adrs/ADR-026-explanation-plugin-semantics.md](adrs/ADR-026-explanation-plugin-semantics.md) — plugin semantics and invariants.

### 4.1 Implementation steps

1. **Consolidate and enhance contributor-facing plugin documentation:**
   - Update `docs/contributor/plugin-contract.md` to ensure comprehensive coverage of all three plugin types (explanation, interval calibrator, plot) with consistent structure, examples, and override guidance.
   - Add sections explaining override precedence for each plugin type (keyword/env/pyproject/registry).
   - Reference ADR-013, ADR-015, and ADR-026 for explanation and interval plugins, alongside existing plot references.
   - Include runnable, headlessly-testable minimal examples for each plugin type.

2. **Update practitioner documentation:**
   - Enhance `docs/practitioner/advanced/use_plugins.md` to cover wiring and usage patterns for all plugin types, not just plot plugins.
   - Add examples for interval calibrator and explanation plugin selection and overrides.

3. **Improve main plugin overview:**
   - Expand `docs/plugins.md` to include brief authoring guidance for all plugin types, not just plot plugins.
   - Ensure links to detailed contributor docs for each type.

4. **Add plugin-specific override guidance:**
   - Document environment variable patterns (e.g., `CE_EXPLANATION_PLUGIN_*`, `CE_INTERVAL_PLUGIN_*`, `CE_PLOT_STYLE_*`) for all types.
   - Explain pyproject.toml configuration for plugin allowlists and defaults.
   - Cover registry-based overrides and dependency seeding.

### 4.2 Decision (v0.11.0)

- Comprehensive plugin documentation must cover explanation plugins, interval calibrator plugins, and plot plugins equally.
- Follow the existing "Hello" example pattern for consistency across all plugin types.
- Ensure override precedence is documented uniformly: explainer parameters > environment variables > pyproject.toml > plugin-declared dependencies.

### 4.3 Verification checklist

- `docs/contributor/plugin-contract.md` contains detailed sections for explanation, interval calibrator, and plot plugins with examples and override guidance.
- `docs/practitioner/advanced/use_plugins.md` includes usage examples for all plugin types.
- `docs/plugins.md` provides authoring overviews for all plugin types.
- All examples are runnable headlessly (no GUI dependencies).
- Links from contributors hub point to comprehensive plugin authoring docs.
- Documentation references relevant ADRs (ADR-013, ADR-015, ADR-026) for non-plot plugins.

## 5) Default change: `condition_source="prediction"`

**Goal:** Change the default behavior for `condition_source` to `"prediction"`
and update docs and upgrade guidance.

### 5.0 Relevant references

- **[RELEASE_PLAN_v1.md](RELEASE_PLAN_v1.md)** — v0.11.0 task definition and release gates.
- [adrs/ADR-005-explanation-json-schema-versioning.md](adrs/ADR-005-explanation-json-schema-versioning.md) — metadata fields and compatibility expectations.
- [adrs/ADR-008-explanation-domain-model-and-compat.md](adrs/ADR-008-explanation-domain-model-and-compat.md) — domain-model semantics that surface `condition_source`.
- [../standards/STD-003-test-coverage-standard.md](../standards/STD-003-test-coverage-standard.md) — tests for default changes.
- [remediation_api_changes.md](remediation_api_changes.md) — user-visible migration notes (if this is a breaking/behavioral change).

### 5.1 Current anchors in code

- `CalibratedExplainer` currently defaults to `"observed"`:
  `src/calibrated_explanations/core/calibrated_explainer.py`.
- Explanation collections also default to `"observed"`:
  `src/calibrated_explanations/explanations/explanations.py`.

### 5.2 Implementation steps

1. Change defaults to `"prediction"` while preserving explicit override.
2. Update docs where `condition_source` is explained.
3. Add an upgrade note and a quick checklist for users who relied on observed labels.
4. Add a compatibility checker that issues a one-time visible warning when the
   old behavior is detected during `fit`.

   - Provide a migration snippet in docs:
      - If a user relied on observed-label conditioning, they must now set
         `condition_source="observed"` explicitly.
      - Include a short example showing both settings side-by-side.

### 5.3 Verification checklist

- Unit tests cover default behavior and explicit override.
- Documentation mentions the default change and migration.


## 6) ADR-020: Legacy API stability gates (checklist + audit workflow)

**Goal:** Add a release checklist hook and an audit workflow for notebooks/API.

### 6.0 Relevant references

- **[RELEASE_PLAN_v1.md](RELEASE_PLAN_v1.md)**  v0.11.0 task definition and release gates.
- **[adrs/ADR-020-legacy-user-api-stability.md](adrs/ADR-020-legacy-user-api-stability.md)**  legacy stability policy and expected audit workflow.
- **[legacy_user_api_contract.md](legacy_user_api_contract.md)**  authoritative list of allowed/legacy user-facing APIs.
- [adrs/ADR-012-documentation-and-gallery-build-policy.md](adrs/ADR-012-documentation-and-gallery-build-policy.md)  release-branch gating expectations (docs as a gate).
- [pull_request_template.md](https://github.com/Moffran/calibrated_explanations/blob/main/.github/pull_request_template.md)  checklist integration point.

### 6.1 Current anchors in repo

- Notebook audit script: scripts/audit_notebook_api.py.
- API snapshot script: scripts/snapshot_public_api.py.
- PR template exists: .github/pull_request_template.md.

### 6.2 Implementation steps

1. **Define the release checklist artifact.**
      - Create docs/improvement/release_checklist.md.
      - Add explicit checklist items:
         - legacy contract touched? (yes/no)
         - notebooks audited? (yes/no)
         - API snapshot updated? (if required)
      - Reference the checklist from .github/pull_request_template.md.

2. **Make notebook audit actionable.**
    - Update scripts/audit_notebook_api.py so it can run in a strict mode:
       - fail when unknown API calls are detected,
       - emit a JSON report artifact.
    - Align the method allowlists with docs/improvement/legacy_user_api_contract.md.
      - Add output guidance: --json writes a stable report artifact path (e.g.,
         rtifacts/audit_notebook_api_<ref>.json).

3. **Wire audit into CI.**
    - Add a CI step that runs the notebook audit in report mode (mainline) and
       strict mode (release branch), consistent with ADR-012 release-branch gates.

### 6.3 Verification checklist

- Running python scripts/audit_notebook_api.py notebooks --json produces a
  report.
- Running strict mode fails on unknown methods.

## 7) CE-First Agent Enablement (Deliverable)

**Goal:** Produce a single, self-contained, practical deliverable (helper module + guide) that enables other AI agents to find, understand, and correctly use Calibrated Explanations in a CE-first way.

### 7.0 Relevant references

- **OSS Quickstart** — Canonical usage patterns.
- **[RELEASE_PLAN_v1.md](RELEASE_PLAN_v1.md)** — v0.11.0 task definition.

### 7.1 Current anchors in code

- New module: `ce_agent_utils.py` (reference implementation).
- New docs: `docs/agent_integration.md` (or similar).

### 7.2 Implementation steps

1. **Create `ce_agent_utils.py` (Helper Library)**
    - Implement `ensure_ce_first_wrapper(model)`:
        - Accepts wrapper or raw model.
        - Enforces `WrapCalibratedExplainer` usage (no custom wrappers unless subclass).
        - Verifies `.fitted` and `.calibrated`.
    - Implement `fit_and_calibrate(wrapper, ...)`:
        - Handles learner and explainer kwargs.
    - Implement `explain_and_narrate(wrapper, X, ...)`:
        - Returns explanation object and narrative text.
        - Validates CE-First preconditions.
    - Implement `get_calibrated_predictions` / `get_uncalibrated_predictions` defaults.
    - Implement `wrap_and_explain` (full sequence).
    - Add telemetry hooks and defensive probes.

2. **Develop Agent Guide & Policy**
    - Executive summary and CE-First checklist.
    - Canonical usage patterns (runnable examples for classification/regression).
    - Narrative templates (short/long).
    - CE-First enforcement logic (`enforce_ce_first_and_execute`).

3. **Testing & QA**
    - Unit tests for `ce_agent_utils` verifying presence checks, wrapper validation, state checks.
    - Test Integration: CI checks asserting CE-First compliance.

### 7.3 Verification checklist

- `ce_agent_utils.py` exists and passes linting.
- Tests verify `ensure_ce_first_wrapper` raises on invalid input.
- `explain_and_narrate` produces correct narrative and explanation object.


## 8) MCP Tool Integration

**Goal:** Make Calibrated Explanations "MCP-able" (Model Context Protocol) by defining a tool specification and registration prompt, allowing agents to call CE safely as a tool.

### 8.0 Relevant references

- **Task 7** (Foundation: `ce_agent_utils.py`).

### 8.1 Current anchors in code

- New artifact: `docs/agent/mcp_tool_spec.json`.
- New artifact: `docs/agent/mcp_registration_prompt.md`.
- Optional example: `examples/mcp_server.py`.

### 8.2 Implementation steps

1. **Define Tool Specification**
    - Create `mcp_tool_spec.json` with endpoints:
        - `wrap_model`, `fit_model`, `calibrate_model`
        - `explain_factual`, `explore_alternatives`
        - `get_explanation`, `add_conjunctions`, `plot`
    - Define error codes (400 for uncalibrated/unwrapped).

2. **Create Agent Registration Prompt**
    - Write the compact text for registering the tool with an agent platform.
    - Instructions on using `calibrated_explanations.wrap_and_explain`.

3. **Server-Side Enforcement Design**
    - Document the requirement for server-side enforcement of CE-First (no custom wrappers, state checks).
    - (Optional) Reference implementation of a simple FastAPI server using `ce_agent_utils`.

### 8.3 Verification checklist

- Tool spec JSON is valid.
- Registration prompt is clear and actionable.


## 9) Conformal Guard (Optional Feature)

**Goal:** Implement an optional "conformal guard" using `crepes` to restrict explanation rule conditions to conformal outer-limits (Meister & Nguyen).

### 9.0 Relevant references

- **Conformalised Data Synthesis (Meister & Nguyen)**.
- **`crepes` library**.
- **ADR-0XX** (New) — Conformal Guard Semantics.

### 9.1 Current anchors in code

- New component: `src/calibrated_explanations/core/conformal_guard.py`.
- Integration: `src/calibrated_explanations/core/calibrated_explainer.py`.
- Computation: `src/calibrated_explanations/core/explain/_computation.py`.

### 9.2 Implementation steps

1. **Core Component (`conformal_guard.py`)**
    - Implement `ConformalGuard` class using `crepes`.
    - `fit()`: fits synthesizer (KNN NCM by default).
    - `conforming_ranges_for_instance(instance)`: returns union-of-intervals (numeric) or sets (categorical).
    - `x_cal_filtered(instance)`: helper for discretizers.

2. **Integration (`CalibratedExplainer`)**
    - Add `conformal_guard` kwarg to `__init__`.
    - Initialize `ConformalGuard` if enabled.
    - Logic: Rule = Discretizer Condition AND Conformal Outer Limit.

3. **Computation Logic (`explain_predict_step`)**
    - Call `conformal_guard` to get outer limits.
    - Pass outer limits to `get_greater_values`, `get_lesser_values`, etc.
    - Filter `x_cal` by outer limits for percentile calculation.
    - Include outer limits in `rule_values` (conjunction).

4. **Rule Semantics & Fallbacks**
    - Fallback: If filtered samples < `min_calib_samples`, warn and use global discretizer (or adaptive strategy).

5. **Tests & Docs**
    - Unit tests for single/disjoint intervals.
    - Test non-conforming value exclusion (factuals).
    - Doc updates for new kwarg and rule semantics.

### 9.3 Verification checklist

- `conformal_guard=None` preserves exact existing behavior.
- Enabling guard producing conjunctive rules.
- Tests pass for synthetic non-conforming examples.


## 10) Reject Policy Hardening & Verification

**Goal:** Strictly verify, document, and evaluate each reject policy (including ablations) to ensure robustness and correctness.

### 10.0 Relevant references

- **[RELEASE_PLAN_v1.md](RELEASE_PLAN_v1.md)** — v0.10.3 task definition.
- **`src/calibrated_explanations/core/reject.py`** — Reject policy implementation.
- **`docs/practitioner/advanced/reject-policy.md`** — User documentation.

### 10.1 Current anchors in code

- Logic: `src/calibrated_explanations/core/reject.py`.
- Integration: `src/calibrated_explanations/plugins/manager.py` (RejectOrchestrator).
- Tests: `tests/integration/test_reject_integration_crepes.py` (existing).

### 10.2 Implementation steps

1. **Strict Verification & Testing**
    - Audit `src/calibrated_explanations/core/reject.py` for edge cases.
    - Implement comprehensive unit tests (e.g., `tests/unit/core/test_reject.py`) for all `RejectPolicy` enums.
    - Verify behavior when policies are combined or when no rejection is applicable.

2. **Ablation Evaluation**
    - Develop an evaluation script (or extend existing benchmarks) to assess the impact of reject policies.
    - Measure metrics such as "Effective Calibration Error" on accepted samples vs rejected samples.
    - Document ablation results to justify default policy choices.

3. **Documentation Upgrade**
    - Update `docs/practitioner/advanced/reject-policy.md` with detailed behavior descriptions for each policy.
    - Include ABI/API guarantees for `RejectResult`.
    - Provide clear examples of "Reject Hardening" in practice (e.g. how to handle rejections safely in production).

### 10.3 Demonstration Update

- Update `notebooks/demo_reject.ipynb` to showcase all reject policies.
- Ensure the demo illustrates the behavior of each policy with sample outputs.

### 10.4 Verification checklist

- All `RejectPolicy` variants have dedicated unit tests.
- Ablation evaluation script exists and reports impact.
- Documentation covers all policies with hard guarantees.
- CI includes reject policy tests.

