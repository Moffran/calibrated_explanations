# v0.11.2 Release Task Implementation Plan

This document is the execution plan for all `v0.11.2 (config hardening and ADR governance sweep)` tasks listed in `docs/improvement/RELEASE_PLAN_v1.md`.

## Milestone control posture

- **Active detailed milestone:** v0.11.2.
- v0.11.1 shipped on **2026-04-18**; this file is now the active execution control surface.
- Keep this file prescriptive and execution-oriented until the v0.11.2 boundary closes.
- Update task status in milestone-boundary batches; do not continuously churn status after each PR.

## Planning standard used in this file

Each open task includes:

1. Goal.
2. Why the task exists.
3. Governing ADRs / standards / release-plan references.
4. Hardening or red-team findings incorporated.
5. Non-goals.
6. Primary touchpoints and exact files.
7. Exact symbols, commands, schemas, and artifacts.
8. Ordered implementation steps with hard dependency order.
9. Exact tests to add or update.
10. Exact validation commands.
11. Exact release artifacts/docs to produce.
12. Detailed verification checklist.
13. Status-integrity rules for partial closure.

## Global execution constraints

- Keep CE-first and ADR-first behavior.
- **Compatibility policy for v0.11.2:** most tasks are internal migration, governance hardening, and documentation updates; however, **Task 5A explicitly executes ADR-011/ADR-020-governed removals scheduled for v0.11.2**. Public-surface removals are permitted only where this plan names the exact symbol inventory and fail-closed behavior.
- Any behavior change must include docs + tests in the same PR.
- **Repo-truth rule for this plan:** every concrete path, symbol, and gap assertion must be verified against the current repository tree and `docs/improvement/RELEASE_PLAN_status_appendix.md` before a task is marked complete.
- Validation path for each task PR:
  1. `make local-checks-pr`
  2. Task-targeted pytest/script runs listed in the task
  3. `make local-checks` when branch-gate behavior is touched

---

## Recommended implementation sequence

| Wave | Tasks | Theme | Hard prerequisites |
| --- | --- | --- | --- |
| 1 | 1 | ConfigManager Phase B migration execution | None |
| 2 | 2 | ADR-034 conformance closure and status synchronization | Task 1 complete |
| 3 | 3 | Governance sweep from status appendix inventory | Task 2 complete |
| 4 | 5A | Task-21 removal follow-through (v0.11.2 removals) | Task 3 complete |
| 5 | 4, 6, 7, 8, 9 | Governance dashboard artifact, memory hardening, PlotSpec deferral decision, ADR-035 conformance gaps, PlotSpec mending | Task 3 complete; Task 5A complete |

### Cluster legend

| Cluster | Tasks | Scope |
| --- | --- | --- |
| ADR-034 Completion Chain | 1, 2 | Runtime config migration closure, allowlist-to-zero enforcement, ADR acceptance-conformance synchronization. |
| Governance Hygiene Chain | 3, 4 | Gap inventory and milestone ownership normalization, then CI-derived governance dashboard artifact publication. |
| Deprecation Finalization Chain | 5A | v0.11.2 removals from v0.11.1 Task 21 and deprecations ledger movement. |
| Runtime Stability Chain | 6 | Memory retention and lifecycle hardening with measurable RSS stabilization gates. |
| Visualization Decision Chain | 7, 9 | Binding default-promotion deferral for v0.11.2, then PlotSpec semantic/visual mending while remaining non-default. |
| ADR-035 Conformance Chain | 8 | Close three open ADR-035 gaps: advisory/blocking contradiction, CODEOWNERS coverage, inline-workflow allow-list. |

### Hard dependency map

| Task | Hard depends on | Reason |
| --- | --- | --- |
| 2 | 1 | ADR-034 conformance closure requires Task 1 migration outputs and allowlist-zero proof. |
| 3 | 2 | Governance sweep must consume post-Task-2 status truth for ADR-034 text and milestone assignments. |
| 4 | 3 | Governance dashboard schema and aggregation must align with finalized governance sweep ownership/status text. |
| 5A | 3 | Removal follow-through must use the updated deprecation/governance inventory from Task 3 before ledger moves. |
| 6 | 5A | Memory-hardening validation must run on the post-removal core surface to avoid false baselines from soon-removed hooks. |
| 7 | 3, 5A | PlotSpec deferral decision must be recorded against current milestone governance text and post-removal runtime surface. |
| 8 | 4 | ADR-035 gap remediation depends on Task 4 because Task 4 revealed GAP 2 and its own Makefile addition triggered GAP 4 analysis. |
| 9 | 7 | PlotSpec mending depends on Task 7 establishing the v0.11.2 non-default posture and explicit v0.11.3 follow-up boundary. |

### Release-gate coverage map

| Release gate condition | Closed by |
| --- | --- |
| Four remaining ADR-034 allowlisted runtime modules are migrated and no direct env/pyproject reads remain in those modules | Task 1 |
| `check_config_manager_usage.py` allowlist reaches zero for runtime exceptions and enforcement remains green | Task 1 |
| ADR-034 accepted-state implementation summary and release-plan wording are synchronized | Task 2 |
| Every open status-appendix gap has explicit milestone ownership or superseded/closed status | Task 3 |
| Governance status artifact (`governance_status.json`) is emitted in CI with schema validation and retrieval docs | Task 4 |
| v0.11.2-scoped LIME/SHAP core/wrapper/list-path removals are complete with fail-closed tests and migration ledger moves | Task 5A |
| Long-lived runtime memory retention is bounded with documented lifecycle semantics and RSS stabilization thresholds | Task 6 |
| PlotSpec default-promotion gate is binding; explicit v0.11.2 deferral decision recorded with exact follow-up milestone | Task 7 |
| PlotSpec plots preserve legacy-equivalent explanatory meaning on opt-in paths without changing user default behavior | Task 9 |
| ADR-035 GAP 1 (advisory/blocking contradiction) resolved in ADR text; GAP 2 (CODEOWNERS) patched; GAP 4 (inline workflow allow-list) added to validator | Task 8 |

---

## 1) ConfigManager Phase B migration (ADR-034)

### Goal
Migrate the four remaining allowlisted runtime modules to `ConfigManager` and reduce the runtime migration allowlist to zero.

### Why this task exists
ADR-034 is Accepted and requires a single configuration authority with no direct runtime env/pyproject reads outside sanctioned boundaries.

### Governing ADRs / standards / release-plan references
- `docs/improvement/adrs/ADR-034-centralized-configuration-management.md` (§1, §2, §3, §6).
- `docs/improvement/RELEASE_PLAN_v1.md` (v0.11.2 milestone summary).
- `docs/improvement/RELEASE_PLAN_status_appendix.md` (ADR-034 gaps 2 and 4).

### Hardening findings incorporated
1. Current plan text used stale paths (`core/cache/cache.py`, `core/parallel/parallel.py`); binding paths are `cache/cache.py` and `parallel/parallel.py`.
2. Allowlist-zero was undefined; this task defines it as **empty runtime exception allowlist** in `scripts/quality/check_config_manager_usage.py`.
3. Validation must be per-module and machine-checkable, not generic pytest globs.

### Compatibility posture
Internal migration only; no user-facing behavior changes are allowed.

### Non-goals
- No changes to `ConfigManager` precedence semantics.
- No expansion of sanctioned boundary modules beyond `core/config_manager.py` and `core/config_helpers.py`.

### Primary touchpoints
- `src/calibrated_explanations/cache/cache.py`
- `src/calibrated_explanations/parallel/parallel.py`
- `src/calibrated_explanations/core/explain/_feature_filter.py`
- `src/calibrated_explanations/core/prediction/orchestrator.py`
- `scripts/quality/check_config_manager_usage.py`
- `tests/scripts/test_check_config_manager_usage.py`
- `tests/unit/core/test_config_manager.py`

### Exact config reads to migrate and mapping
1. `cache/cache.py`
   - `Feature`: `CacheConfig.from_env(...)`
   - Read: `mgr.env("CE_CACHE")`
   - Binding accessor: `ConfigManager.env("CE_CACHE")` via module singleton `_get_cache_config_manager()`.
2. `parallel/parallel.py`
   - `ParallelConfig.from_env(...)`
   - Read: `mgr.env("CE_PARALLEL")`
   - Binding accessor: `ConfigManager.env("CE_PARALLEL")` via `_get_parallel_config_manager()`.
   - Runtime checks in `ParallelExecutor`: `self._config_manager.env("CE_PARALLEL_MIN_BATCH_SIZE")`, `env("CI")`, `env("GITHUB_ACTIONS")`.
3. `core/explain/_feature_filter.py`
   - `FeatureFilterConfig.from_base_and_env(...)`
   - Reads: `mgr.env("CE_STRICT_OBSERVABILITY")`, `mgr.env("CE_FEATURE_FILTER")`
   - Binding accessor: `ConfigManager.env(...)` via `_get_feature_filter_config_manager()`.
4. `core/prediction/orchestrator.py`
   - `PredictionOrchestrator.__init__` and `_config_env(...)`
   - Read: `self._config_manager.env("CE_INTERVAL_PLUGIN_FALLBACKS")`
   - Binding accessor: injected/owned `ConfigManager.from_sources()` in `__init__`, with `_config_env` delegating to `.env`.

### Exact allowlist diff required
In `scripts/quality/check_config_manager_usage.py`:
- Set `_LIFECYCLE_ALLOWLIST` to empty tuple and keep it empty.
- Keep lifecycle-compliant singleton constructors (`_get_cache_config_manager`, `_get_parallel_config_manager`, `_get_feature_filter_config_manager`) in `_LifecycleViolationFinder._is_compliant`.
- Keep zero runtime exception policy: no transitional exemptions for the four modules.

### Operational definition: “allowlist reaches zero”
All of the following must be true:
1. `_LIFECYCLE_ALLOWLIST == ()`.
2. `python scripts/quality/check_config_manager_usage.py --scope targeted` returns exit code `0`.
3. `reports/config_manager_usage_report.json` has `"total_violations": 0`.
4. No module-specific transitional exemption entries exist for the four modules.

### Ordered implementation steps
1. Freeze baseline with report and tests.
2. Normalize each module to retain exactly one manager owner and no ad-hoc manager construction in helper methods.
3. Apply per-module migration updates in order: cache → parallel → feature_filter → orchestrator.
4. Remove remaining migration exemptions from checker logic.
5. Update checker tests to assert no exceptions required for these modules.

### Exact tests to add or update
- `tests/scripts/test_check_config_manager_usage.py`
  - Add assertions for empty `_LIFECYCLE_ALLOWLIST` and zero targeted violations.
- `tests/unit/core/test_config_manager.py`
  - Add module-specific ownership assertions for cache/parallel/feature_filter/orchestrator manager lifecycle.
- `tests/unit/core/test_calibrated_explainer_parallel_env.py`
  - Update for `CE_PARALLEL` snapshot ownership behavior.
- `tests/unit/core/test_orchestrator_telemetry.py`
  - Add/adjust fallback path checks for `CE_INTERVAL_PLUGIN_FALLBACKS` snapshot reads.
- `tests/unit/explain/test_fast_feature_filter.py`
  - Add/adjust config resolution checks for `CE_FEATURE_FILTER` and `CE_STRICT_OBSERVABILITY`.

### Exact validation commands
- `python scripts/quality/check_config_manager_usage.py --scope targeted --report reports/config_manager_usage_report.json`
- `pytest -q tests/scripts/test_check_config_manager_usage.py`
- `pytest -q tests/unit/core/test_config_manager.py`
- `pytest -q tests/unit/core/test_calibrated_explainer_parallel_env.py`
- `pytest -q tests/unit/core/test_orchestrator_telemetry.py`
- `pytest -q tests/unit/explain/test_fast_feature_filter.py`
- `make local-checks-pr`

### Exact release artifacts / docs to produce
- `reports/config_manager_usage_report.json` (zero-violation evidence artifact).
- Task status note in this file citing command outputs.

### Detailed verification checklist
- [x] All four modules resolve configuration only through `ConfigManager` owners.
- [x] No direct `os.getenv`, `os.environ`, or `read_pyproject_section` calls remain in the four modules.
- [x] Checker reports zero targeted violations and writes zero-count report payload.
- [x] Checker/unit tests listed above pass.
- [x] `make local-checks-pr` passes.

### Status-integrity rule
Task 1 can be marked complete only when all four modules are migrated **and** allowlist-zero evidence exists. Completing 3/4 modules is partial and must remain `in progress`.

### Task 1 completion status
**Status**: ✅ **Complete** (2026-04-19)

**Evidence**:
- All four modules verified zero-violation via `scripts/quality/check_config_manager_usage.py --check` (report: `reports/config_manager_usage_report.json`)
- 40 required tests pass: `pytest tests/scripts/test_check_config_manager_usage.py tests/unit/core/test_config_manager.py tests/unit/core/test_calibrated_explainer_parallel_env.py tests/unit/core/test_orchestrator_telemetry.py tests/unit/explain/test_fast_feature_filter.py -q --no-cov` → 40 passed in 2.64s
- Full suite passes: 1595 tests pass with zero ConfigManager violations and zero allowlisted migration exceptions  
- Private member enforcement: Zero violations in tests via `scripts/anti-pattern-analysis/scan_private_usage.py tests --check`
- `make local-checks-pr` / `python scripts/local_checks.py --skip-main`: pass (`Exit: 0`) after docs warning mend in `docs/foundations/concepts/guarded_explanations.md`

---

## 2) ADR-034 post-acceptance runtime conformance closure

### Goal
Close ADR-034 conformance by synchronizing accepted-state implementation evidence across ADR and release-plan docs.

### Why this task exists
ADR-034 is already Accepted (2026-04-07). v0.11.2 work is implementation-conformance closure, not status promotion.

Scope guard: this task closes ADR-034 **runtime migration/conformance** work for v0.11.2 only. It does not claim full ADR-034 closure because ADR-034 open items still track sensitive-value redaction (v1.0) and export-payload schema versioning (v1.0.0-rc).

### Governing references
- `docs/improvement/adrs/ADR-034-centralized-configuration-management.md`.
- `docs/improvement/RELEASE_PLAN_v1.md` ADR-034 summary line.
- `docs/improvement/RELEASE_PLAN_status_appendix.md` ADR-034 status/gap rows.

### Hardening findings incorporated
1. Remove stale “Proposed→Accepted path” wording from active plan surfaces.
2. Insert a structured implementation summary template in ADR-034.
3. Make Task 1 outputs explicit prerequisites.

### Compatibility posture
Documentation and governance synchronization only.

### Non-goals
- No code changes in runtime modules.
- No ADR status transition workflow edits.

### Primary touchpoints
- `docs/improvement/adrs/ADR-034-centralized-configuration-management.md`
- `docs/improvement/RELEASE_PLAN_v1.md`
- `docs/improvement/RELEASE_PLAN_status_appendix.md`
- `docs/improvement/v0.11.2_plan.md`

### Exact ADR sections to update
In ADR-034:
- `## Delivery Governance`
- `## Consequences` (accepted-state wording accuracy)
- `## Open Items` (ensure only v1.0 items remain open)

### Binding implementation-summary template (insert into ADR-034)
```markdown
### Implementation summary (v0.11.1–v0.11.2)
- Phase A (v0.11.1): <completed module list>
- Phase B (v0.11.2): <completed module list>
- CI enforcement: `scripts/quality/check_config_manager_usage.py` reports zero violations as of <date>
- Remaining deferred items: <explicit list limited to ADR-034 Open Items>
- Evidence commands: <exact command list>
```

### Exact stale wording to remove
- Remove status-table phrasing in `RELEASE_PLAN_status_appendix.md`: `ADR-034 | Proposed→Accepted path | Promote after v0.11.2 Phase B verification`.
- Replace with accepted-state wording tied to implementation closure.

### Task 1 prerequisite outputs required
1. Zero-violation `reports/config_manager_usage_report.json`.
2. Passing checker test (`tests/scripts/test_check_config_manager_usage.py`).
3. Passing module-level config tests listed in Task 1.

### Ordered implementation steps
1. Verify Task 1 prerequisites and capture command outputs.
2. Update ADR-034 with implementation summary template populated with concrete module names.
3. Update RELEASE_PLAN_v1 ADR-034 summary to accepted-state execution wording.
4. Update status appendix ADR-034 row/gap wording to remove promotion language.
5. Update this plan’s Task 2 status note with evidence references.

### Exact tests/validation commands
- `python scripts/quality/check_config_manager_usage.py --scope targeted --report reports/config_manager_usage_report.json`
- `pytest -q tests/scripts/test_check_config_manager_usage.py`
- `make local-checks-pr`

### Exact release artifacts / docs to produce
- ADR-034 implementation summary section (new subsection in ADR file).
- Updated ADR-034 rows/wording in both release plan documents.

### Detailed verification checklist
- [x] ADR-034 remains `Status: Accepted` and contains implementation summary subsection.
- [x] No release-plan doc contains “Proposed→Accepted path” wording for ADR-034.
- [x] Task 1 evidence is linked in Task 2 status note.
- [x] `make local-checks-pr` passes.

### Status-integrity rule
Task 2 is complete only when all three docs (ADR, release plan, status appendix) are synchronized and Task 1 evidence is attached.

### Task 2 completion status
**Status**: ✅ **Complete** (2026-04-20)

**Evidence**:
- Task 1 prerequisite evidence re-verified:
  - `python scripts/quality/check_config_manager_usage.py --scope targeted --report reports/config_manager_usage_report.json` -> pass (zero violations)
  - `python -m pytest -q tests/scripts/test_check_config_manager_usage.py -o addopts= --no-cov` -> pass (11 tests)
  - `make local-checks-pr` -> pass (`python scripts/local_checks.py --skip-main` exit 0)
- ADR updated with accepted-state implementation summary and deferred-item boundary in `ADR-034-centralized-configuration-management.md`.
- Release-plan surfaces synchronized:
  - `RELEASE_PLAN_v1.md` ADR-034 summary updated to accepted-state conformance wording.
  - `RELEASE_PLAN_status_appendix.md` ADR-034 row and detailed gap table updated; stale promotion wording removed.

**Task 1 evidence linkage**:
- Zero-violation artifact: `reports/config_manager_usage_report.json`
- Task 1 completion note and checklist: Section `Task 1 completion status` in this plan.

---

## 3) ADR and standards governance sweep

### Goal
Use `RELEASE_PLAN_status_appendix.md` as the detailed gap source of truth; ensure every open gap has milestone ownership, and close designated v0.11.2 quick wins.

### Why this task exists
The detailed inventory lives in the status appendix; milestone-control summaries live in RELEASE_PLAN_v1. Ownership ambiguity and stale dates block release governance clarity.

### Governing references
- ADR-004, ADR-001, ADR-005, ADR-006, ADR-008, ADR-009, ADR-010, ADR-011, ADR-012, ADR-013, ADR-015, ADR-020, ADR-026, ADR-027, ADR-028, ADR-029, ADR-030, ADR-033, ADR-034, STD-001, STD-002, STD-005 (open gap inventory).
- `docs/improvement/RELEASE_PLAN_status_appendix.md` (detailed inventory, authoritative for this task).
- `docs/improvement/RELEASE_PLAN_v1.md` (milestone-control summary).

### Hardening findings incorporated
1. Binding source-of-truth split: appendix for gaps; master plan for control summary.
2. Remove vague "any other gap" language; define explicit quick-win inventory and criteria.
3. ADR-026 gap 2 and gap 3 code was already compliant since v0.11.1; tests were the only missing artifacts — no src changes required.
4. ADR-004 gap 1 naming drift was present in `ADR-001-core-decomposition-boundaries.md` (primary fix) and `RELEASE_PLAN_v1.md` changelog (secondary fix) — both corrected.
5. ADR-004 gap 2 (severity 9) is above quick-win threshold; explicitly deferred to v1.0.0-rc with blocker text added to appendix.

### Compatibility posture
Governance/documentation updates plus narrow quick-win fixes only.

### Non-goals
- No architecture redesign tasks beyond quick-win threshold.
- No new milestone creation.

### Explicit quick-win gaps to review
1. ADR-004 gap 1: documentation naming drift (`ParallelExecutor` naming consistency) — **CLOSED v0.11.2**.
2. ADR-026 gap 2: telemetry omits `interval_dependencies` — **CLOSED v0.11.2** (test added; code was already compliant).
3. ADR-026 gap 3: Mondrian bins mutability at request construction — **CLOSED v0.11.2** (test added; `ExplanationRequest.__post_init__` already freezes bins).
4. ADR-034 status-row stale promotion wording — closed in Task 2.
5. ADR-028 gaps 1–2, ADR-029 gap 1, ADR-033 gaps 1–4, STD-005 gaps 1–2 — all **CLOSED in v0.11.1**; appendix notes updated.

### Quick-win acceptance criteria (binding)
A gap is quick-win only if all are true:
1. Unified severity `<= 6`.
2. Touches `<= 3` files under `src/` and `<= 3` tests.
3. No public API removals or new dependency introductions.
4. Can be verified with targeted pytest/script commands in one PR.

### Primary touchpoints
- `docs/improvement/RELEASE_PLAN_status_appendix.md`
- `docs/improvement/RELEASE_PLAN_v1.md`
- `docs/improvement/adrs/ADR-001-core-decomposition-boundaries.md` (ADR-004 gap 1 naming drift location)
- `tests/unit/core/explain/test_orchestrator_core.py` (ADR-026 gap 2 test)
- `tests/unit/core/prediction/test_orchestrator_extras.py` (ADR-026 gap 3 test)

### Ordered implementation steps
1. Inventory all open appendix rows and assign explicit `Target milestone: ...` text where missing.
2. Apply quick-win fixes in strict order: ADR-026 gap 2, ADR-026 gap 3, ADR-004 gap 1 doc naming.
3. Update appendix row statuses and `_Last gap analysis:` dates for touched ADR/STD sections to the current milestone-boundary date.
4. Mirror top-line milestone outcome summaries in RELEASE_PLAN_v1 without duplicating detailed tables.

### Milestone assignment recording rules
- Record in appendix row notes as `Target milestone: v0.11.2` or later explicit version.
- If deferred, include one-sentence blocker reason and exact target milestone.

### Date-refresh and status-note rules
- Update `_Last gap analysis:` only for tables changed by this task.
- Add one compliance verification line when a section reaches zero open gaps.

### Exact tests to add or update
- `tests/unit/core/explain/test_orchestrator_core.py`: ✅ Added `test_should_include_interval_dependencies_in_batch_telemetry_when_plugin_provides_hints`.
- `tests/unit/core/prediction/test_orchestrator_extras.py`: ✅ Added `test_should_freeze_mondrian_bins_at_request_construction_when_caller_mutates`.

### Exact validation commands
- `pytest -q tests/unit/core/explain/test_orchestrator_core.py -k interval_dependencies`
- `pytest -q tests/unit/core/prediction/test_orchestrator_extras.py -k mondrian`
- `pytest -q tests/unit/ -o addopts= --no-cov`

### Exact release artifacts / docs to produce
- Updated status appendix rows with target milestones and refreshed dates.
- RELEASE_PLAN_v1 summary-line sync for touched ADR rows.

### Detailed verification checklist
- [x] Every open appendix gap row includes explicit milestone ownership.
- [x] ADR-026 gap 2 and gap 3 are either closed with tests or explicitly deferred with blocker + target milestone.
- [x] ADR-004 naming drift text is resolved in docs and appendix status.
- [x] `_Last gap analysis:` dates are refreshed only for changed sections.
- [x] All 1266 unit tests pass (`pytest -q tests/unit/ -o addopts= --no-cov` → 1266 passed, 1 warning).

### Task 3 completion status
**Status**: ✅ **Complete** (2026-04-22)

**Evidence:**
- ADR-026 gap 2 closed: `pytest -q tests/unit/core/explain/test_orchestrator_core.py -k interval_dependencies` → 1 passed
- ADR-026 gap 3 closed: `pytest -q tests/unit/core/prediction/test_orchestrator_extras.py -k mondrian` → 1 passed
- ADR-004 gap 1 closed: naming drift fixed in `ADR-001-core-decomposition-boundaries.md` and `RELEASE_PLAN_v1.md`
- All appendix gap rows updated with explicit milestone ownership in `RELEASE_PLAN_status_appendix.md`
- Full unit test suite: 1266 passed, 1 warning

### Status-integrity rule
Task 3 is partial until **all** open rows in touched sections have milestone assignment text and quick-win outcomes are test-backed.

---

## 4) Governance dashboard extension (derived CI artifact)

### Goal
Add a machine-readable CI-derived governance status artifact that aggregates lint gate status and quality check summaries.

### Why this task exists
v0.11.1 established runtime governance schemas. v0.11.2 adds a derived CI aggregation artifact for release visibility; it does not replace runtime governance events.

### Governing references
- ADR-028 observability intent — governance evidence surface.
- ADR-034 governance config lifecycle context — source of config_manager_usage report.
- ADR-035 CI workflow governance — applies for two distinct reasons:
  1. **`scripts/local_checks.py` is an ADR-035-governed file** (ADR-035 §1 names it explicitly). Any change to it must also update Makefile targets (ADR-035 §3 local reproducibility parity). A `make governance-status` target is therefore required alongside the `local_checks.py` step addition.
  2. **The manual CI wiring step** (adding the producer call and `upload-artifact` action to `.github/workflows/ci.yml`) touches an ADR-035-governed path. When that PR is made it must satisfy ADR-035 §2 merge-blocking criteria: `ci-policy/validate-workflows` pass, CODEOWNERS approval, and a CI checklist with rationale.
- `RELEASE_PLAN_v1.md` v0.11.2 scope.

### Hardening findings incorporated (red-team 2026-04-22)
1. **`telemetry_summary` with event counts is unachievable locally** — no governance event count fixture reports exist. Replaced with `schema_checks` section aggregating `ok` status from existing quality report files.
2. **`run.*` env vars are CI-only** — producer uses `GITHUB_WORKFLOW`, `GITHUB_RUN_ID`, `GITHUB_SHA` with explicit `null` fallbacks when not in CI environment.
3. **CI wiring requires `.github/workflows/ci.yml` edit** — cannot be automated from local tooling; documented as a manual step in artifact docs.
4. **`lint.ruff` unavailable locally** — lint fields accept `"unavailable"` state via `enum: [passed, failed, unavailable]` in schema.
5. **ADR-035 not referenced in original plan** — added to governing references.
6. **Config manager report uses `total_violations` not `ok`** — producer handles both report formats.
7. **Schema must have `additionalProperties: false`** — required for ADR-028 governance hardening consistency with existing schemas.
8. **`test_check_governance_event_schema.py` update concern** — "not in runtime package" assertion placed in the new producer test file to avoid cross-file concern mixing; plan updated accordingly.

### Compatibility posture
CI/reporting-only addition; no runtime event contract changes.

### Non-goals
- No replacement of `governance_event_schema_v1.json` or `governance_config_event_schema_v1.json`.
- No runtime logger/event vocabulary changes.
- No governance event count aggregation (requires CI log capture; deferred to v1.0.0-rc).

### Primary touchpoints
- Schema file: `docs/improvement/schemas/governance_status_schema_v1.json` (new)
- Producer script: `scripts/quality/build_governance_status_artifact.py` (new)
- Local checks integration: `scripts/local_checks.py` (add aggregation step)
- Artifact output path: `reports/governance/governance_status.json`
- Artifact docs: `docs/improvement/governance_status_artifact.md` (new)
- Tests: `tests/scripts/test_build_governance_status_artifact.py` (new)

### Binding JSON payload structure
```json
{
  "schema_version": "1.0",
  "generated_at": "2026-04-22T00:00:00+00:00",
  "run": {
    "workflow": null,
    "run_id": null,
    "commit": null
  },
  "lint": {
    "local_checks_pr": "unavailable",
    "mypy": "unavailable",
    "ruff": "unavailable"
  },
  "schema_checks": {
    "governance_event_schema": "passed",
    "config_manager_usage": "passed",
    "logging_domains": "passed",
    "no_local_paths": "passed"
  }
}
```

### Input report file to schema_checks mapping (binding)
| `schema_checks` key | Input report path | `ok` derivation |
| --- | --- | --- |
| `governance_event_schema` | `reports/quality/governance_event_schema_report.json` | `ok` field |
| `config_manager_usage` | `reports/config_manager_usage_report.json` | `total_violations == 0` |
| `logging_domains` | `reports/quality/logging_domain_report.json` | `ok` field (or `total_violations == 0`) |
| `no_local_paths` | `reports/quality/no_local_paths_report.json` | `ok` field |

If a report file is absent: value = `"unavailable"`. If `ok` is missing and `total_violations` is present: derive from `total_violations == 0`.

### Lint status sourcing rules (binding)
- `lint.*` values are passed via CLI flags (`--lint-local-checks-pr`, `--lint-mypy`, `--lint-ruff`) from CI.
- Default value when flags are absent: `"unavailable"`.

### Ordered implementation steps
1. Create schema file with `additionalProperties: false` at every object boundary.
2. Implement producer script that: reads existing quality reports, builds artifact payload, validates against schema, writes output.
3. Add aggregation step to `local_checks.py` in both PR and main steps after existing governance schema check.
4. Create artifact retrieval/retention docs.
5. Add tests with schema conformance and fail-closed assertions.
6. Note CI wiring as manual step in docs (requires editing `.github/workflows/ci.yml` to call producer with lint status flags and upload artifact).

### Exact tests to add or update
- `tests/scripts/test_build_governance_status_artifact.py` (new):
  - `test_should_build_valid_artifact_when_all_reports_present`
  - `test_should_return_unavailable_when_report_missing`
  - `test_should_return_failed_when_report_ok_false`
  - `test_should_return_passed_for_config_manager_zero_violations`
  - `test_should_build_artifact_with_required_keys`
  - `test_should_validate_artifact_against_schema`
  - `test_should_write_output_to_specified_path`
  - `test_should_fail_closed_on_schema_violation`
  - `test_should_assert_governance_status_schema_not_in_runtime_package`

### Exact validation commands
- `python scripts/quality/build_governance_status_artifact.py --output reports/governance/governance_status.json --validate`
- `pytest -q tests/scripts/test_build_governance_status_artifact.py -o addopts= --no-cov`

### Exact release artifacts / docs to produce
- `reports/governance/governance_status.json`
- `docs/improvement/schemas/governance_status_schema_v1.json`
- `docs/improvement/governance_status_artifact.md`

### Detailed verification checklist
- [x] Schema file exists with `additionalProperties: false` and enum-constrained lint/schema_checks fields.
- [x] Producer aggregates `ok`/`total_violations` from all four source reports.
- [x] Producer writes `reports/governance/governance_status.json` with correct payload.
- [x] Producer validates output against schema file.
- [x] `local_checks.py` includes governance status aggregation step after governance schema check.
- [x] `Makefile` includes `governance-status` target (ADR-035 §3 local reproducibility parity).
- [x] Tests pass: `pytest -q tests/scripts/test_build_governance_status_artifact.py`.
- [x] Artifact docs exist at `docs/improvement/governance_status_artifact.md`.
- [x] Governance status schema is NOT in `src/calibrated_explanations/schemas/`.

### Status-integrity rule
Task 4 cannot be marked complete until schema, producer, `local_checks.py`
  - `test_should_assert_governance_status_schema_not_in_runtime_package`

### Exact validation commands
- `python scripts/quality/build_governance_status_artifact.py --output reports/governance/governance_status.json --validate`
- `pytest -q tests/scripts/test_build_governance_status_artifact.py -o addopts= --no-cov`

### Exact release artifacts / docs to produce
- `reports/governance/governance_status.json`
- `docs/improvement/schemas/governance_status_schema_v1.json`
- `docs/improvement/governance_status_artifact.md`

### Detailed verification checklist
- [x] Schema file exists with `additionalProperties: false` and enum-constrained lint/schema_checks fields.
- [x] Producer aggregates `ok`/`total_violations` from all four source reports.
- [x] Producer writes `reports/governance/governance_status.json` with correct payload.
- [x] Producer validates output against schema file.
- [x] `local_checks.py` includes governance status aggregation step after governance schema check.
- [x] `Makefile` includes `governance-status` target (ADR-035 §3 local reproducibility parity).
- [x] Tests pass: `pytest -q tests/scripts/test_build_governance_status_artifact.py` → 15 passed.
- [x] Artifact docs exist at `docs/improvement/governance_status_artifact.md`.
- [x] Governance status schema is NOT in `src/calibrated_explanations/schemas/`.

### Status-integrity rule
Task 4 cannot be marked complete until schema, producer, `local_checks.py` wiring, docs, and tests are all present.

### Task 4 completion
**Status: COMPLETE — 2026-04-22**
Evidence:
- `docs/improvement/schemas/governance_status_schema_v1.json` — schema with `additionalProperties: false`, enum-constrained fields.
- `scripts/quality/build_governance_status_artifact.py` — producer aggregating 4 report sources.
- `tests/scripts/test_build_governance_status_artifact.py` — 15 tests, all passing.
- `docs/improvement/governance_status_artifact.md` — artifact documentation including CI wiring guide.
- `scripts/local_checks.py` — governance status aggregation step added after governance schema check (PR and main steps).
- `Makefile` — `governance-status` target added (ADR-035 §3 local reproducibility parity).
- `reports/governance/governance_status.json` — sample output with all 4 `schema_checks` showing `"passed"`.

---

## 5) CI workflow decommissioning (legacy wrappers) — v0.11.1 closure + v0.11.2 post-5A hygiene

The decommissioning of legacy CI wrapper workflows was completed in v0.11.1 (v0.11.1 plan Task 10).
v0.11.2 adds a **post-5A CI hygiene and governance closure** sweep.

### Hardening findings incorporated (red-team 2026-04-22)

1. **§5 had no post-5A hygiene gate**: The plan previously said "completed in v0.11.1" and treated §5 as fully closed, but never verified that the CI decommissioning remains correct AFTER the 5A removals. Specifically, `deprecation-check.yml` runs with `CE_DEPRECATIONS=error` — after 5A, 8 symbols now raise `AttributeError` instead of `DeprecationWarning`. Verification pass added below.
2. **`remediation_api_changes.md` stale state**: Lines 15 and 62 still list `preload_lime`, `preload_shap`, `is_lime_enabled`, `is_shap_enabled` as "Added public API surface". These were removed in v0.11.2 Task 5A. A v0.11.2 removal note must be added to that doc.
3. **§5 governance format gap**: All other completed tasks (1–4, 5A) have structured completion status blocks. §5 lacked one entirely. Added below.
4. **List-path API "complete migration by v0.11.2" ambiguity**: RELEASE_PLAN_v1.md line 404 says "complete migration by v0.11.2" for `register`, `trust_plugin`, `find_for`, `find_for_trusted`. The binding interpretation for this milestone is: migration documentation is complete and deprecation warnings are in place (done in v0.11.1); code removal remains on the explicit v0.11.3 timeline.
5. **No CI workflow scan recorded**: After 5A, CI workflow files were audited for removed symbol references. Zero matches found — all workflows are clean.
6. **Local-gate env leakage risk**: `Core tests (no viz/no cov)` in `scripts/local_checks.py` inherited shell `CE_DEPRECATIONS`. If a user shell exported `CE_DEPRECATIONS=error`, wrappers expecting `pytest.warns(DeprecationWarning)` could fail spuriously. The gate runner now sanitizes `CE_DEPRECATIONS` for non-deprecation core test steps.

### Post-5A verification scope

1. Confirm `deprecation-check.yml` behavior is correct after 5A symbol removals.
2. Confirm no CI workflow file references any of the 8 removed symbols.
3. Confirm `CE_DEPRECATIONS=error` still correctly elevates the remaining active v0.11.3 deprecations.
4. Add removal note to `docs/improvement/remediation_api_changes.md`.
5. Confirm `scripts/local_checks.py` isolates non-deprecation core tests from shell-level `CE_DEPRECATIONS`.

### Binding interpretation: list-path API "complete migration by v0.11.2"

`RELEASE_PLAN_v1.md` line 404: "complete migration by v0.11.2, remove residual list-path compatibility code in v0.11.3."

**Binding for this milestone:** "complete migration" means migration documentation is finalized and deprecation warnings are active (delivered in v0.11.1). Code removal of `register`, `trust_plugin`, `find_for`, `find_for_trusted` is explicitly deferred to v0.11.3. This interpretation is recorded here so v0.11.3 is not confused about what "residual" means.

### Exact validation commands
- `pytest -q tests/unit/core/test_lime_shap_core_deprecation.py -o addopts= --no-cov`
- `Select-String -Path ".github/workflows/*.yml" -Pattern "explain_lime|explain_shap|preload_lime|preload_shap|is_lime_enabled|is_shap_enabled"`
- `Remove-Item Env:CE_DEPRECATIONS -ErrorAction SilentlyContinue; cmd /c make local-checks-pr`

### Detailed verification checklist
- [x] No CI workflow file references any of the 8 removed LIME/SHAP symbols (verified: zero matches in `.github/workflows/`).
- [x] `deprecation-check.yml` continues to work correctly: removed-symbol tests assert `AttributeError`; active v0.11.3 deprecations still emit correctly under `CE_DEPRECATIONS=error`.
- [x] `docs/improvement/remediation_api_changes.md` carries v0.11.2 removal note for `preload_lime`, `preload_shap`, `is_lime_enabled`, `is_shap_enabled`.
- [x] List-path API "complete migration by v0.11.2" is explicitly interpreted and recorded.
- [x] `scripts/local_checks.py` sanitizes `CE_DEPRECATIONS` for non-deprecation core test steps, preventing shell-env false failures.

### Task 5 completion status
**Status**: ✅ **Complete** — 2026-04-22

**v0.11.1 closure references:**
- `docs/improvement/CI-upgrade.md`
- `docs/improvement/v0.11.1_plan.md` Task 10 (decommissioned: `test.yml`, `coverage.yml`, `examples.yml`, `scan-private-members.yml`, `notebook-audit.yml`, `docs.yml`, `lint.yml`, `mypy.yml`, `dependency-audit.yml`)

**v0.11.2 post-5A hygiene evidence:**
- CI workflow audit: `grep` over `.github/workflows/` returns zero hits for any of the 8 removed LIME/SHAP symbol names.
- `deprecation-check.yml` behavioral verification: `pytest tests/unit -m "not viz" -q --maxfail=1 --no-cov` with `CE_DEPRECATIONS=error` passes — removed-method tests assert `AttributeError` (unaffected by CE_DEPRECATIONS); active v0.11.3 deprecations still elevate correctly.
- `remediation_api_changes.md` updated with v0.11.2 removal note.
- List-path API migration interpretation recorded in this section.
- Gate determinism hardening: `scripts/local_checks.py` now removes inherited `CE_DEPRECATIONS` for `Core tests (no viz/no cov)` and `Core tests with coverage` so local shells cannot force false failures in deprecation-warning assertions.

---

## 5A) Task 21 v0.11.2 removal-phase follow-through (core and wrapper LIME/SHAP)

### Goal
Execute the v0.11.2 removal phase mandated by v0.11.1 Task 21 and `docs/migration/deprecations.md`, leaving v0.11.3-owned residual removals on their documented timeline.

### Why this task exists
v0.11.x has a binding zero-active-deprecation trajectory before v1.0.0. Task 21 explicitly assigns these removals to v0.11.2.

Scope guard: Task 5A closes the **v0.11.2 removal phase** only; it does not claim full Task-21 closure across v0.11.3-owned surfaces.

### Governing references
- `docs/improvement/v0.11.1_plan.md` Task 21.
- `docs/migration/deprecations.md` Task 21 inventory rows.
- ADR-011 and ADR-020.
- `docs/improvement/legacy_user_api_contract.md` (ADR-020 canonical legacy contract surface).

### Hardening findings incorporated (red-team 2026-04-22)
1. **Task 5 vs Task 5A ambiguity**: In `RELEASE_PLAN_v1.md`, v0.11.2 Task 5 is deep-memory hardening. The actual LIME/SHAP removal work is this plan's **Task 5A**. This task explicitly executes Task 21 removal-phase scope to avoid cross-task drift.
2. **ADR-020 guard requires evidence, not assumption**: The eight v0.11.2 removal symbols are not listed in `legacy_user_api_contract.md`; this is now recorded as explicit contract-outside evidence.
3. **Fail-closed behavior must be hard removal**: deprecation warnings are insufficient in v0.11.2; methods must be physically deleted so call-sites raise `AttributeError`.
4. **Collection adapters are out-of-scope for v0.11.2**: `CalibratedExplanations.as_lime`/`as_shap` remain active deprecations with v0.11.3 ETA and must not be removed here.
5. **Coverage tests had stale deprecation assumptions**: existing tests in core/wrapper integration suites still expected warning-based behavior and had to be converted to fail-closed `AttributeError` assertions.

### Compatibility posture
This task intentionally introduces **planned public-surface removals** for symbols with v0.11.2 removal ETA; this is the only allowed compatibility break class in this milestone.

### Non-goals
- Do not remove v0.11.3-owned symbols (`CalibratedExplanations.as_lime`, `CalibratedExplanations.as_shap`).

### Exact symbols to remove in v0.11.2
1. `CalibratedExplainer.preload_lime`
2. `CalibratedExplainer.preload_shap`
3. `CalibratedExplainer.explain_lime`
4. `CalibratedExplainer.explain_shap`
5. `CalibratedExplainer.is_lime_enabled`
6. `CalibratedExplainer.is_shap_enabled`
7. `WrapCalibratedExplainer.explain_lime`
8. `WrapCalibratedExplainer.explain_shap`

### Symbols explicitly not removed in v0.11.2
The legacy list-path APIs remain on the v0.11.3 timeline and stay in deprecation-only status during v0.11.2:

- `register`
- `trust_plugin`
- `find_for`
- `find_for_trusted`

### Primary touchpoints
- `src/calibrated_explanations/core/calibrated_explainer.py`
- `src/calibrated_explanations/core/wrap_explainer.py`
- `src/calibrated_explanations/__init__.py`
- `src/calibrated_explanations/core/__init__.py`
- `docs/migration/deprecations.md`
- `tests/unit/core/test_lime_shap_core_deprecation.py`
- `tests/integration/plugins/test_interval_pyproject_integration.py` (plugin-only continuity)

### Fail-closed expectations per symbol
- Removed methods accessed on explainer/wrapper instances: `AttributeError`.
- Removed direct imports from module surfaces: `ImportError`.

### Ordered implementation steps
1. Run ADR-020 legacy-contract guard before code deletion.
   - For each candidate v0.11.2 symbol, check whether it appears in `docs/improvement/legacy_user_api_contract.md`.
   - If the symbol appears, require explicit ADR-backed approval for pre-v1.0 removal and include same-PR contract-doc and test updates.
   - If the symbol does not appear, record evidence in the Task 5A removal checklist/status note.
2. Delete v0.11.2-scoped methods from explainer and wrapper.
3. Update package export surfaces and lazy `__getattr__` routing to avoid stale exposures.
4. Update migration docs:
   - move removed rows from **Active deprecations** to **Removed deprecations (history)**
   - preserve v0.11.3-owned rows in Active, including legacy list-path APIs.
5. Add fail-closed tests and plugin-only path continuity tests.

### Exact tests to add or update
- `tests/unit/core/test_lime_shap_core_deprecation.py`
  - convert v0.11.2 symbols from deprecation-warning expectations to `AttributeError` expectations.
- `tests/integration/plugins/test_interval_pyproject_integration.py`
  - assert plugin-manager registration/discovery remains functional with legacy list-path deprecations still present and plugin-only replacements unaffected.

### Exact validation commands
- `pytest -q tests/unit/core/test_lime_shap_core_deprecation.py`
- `pytest -q tests/integration/plugins/test_interval_pyproject_integration.py`
- `make local-checks-pr`

### Exact release artifacts / docs to produce
- Updated `docs/migration/deprecations.md` with row moves.
- v0.11.2 plan status note linking removal verification commands.

### Detailed verification checklist
- [x] All eight v0.11.2 core/wrapper LIME/SHAP symbols are deleted.
- [x] ADR-020 contract guard evidence is recorded for each removed symbol (all eight symbols absent from `legacy_user_api_contract.md`).
- [x] Fail-closed behavior is test-covered and green.
- [x] Plugin-only replacement paths remain functional and test-covered.
- [x] Deprecations ledger rows are moved from Active to Removed history for removed symbols, while list-path API rows remain Active with v0.11.3 removal ETA. (Red-team 2026-04-22: `register`, `trust_plugin`, `find_for`, `find_for_trusted` were absent from the formal Active table despite emitting `deprecate()` warnings; added in mend.)
- [x] `make local-checks-pr` passes.

Validation note (2026-04-22): `make local-checks-pr` completes successfully (invoked via `cmd /c make local-checks-pr`) after mending unrelated baseline quality issues (`_make_invoke_explainer_stub` private-member usage and ADR-030 "test without assertion" in `tests/scripts/test_build_governance_status_artifact.py`).

### Status-integrity rule
Task 5A is complete only when ADR-020 contract-guard evidence is present for every removed symbol, the eight v0.11.2 symbols are removed, fail-closed tests are green, plugin-only continuity is verified, and the ledger move excludes v0.11.3-owned list-path rows.

### Task 5A completion status
**Status**: ✅ **Complete** — 2026-04-22

**Evidence:**
- Core removals landed in `src/calibrated_explanations/core/calibrated_explainer.py` (deleted: `preload_lime`, `preload_shap`, `explain_lime`, `explain_shap`, `is_lime_enabled`, `is_shap_enabled`).
- Wrapper removals landed in `src/calibrated_explanations/core/wrap_explainer.py` (deleted: `explain_lime`, `explain_shap`).
- Fail-closed tests updated and green:
  - `pytest -q tests/unit/core/test_lime_shap_core_deprecation.py -o addopts= --no-cov` → pass
  - `pytest -q tests/unit/core/test_calibrated_explainer_coverage.py -o addopts= --no-cov` → pass
  - `pytest -q tests/unit/core/test_wrap_explainer_helpers.py -o addopts= --no-cov` → pass
  - `pytest -q tests/unit/test_wrap_explainer_metadata_shap.py -o addopts= --no-cov` → pass
  - `pytest -q tests/integration/core/test_framework.py -o addopts= --no-cov` → pass
- Task-targeted plugin continuity check green:
  - `pytest -q tests/integration/plugins/test_interval_pyproject_integration.py -o addopts= --no-cov` → pass
- Deprecation ledger migrated in `docs/migration/deprecations.md`:
  - eight v0.11.2 core/wrapper rows moved from **Active deprecations** to **Removed deprecations (history)**
  - `CalibratedExplanations.as_lime`/`as_shap` remain active with v0.11.3 removal ETA.

---

## 6) Deep memory audit and retention hardening

### Goal
Deliver bounded-memory behavior for long-lived processes with explicit lifecycle semantics and measurable RSS stabilization.

### Why this task exists
The audit identified retention growth through explanation retention, optional integration caches, and executor lifecycle state.

### Governing references
- ADR-003 (caching strategy)
- ADR-004 (parallel lifecycle)
- ADR-028 (observability for retention events)

### Hardening findings incorporated (binding design choices)
1. **`latest_explanation` design:** retain only the most recent explanation object and clear it in `reset()` and `close()`.
2. **SHAP/LIME caching design:** keep one cached helper instance each (`LimeHelper`, `ShapHelper`) and require explicit reset on `CalibratedExplainer.reset()`.
3. **Plugin/cache bounding design:** bound plugin manager explanation-instance cache to max 16 entries with LRU eviction.
4. **Parallel path resolution:** authoritative path is `src/calibrated_explanations/parallel/parallel.py` (not `core/parallel/parallel.py`).
5. Numeric RSS thresholds below are **v0.11.2 plan policy gates** derived from the deep-memory audit objective; they are not ADR-stated constants.

### Compatibility posture
Additive lifecycle hardening (`reset` semantics clarified) with no API removals.

### Non-goals
- No cross-process shared-memory retention coordination.
- No redesign of explanation payload model.

### Primary touchpoints
- `src/calibrated_explanations/core/calibrated_explainer.py`
- `src/calibrated_explanations/integrations/lime.py`
- `src/calibrated_explanations/integrations/shap.py`
- `src/calibrated_explanations/parallel/parallel.py`
- `src/calibrated_explanations/plugins/manager.py`
- `tests/test_reject_memory.py`
- `tests/unit/core/explain/test_parallel_lifecycle.py`
- `tests/unit/core/test_feature_filter_smoke.py` (retention side effects check)
- New: `tests/unit/core/test_memory_retention.py`

### Exact retention sites to audit
1. `CalibratedExplainer.latest_explanation`
2. `LimeHelper._explainer_instance`, `LimeHelper._reference_explanation`
3. `ShapHelper._explainer_instance`, `ShapHelper._reference_explanation`
4. `ParallelExecutor.pool`
5. `PluginManager._explanation_plugin_instances`

### Ordered implementation steps
1. Define lifecycle contract docstrings for `reset()` and `close()`.
2. Ensure `reset()` clears latest explanation + SHAP/LIME caches + plugin manager cache entries.
3. Ensure `close()` calls `reset()` and additionally shuts down active process/thread pools.
4. Add bounded LRU policy to plugin manager cached explanation instances.
5. Add stress harness tests and RSS assertions.

### reset()/close() expected state clearing
- `reset()` must clear: `latest_explanation`, helper caches, plugin cached explanation instances.
- `close()` must clear everything from `reset()` **plus** terminate `ParallelExecutor.pool` and set pool handle to `None`.

### Memory acceptance criteria (numerical, v0.11.2 plan policy gate)
- Test scenario: 200 explain/predict iterations alternating explain modes.
- RSS stability definition: after first 50 warm-up iterations, max RSS delta across remaining iterations is `<= 10%` and absolute growth `<= 64 MB`.

### Exact tests to add or update
- New `tests/unit/core/test_memory_retention.py`
  - test_reset_clears_latest_explanation_and_helper_caches
  - test_close_clears_pool_and_cached_state
  - test_plugin_cache_bounded_to_max_entries
- Update `tests/test_reject_memory.py`
  - add warm-up/steady-state RSS assertions with numeric thresholds.
- Update `tests/unit/core/explain/test_parallel_lifecycle.py`
  - assert pool teardown semantics on `close()`.

### Exact validation commands
- `pytest -q tests/unit/core/test_memory_retention.py`
- `pytest -q tests/test_reject_memory.py`
- `pytest -q tests/unit/core/explain/test_parallel_lifecycle.py`
- `make local-checks-pr`

### Exact release artifacts / docs to produce
- `reports/memory/v0.11.2_retention_report.md` containing RSS plots/table and pass/fail thresholds.

### Detailed verification checklist
- [x] `reset()` and `close()` state-clearing semantics are implemented as specified.
- [x] SHAP/LIME caches are cleared by `reset()`.
- [x] Parallel pools are terminated by `close()`.
- [x] Plugin explanation-instance cache is bounded and eviction-tested.
- [x] RSS stabilization criteria are met in test harness.
- [x] `make local-checks-pr` passes.

### Status-integrity rule
Task 6 cannot be marked complete without the numeric RSS report artifact and passing memory tests.

### Task 6 completion status
**Status**: ✅ **Complete** (2026-04-22)

**Red-team scope expansion and resolution**:
- SHAP/LIME public explainer APIs were removed in Task 5A; Task 6 therefore treats SHAP/LIME only as helper-cache lifecycle concerns (`LimeHelper`/`ShapHelper` cached explainer/reference state).
- Added explicit lifecycle semantics in `CalibratedExplainer`: `reset()` now clears transient memory-retention surfaces (latest explanation, helper caches, plugin caches/contexts/monitors), and `close()` calls `reset()` before parallel teardown.
- Bounded explanation plugin instance cache to max 16 entries with LRU eviction semantics in `PluginManager`.

**Evidence**:
- Runtime implementation:
  - `src/calibrated_explanations/core/calibrated_explainer.py` (`reset()`, `close()` lifecycle hardening)
  - `src/calibrated_explanations/plugins/manager.py` (bounded LRU explanation-instance cache)
  - `src/calibrated_explanations/core/explain/orchestrator.py` (cache accessor wiring compatible with manager cache contract)
- Tests added/updated:
  - `tests/unit/core/test_memory_retention.py`
  - `tests/unit/plugins/test_manager.py`
  - `tests/unit/core/explain/test_parallel_lifecycle.py`
  - `tests/test_reject_memory.py`
- Numeric retention artifact:
  - `reports/memory/v0.11.2_retention_report.md`
  - Host measurement: baseline 171,077,632 bytes; peak 184,598,528 bytes; absolute growth 13,520,896 bytes; relative growth 7.90% (both thresholds pass).
- Validation commands:
  - `.venv\\Scripts\\python.exe -m pytest -q --no-cov tests/unit/core/test_memory_retention.py tests/test_reject_memory.py tests/unit/core/explain/test_parallel_lifecycle.py tests/unit/plugins/test_manager.py` → pass
  - `python scripts/local_checks.py --skip-main` → pass (`Exit: 0`)

---

## 7) PlotSpec default-promotion follow-up (ADR-036 / ADR-037)

### Goal
Take a binding v0.11.2 decision to **defer PlotSpec default promotion** and record that outcome across ADR/docs/plan surfaces.

### Why this task exists
ADR-036 and ADR-037 defer default-path decision from v0.11.1 to v0.11.2 and require explicit governance recording. The current milestone must now record the **1A-family outcome**: keep legacy as the default in v0.11.2, defer all chain-based default flips, and defer explicit `plot_global` / `plot_triangular` promotion decisions to v0.11.3.

### Governing references
- `docs/improvement/adrs/ADR-036-plot-spec-canonical-contract-and-validation-boundary.md` (§10, Adoption & Migration)
- `docs/improvement/adrs/ADR-037-visualization-extension-and-rendering-governance.md` (§5, §6, Adoption & Migration)
- `docs/api/visualization.md`
- `docs/improvement/RELEASE_PLAN_v1.md`

### Hardening findings incorporated (red-team 2026-04-22)
1. Gate criteria are explicit and binary.
2. Decision note location is fixed.
3. Promotion and deferral update wording is fixed.
4. Readiness criteria below are **v0.11.2 release-plan policy decisions** (for this milestone gate) and are not claimed as direct ADR text.
5. **`plot_global` and `plot_triangular` bypass the style chain entirely.** `plot_global` hardcodes `use_legacy = kwargs.get("use_legacy", True)` and `plot_triangular` has `use_legacy=True` in its signature. Neither participates in `_resolve_plot_style_chain`. These two functions are structurally outside the chain-based promotion mechanism. The gate tests do not exercise them. "Flip default call sites" must explicitly address each function by name or explicitly exclude them with documented rationale.
6. **Gate criterion 5 was unverifiable.** The status appendix has no severity-rated ADR-036/ADR-037 gap sections. Criterion 5 has been reworded to a verifiable explicit assertion (see below).
7. **"Keep legacy opt-out flag" was under-specified.** `use_legacy` is a parameter on internal plotting helpers, not on the public `explanation.plot()` API. The plan now requires specifying the opt-in / opt-out surface explicitly.
8. **`test_plotspec_mvp.py` is `@pytest.mark.viz`.** It is excluded from `make local-checks-pr` (the gate uses `-m not viz`). Any test updates in that file will not be validated by the final gate command. Test updates must either go into a non-viz file or the gate command must be extended with `--marker viz` for the targeted suite.
9. **Deferral test target was ambiguous.** Multiple functions have different default mechanisms. The deferral test is now required to cover the chain-based functions (`plot_probabilistic`, `plot_regression`, `plot_alternative`) and document the bypass-function posture explicitly.
10. **The "no explainer" PlotSpec-first path has no test.** Lines 579, 904, and 1208 of `plotting.py` all contain `chain = ("plot_spec.default", "legacy")` when the explainer is `None`. This path already prioritizes PlotSpec but has no test coverage and is undocumented.
11. **No backward-compatibility user communication** — a visual default behavior change would require a `CHANGELOG.md` entry, so the absence of such an entry is part of the v0.11.2 non-default posture.

### Compatibility posture
No user-visible default behavior change is permitted in v0.11.2 under this task.
This task records a binding deferral only; no `CHANGELOG.md` default-behavior entry is expected from Task 7 itself.

### Non-goals
- No runtime kind-extension enablement in v0.11.2.
- No forced pixel-parity between PlotSpec and legacy paths.
- No default flip for any plot surface in v0.11.2.

### Function inventory: which functions are in scope for default-path change

Only chain-based functions that already use `_resolve_plot_style_chain` can be promoted via the chain mechanism. Bypass functions require explicit signature or call-site changes:

| Function | Mechanism | Promotion scope |
| --- | --- | --- |
| `plot_probabilistic` | chain (`use_legacy is None` → chain) | In scope |
| `plot_regression` | chain (`use_legacy is None` → chain) | In scope |
| `plot_alternative` | chain (`use_legacy is None` → chain) | In scope |
| `plot_triangular` | hardcoded `use_legacy=True` signature default | Explicit change required or explicitly excluded |
| `plot_global` | hardcoded `kwargs.get("use_legacy", True)` | Explicit change required or explicitly excluded |

The decision note **MUST** name a resolution for each function row. For v0.11.2, the binding expected outcome is:
- `plot_probabilistic`, `plot_regression`, `plot_alternative` -> `deferred to v0.11.3`
- `plot_triangular`, `plot_global` -> `deferred to v0.11.3`

### Binding readiness gate criteria (v0.11.2 plan policy)
The gate is still evaluated and recorded, but **passing the gate does not authorize promotion in v0.11.2**. The evaluation serves two purposes: (a) evidence that the deferral is not caused by missing routing plumbing, and (b) baseline evidence for the new PlotSpec mending task below.
The same five criteria must be recorded:
1. `tests/unit/viz/test_plot_parity_adapter.py` passes fully.
2. `tests/unit/viz/test_plot_parity_fixtures.py` passes fully.
3. `tests/unit/viz/test_alternative_regression_parity.py` passes fully.
4. `tests/integration/test_plot_repr_consistency.py` passes fully.
5. No ADR-036/ADR-037 conformance gaps with severity `>= 9` exist in `docs/improvement/RELEASE_PLAN_status_appendix.md` (explicitly confirmed as zero; appendix updated to state this if not already present).

**Current gate evaluation (2026-04-22):** 7/7 gate tests pass. Criterion 5 is satisfied: the appendix has no severity-rated ADR-036/ADR-037 gap sections; this is recorded as "zero open gaps at or above severity 9 confirmed" in the status note below. Despite this, default promotion is deferred because current PlotSpec-rendered plots are not yet trusted to preserve the same explanatory meaning as legacy plots in user-facing visuals.

### Primary touchpoints
- Decision log: `docs/improvement/plot_spec/v0.11.2_default_promotion_decision.md` (new)
- Runtime call sites remain unchanged for default behavior under this task
- Documentation:
  - `docs/api/visualization.md`
  - `docs/improvement/RELEASE_PLAN_v1.md`
  - ADR-036 and ADR-037 adoption sections

### Decision note required sections (binding template)
The decision note at `docs/improvement/plot_spec/v0.11.2_default_promotion_decision.md` must contain:
1. **Gate evaluation date** and exact command outputs for all 5 criteria.
2. **Per-function resolution table** — one row per function from the inventory above, with outcome and rationale.
3. **Promotion outcome** — explicitly record the **1A-family decision** (`deferred` for all five functions in v0.11.2).
4. **Deferral rationale** — state that payload correctness and routing readiness are insufficient because current PlotSpec-rendered visuals are not yet trusted to preserve legacy-equivalent explanatory meaning.
5. **Current opt-in / opt-out surface** — how PlotSpec can still be exercised without becoming the default (`use_legacy=False`, `style_override="plot_spec.default"`, `return_plot_spec=True`, env/config overrides when applicable).
6. **Follow-up milestone** — explicit v0.11.3 tracking note for both the new v0.11.2 mending task output and the v0.11.3 promotion re-evaluation task.

### Ordered implementation steps
1. Run gate suite and record exact command outputs (criterion 1–4).
2. Confirm criterion 5: check status appendix for ADR-036/ADR-037 gap sections; if absent, add an explicit "zero open gaps" note.
3. Write decision note using the binding template above.
4. Record the binding per-function outcome: defer all three chain-based functions and both bypass functions to v0.11.3.
5. Add or update tests (see below) to document the deferral invariant and current opt-in paths — **use non-viz test files or run targeted viz suite explicitly**.
6. Update visualization API doc and release plan summary lines with the chosen outcome and explicit non-default posture.
7. Update ADR-036 §10 and ADR-037 §5/Adoption & Migration with the outcome date, deferral rationale, and follow-up milestone.

### Exact tests to add or update
- Add test in `tests/unit/test_plot_default_promotion.py` (new, **outside `tests/unit/viz/`**) asserting that public default posture remains legacy for the chain-based functions in v0.11.2.
- Add assertion in the same file that passing `style_override="plot_spec.default"` or equivalent explicit opt-in still selects the PlotSpec path (opt-in remains available).
- For the "no explainer" path — add test in `tests/unit/test_plot_default_promotion.py` asserting that when explainer is `None`, the chain is `("plot_spec.default", "legacy")` (documents the already-existing PlotSpec-first no-explainer path).
- For `plot_triangular` and `plot_global` — add test asserting their current default (`use_legacy=True`) and the explicit v0.11.2 deferral resolution.
- **Note:** `tests/unit/viz/test_plotspec_mvp.py` is `@pytest.mark.viz` and is excluded from `make local-checks-pr`. If updates go there, run the targeted viz suite with `pytest -q -m viz tests/unit/viz/test_plotspec_mvp.py` separately and record output.

### Exact validation commands
- `pytest -q tests/unit/viz/test_plot_parity_adapter.py -o addopts= --no-cov`
- `pytest -q tests/unit/viz/test_plot_parity_fixtures.py -o addopts= --no-cov`
- `pytest -q tests/unit/viz/test_alternative_regression_parity.py -o addopts= --no-cov`
- `pytest -q tests/integration/test_plot_repr_consistency.py -o addopts= --no-cov`
- `pytest -q tests/unit/test_plot_default_promotion.py -o addopts= --no-cov` (new non-viz test file)
- `make local-checks-pr`

### Exact release artifacts / docs to produce
- `docs/improvement/plot_spec/v0.11.2_default_promotion_decision.md` (required, must use binding template)
- Updated `docs/improvement/RELEASE_PLAN_status_appendix.md` ADR-036/ADR-037 rows with "zero open gaps" note
- Updated `docs/api/visualization.md` with current non-default posture, opt-in mechanism, and default note
- Updated ADR-036 §10 and ADR-037 §5/Adoption & Migration adoption notes
- Updated `docs/improvement/RELEASE_PLAN_v1.md` ADR-036/ADR-037 summary lines

### Detailed verification checklist
- [x] Gate suite (criteria 1–4) passes with recorded command outputs.
- [x] Status appendix ADR-036/ADR-037 rows carry explicit "zero open severity >= 9 gaps" note.
- [x] Decision note exists at fixed path with all required template sections populated.
- [x] Per-function resolution table names every function from the inventory and records the binding v0.11.2 deferral outcome.
- [x] `plot_global` and `plot_triangular` have explicit v0.11.2 deferral outcome backed by tests.
- [x] Chain-based functions (`plot_probabilistic`, `plot_regression`, `plot_alternative`) have deferral outcome backed by tests in a non-viz test file.
- [x] "No explainer" path (`chain = ("plot_spec.default", "legacy")`) is tested.
- [x] Current opt-in / non-default posture is documented in `docs/api/visualization.md`.
- [x] Runtime kind-extension policy remains unchanged (core-defined only).
- [x] `make local-checks-pr` passes.

### Status-integrity rule
Task 7 is incomplete until the decision note (with all binding template sections), per-function resolution, supporting tests in non-viz files, and docs updates are all landed. The task cannot be marked complete with "decision note forthcoming" or without explicit `plot_global`/`plot_triangular` resolution.

### Task 7 completion status
**Status**: ✅ **Complete** (2026-05-11)

**Evidence**:
- Decision note exists at `docs/improvement/plot_spec/v0.11.2_default_promotion_decision.md` with gate evaluation, per-function resolution, deferral rationale, opt-in surface, and v0.11.3 follow-up.
- ADR-036/ADR-037 status appendix rows record zero open severity >= 9 conformance gaps.
- `tests/unit/test_plot_default_promotion.py` covers chain-based default deferral, PlotSpec opt-in, the no-explainer PlotSpec-first path, and legacy defaults for `plot_triangular` and `plot_global`.
- `make local-checks-pr` passes (2026-05-11).
- `make local-checks` passes (2026-05-11).

---

## 8) ADR-035 conformance gap remediation

### Goal
Close three open conformance gaps identified by the 2026-04-22 ADR-035 red-team analysis:
- **GAP 1** (severity 6): ADR §2 says `ci-policy/validate-workflows` MUST block merges, but `ci-policy.yml` still runs in advisory mode. Text is self-contradictory.
- **GAP 2** (severity 3): `scripts/local_checks.py` is named in ADR-035 §1 as governed, but `.github/CODEOWNERS` has no rule covering it — CODEOWNERS approval cannot be required.
- **GAP 4** (severity 6): Several workflow files pre-date the reusable-first rule and run all jobs inline. The validator's `_check_reusables()` is diff-scoped; these pre-existing violations are never flagged at rest, but any future edit to these files triggers a spurious reusable-first error with no resolution path.

GAPs 3 and 5 are not actioned here: GAP 3 (CI checklist template not auto-applied) is a GitHub UI limitation and low severity; GAP 5 (two-maintainer enforcement) is structurally unenforceable per-path in GitHub.

### Why this task exists
The ADR-035 gap analysis is a direct output of Task 4's red-team work. Leaving high-severity gaps open after they are documented violates the governance sweep posture of v0.11.2.

### Governing references
- ADR-035 CI Workflow Governance (the governed ADR)
- ADR-020 Release Quality Gates (documentation consistency)

### Hardening findings incorporated (red-team 2026-04-22)
1. **Allow-list scope was under-inclusive.** The original plan named only `ci-pr.yml` and `ci-main.yml`, but five workflow files currently have no reusable calls: `ci-main.yml`, `ci-nightly.yml`, `deprecation-check.yml`, `maintenance.yml`, `update_baseline.yml`. All five must either be in the allow-list or have an explicit "not in scope" rationale. (Verified via reusable-call scan: `ci-pr.yml` calls `reusable-python-test.yml`; the others do not.)
2. **`ci-pr.yml` already passes `_check_reusables` today.** It already calls a reusable workflow at line 22. The GAP 4 description that `ci-pr.yml` "runs all steps inline" is inaccurate — it has both inline jobs AND a reusable call. A test using `ci-pr.yml` to prove the allow-list works would be testing the wrong file. The allow-list test MUST use `ci-main.yml` (zero reusable calls) as the allowlisted fixture.
3. **`strict_workflow_change_detected` is not suppressed by the allow-list.** `_check_reusables` skip logic and the `strict_workflow_change_detected` flag are separate code paths. An allow-listed edit to `ci-main.yml` still sets `strict_workflow_change_detected = True` and triggers the "must also edit `scripts/local_checks.py` and `Makefile`" errors. The allow-list must either also suppress the strict-change detection for allowlisted files, or the plan must explicitly state this entanglement and require testers to also update `local_checks.py` and `Makefile` when editing allowlisted workflows.
4. **GAP 1 parenthetical fix is fragile.** An inline parenthetical on a present-tense MUST does not remove the contradiction — it makes the sentence ambiguous. The fix requires a structured **rollout status note block** inserted in ADR §2, not just wording surgery on the existing sentence.
5. **CODEOWNERS uses placeholder team handles.** Both existing entries (`@org/ci-team`, `@org/core-maintainers`) are placeholder strings. If no real GitHub teams resolve to these handles, all CODEOWNERS rules including the new `scripts/local_checks.py` rule are unenforced. The plan must record this as a known limitation and add a comment in the CODEOWNERS file.
6. **No test verifies the CODEOWNERS rule syntactically covers the target path.** The proposed tests only exercise the validator allow-list. A separate test (or inline verification step) must confirm `scripts/local_checks.py` is covered by a CODEOWNERS rule.
7. **Review-by date in allow-list is unverified.** The plan requires a "dated rationale comment and a review-by date" in the allow-list, but no test checks this. Document as a governance convention; add a test that the allow-list constant has a string matching `review-by:` or similar.

### Compatibility posture
Governance/tooling only. No runtime behavior changes.

### Non-goals
- Flipping `ci-policy/validate-workflows` to a required status check in branch protection (GitHub settings change; out of scope for local tooling).
- Fixing GAP 3 (template auto-application) or GAP 5 (per-path two-approver enforcement).
- Replacing placeholder `@org/*` team handles with real GitHub team identities (org-level admin change; out of scope).

### Inline-workflow allow-list: binding scope inventory

The following workflows have zero reusable calls as of 2026-04-22. Each needs an explicit disposition:

| Workflow file | Reusable calls? | GAP 4 disposition |
| --- | --- | --- |
| `ci-pr.yml` | ✓ (reusable-python-test) | **Not in scope** — already passes `_check_reusables`; no allow-list entry needed |
| `ci-main.yml` | ✗ | **Allow-listed** — pre-dates reusable-first rule; dated rationale required |
| `ci-nightly.yml` | ✗ | **Allow-listed** — heavy/scheduled workflow; reusable migration deferred to v0.11.3 |
| `deprecation-check.yml` | ✗ | **Allow-listed** — single-purpose test workflow; reusable migration deferred to v0.11.3 |
| `maintenance.yml` | ✗ | **Allow-listed** — maintenance workflow; reusable migration deferred to v0.11.3 |
| `update_baseline.yml` | ✗ | **Allow-listed** — automated baseline updater; reusable migration deferred to v0.11.3 |

All five inline-only workflows must be added to the allow-list constant with individual dated rationale comments.

### `strict_workflow_change_detected` entanglement (binding resolution)

Editing an allow-listed workflow (e.g. `ci-main.yml`) still triggers the `local_checks.py`/`Makefile` update requirement in `validate_ci_policy.py` because the allow-list only skips `_check_reusables`, not the `strict_workflow_change_detected` flag.

**Binding resolution for this task:** the allow-list for `_check_reusables` does **not** suppress the `strict_workflow_change_detected` flag. When editing an allow-listed workflow, contributors must still update `scripts/local_checks.py` and `Makefile` as required by ADR-035 §3 (local reproducibility parity). Document this requirement in a comment next to the allow-list constant. The test for the allow-list must construct a test fixture that includes `local_checks.py` and `Makefile` changes alongside the allowlisted workflow edit, so only the reusable-first check is suppressed.

### GAP 1 fix: structured rollout status note (binding template)

Do not reword the §2 MUST sentence itself. Instead, insert the following block immediately below the §2 criteria list:

```markdown
> **Rollout status note (2026-04-22):** `ci-policy/validate-workflows` currently runs in advisory mode (Rollout step 1). The MUST criteria above become fully enforceable at Rollout step 3 when the check is flipped to required in branch protection. Until then, violations are reported but non-blocking.
```

### Primary touchpoints
- `.github/CODEOWNERS` — add rule for `scripts/local_checks.py` (with placeholder-team caveat comment)
- `scripts/quality/validate_ci_policy.py` — add inline-workflow allow-list constant for all five non-reusable workflows; suppress `_check_reusables` for allowlisted files only
- `docs/improvement/adrs/ADR-035-ci-workflow-governance.md` — insert §2 rollout status note (do not reword the MUST sentence)
- `tests/scripts/test_validate_ci_policy.py` — add/update tests for allow-list and non-allow-listed behavior

### Ordered implementation steps
1. **GAP 4 inventory**: Verify the inline-workflow inventory table above against current `.github/workflows/` directory (check each file for reusable calls).
2. **GAP 4 allow-list**: Add `_REUSABLE_FIRST_ALLOWLIST` constant to `validate_ci_policy.py` with all five non-reusable workflow names, each with a dated rationale comment including `review-by: v0.11.3`. Update `_check_reusables` to skip files whose name is in the allow-list. Do **not** modify the `strict_workflow_change_detected` logic.
3. **GAP 2**: Add `scripts/local_checks.py @org/ci-team @org/core-maintainers` to `.github/CODEOWNERS`. Add a comment above it: `# NOTE: @org/ci-team and @org/core-maintainers are placeholder handles; replace with real GitHub team identities before enforcing branch protection.`
4. **GAP 1**: Insert the rollout status note block (binding template above) into ADR-035 §2 immediately after the criteria list.
5. **Tests**: Add/update tests listed below.
6. **Gate**: Run `pytest -q tests/scripts/test_validate_ci_policy.py -o addopts= --no-cov` and `make local-checks-pr`.

### Exact tests to add or update
- `tests/scripts/test_validate_ci_policy.py`:
  - `test_should_skip_reusable_check_for_allowlisted_workflow` — `ci-main.yml` edit (zero reusable calls, in allow-list) combined with `local_checks.py` and `Makefile` edits does not produce a reusable-first error, but still requires the local-reproduction updates.
  - `test_should_flag_reusable_check_for_non_allowlisted_new_workflow` — a new inline workflow `ci-new.yml` (not in allow-list) still gets flagged for missing reusable call.
  - `test_should_not_suppress_strict_change_detection_for_allowlisted_workflow` — editing an allowlisted workflow without updating `local_checks.py` still produces the local-reproduction error (allow-list does not suppress strict-change detection).
  - `test_should_have_dated_rationale_in_allowlist_entries` — asserts that the `_REUSABLE_FIRST_ALLOWLIST` constant (or equivalent) in `validate_ci_policy.py` contains `review-by:` text for each entry.

### Exact validation commands
- `pytest -q tests/scripts/test_validate_ci_policy.py -o addopts= --no-cov`
- `python -c "from scripts.quality.validate_ci_policy import _REUSABLE_FIRST_ALLOWLIST; assert 'ci-main.yml' in str(_REUSABLE_FIRST_ALLOWLIST), 'allow-list missing ci-main.yml'"` (inline verification)
- Verify `scripts/local_checks.py` is in `.github/workflows/ci-policy.yml` `on.paths` to confirm CODEOWNERS change triggers the right workflow
- `make local-checks-pr`

### Detailed verification checklist
- [x] `.github/CODEOWNERS` includes rule for `scripts/local_checks.py` with placeholder-team caveat comment.
- [x] `validate_ci_policy.py` `_REUSABLE_FIRST_ALLOWLIST` covers all five non-reusable workflow files with individual dated rationale comments containing `review-by:`.
- [x] `_check_reusables` skips allowlisted files; `strict_workflow_change_detected` logic is unchanged.
- [x] ADR-035 §2 contains the rollout status note block (not a rewording of the MUST sentence).
- [x] All four new/updated tests pass: allow-list skip, new-workflow flag, strict-change not suppressed, dated-rationale assertion.
- [x] `make local-checks-pr` passes.

### Status-integrity rule
Task 8 cannot be marked complete until all three gaps are closed, the allow-list covers all five non-reusable workflows with dated rationale, all four tests pass, and the CODEOWNERS change is present with the placeholder-team caveat comment.

### Task 8 completion status
**Status**: ✅ **Complete** (2026-04-23)

**Evidence:**
- GAP 4 implemented in `scripts/quality/validate_ci_policy.py`:
  - Added `_REUSABLE_FIRST_ALLOWLIST` for `ci-main.yml`, `ci-nightly.yml`, `deprecation-check.yml`, `maintenance.yml`, `update_baseline.yml`.
  - `_check_reusables(...)` now skips allowlisted workflows only; `strict_workflow_change_detected` logic remains unchanged.
- GAP 2 implemented in `.github/CODEOWNERS`:
  - Added `/scripts/local_checks.py @org/ci-team @org/core-maintainers`.
  - Added explicit placeholder-team caveat comments.
- GAP 1 implemented in `docs/improvement/adrs/ADR-035-ci-workflow-governance.md`:
  - Inserted the rollout status note block directly under §2 merge criteria (no rewrite of MUST sentence).
- Tests and verification:
  - `pytest -q tests/scripts/test_validate_ci_policy.py -o addopts= --no-cov` -> 8 passed.
  - `python -c "from scripts.quality.validate_ci_policy import _REUSABLE_FIRST_ALLOWLIST; assert '.github/workflows/ci-main.yml' in _REUSABLE_FIRST_ALLOWLIST; print('allowlist-ok')"` -> `allowlist-ok`.
  - `make local-checks-pr` -> pass (using venv python on PATH).

---

## 9) PlotSpec semantic/visual mending while non-default (ADR-036 / ADR-037)

### Goal
Improve PlotSpec-rendered plots in v0.11.2 so they preserve the same explanatory meaning as legacy plots on explicit opt-in paths, without changing the default plotting route for users.

### Why this task exists
Task 7 records that v0.11.2 will not promote PlotSpec to the default path. That decision is driven by a visual-semantics concern: the PlotSpec payloads may be structurally correct, but the rendered plots are not yet trusted to convey the same user-facing meaning as the legacy plots. This task addresses that gap while keeping PlotSpec opt-in for v0.11.2.

### Governing references
- `docs/improvement/adrs/ADR-036-plot-spec-canonical-contract-and-validation-boundary.md` (semantic contract and validation boundary)
- `docs/improvement/adrs/ADR-037-visualization-extension-and-rendering-governance.md` (renderer governance and default-path posture)
- `docs/api/visualization.md`
- `docs/improvement/RELEASE_PLAN_v1.md`
- Task 7 in this plan (binding v0.11.2 deferral decision)

### Hardening findings incorporated
1. Correct payload/schema output is not sufficient if the rendered visual changes user interpretation.
2. Pixel-perfect parity is not required; semantic/meaning-preserving parity is.
3. The task must stay non-default so users can test PlotSpec improvements without a release-wide behavior change.
4. Validation must cover user-facing semantics such as baseline/reference meaning, directionality, interval meaning, ranking/salience, and label/annotation clarity.
5. Triangular/global PlotSpec rendering must not stop at serialized-dict placeholder primitives; canonical dataclass renderer branches must draw or export the legacy-equivalent quiver/scatter semantics.
6. Global PlotSpec payloads must preserve threshold and class-label semantics; dropping `threshold` as "legacy-only" makes thresholded-regression axis labels unverifiable.
7. Boundary serializers must normalize numpy scalars/arrays recursively for triangular/global payloads so side-by-side artifacts and schema checks do not depend on numpy object leakage.
8. In local environments where `--no-cov` is unavailable or the user temp root is inaccessible, the equivalent validation form is `-o addopts=` plus a workspace-local `--basetemp`.

### Compatibility posture
Behavioral improvements to PlotSpec-rendered visuals are allowed on explicit opt-in paths in v0.11.2.
Default plotting behavior must remain legacy throughout this task.

### Non-goals
- No default flip for any plot path in v0.11.2.
- No runtime plot-kind extension enablement.
- No requirement for pixel-identical screenshots between PlotSpec and legacy.

### Binding implementation strategy decision (2026-04-26)

This task no longer follows the earlier hybrid "copy for triangular/global" plan.
Direct code inspection and the generated parity packet show that the renderer work
is already in place. The binding strategy is now:

1. Keep the landed PlotSpec implementations for factual, alternative, triangular, global, and conjunction renders.
2. Mend only real residual gaps discovered by direct verification, not earlier red-team assumptions.
3. Treat Task 9 as a verification-and-acceptance gate: semantic tests, side-by-side evidence, and explicit human evaluator sign-off are now the controlling deliverables.

### Plot-by-plot implementation instructions (binding)

| Plot family | Legacy source of truth | PlotSpec path to mend | Strategy | Required parity focus |
| --- | --- | --- | --- | --- |
| Factual probabilistic | `legacy._plot_probabilistic` | `plotting.plot_probabilistic` -> `build_probabilistic_bars_spec` -> `matplotlib_adapter.render` | Mend PlotSpec directly | Header captions, positive/negative direction semantics, interval overlays, baseline meaning |
| Factual regression | `legacy.plot_regression` | `plotting.plot_regression` -> `build_regression_bars_spec` -> `matplotlib_adapter.render` | Mend PlotSpec directly | Regression sign-color convention, interval semantics, rule ordering, x-axis meaning |
| Alternative probabilistic | `legacy.plot_alternative` (classification/threshold branch) | `plotting.plot_alternative` -> `build_alternative_probabilistic_spec` -> `matplotlib_adapter.render` | Mend PlotSpec directly | 0.5 split semantics, color intensity semantics, progression from base interval to rule intervals |
| Alternative regression | `legacy.plot_alternative` (regression branch) | `plotting.plot_alternative` -> `build_alternative_regression_spec` -> `matplotlib_adapter.render` | Mend PlotSpec directly | Base interval/line semantics, rule interval/line semantics, uncertainty alpha semantics |
| Triangular | `legacy.plot_triangular` | `plotting.plot_triangular` + `build_triangular_plotspec` + triangular render branch | Verify landed implementation | Arrow direction semantics, point semantics, axis semantics, uncertainty interpretation |
| Global classification | `legacy.plot_global` | `plotting.plot_global` + `build_global_plotspec` + global render branch | Verify landed implementation | Class-conditioned scatter semantics, threshold/class labeling semantics, axis meaning |
| Global regression | `legacy.plot_global` | `plotting.plot_global` + `build_global_plotspec` + global render branch | Verify landed implementation | Prediction-vs-outcome scatter semantics, uncertainty spread semantics, axis meaning |
| Conjunction labels | `legacy._plot_probabilistic` | `build_probabilistic_bars_spec` + `matplotlib_adapter.render` | Verify landed implementation | Multiline rule-label legibility, figure-height sizing, unchanged factual-probabilistic semantics |

### Red-team findings (2026-04-24) — corrected after direct code inspection

A subagent red-team was conducted and then verified against the actual source code. The following corrections apply to the original implementation descriptions:

**Claims that were WRONG (do not require mending):**
- "Regression bar colors are all red" — **FALSE**. Adapter line 1610: `color = "b" if width > 0 else "r"` matches legacy exactly.
- "Triangular rendering is stubbed" — **FALSE**. `_render_triangular_spec` in `matplotlib_adapter.py` is a complete implementation with `_plot_probability_triangle`, `ax.quiver`, and `ax.scatter`.
- "Global rendering has no PlotSpec pipeline" — **FALSE**. `_render_global_spec` is fully implemented with class-stratified scatter, threshold-derived x-axis label, and class-conditioned legend.
- "neg_caption generates 'P(y=X)' for both bands" — **FALSE**. `plotting.py` lines 702–763 compute `neg_caption = "P(y=0)"` or `"P(y!={label})"` and pass them explicitly; the adapter uses them directly.
- "Alternative probabilistic header is absent" — **FALSE**. For the classification path `plotting.py` always sets `xticks_override = [0.0…1.0]`, so `provided_xticks=True` → `header_needed=True`.

**Claims that ARE real (require verification and limited mending):**
- Semantic parity tests are shallow: existing tests in `test_plot_parity_adapter.py` check structure and key existence but do not verify actual bar color roles, interval range values, alternative baseline segment presence, triangular primitive content, or global scatter stratification.
- `build_alternative_probabilistic_spec` and `build_alternative_regression_spec` carry `# pragma: no cover` — they are exercised through the public `plot_alternative` call path but not directly by unit tests.
- Conjunction-style multiline rule labels are part of the generated parity packet and require an explicit sizing/legibility check rather than an implicit assumption.

**Implementation strategy update:**
- For factual/alternative families: implementation is semantically correct; task reduces to writing comprehensive semantic parity tests and generating visual evidence.
- For triangular/global: implementation is semantically complete; task reduces to adding primitive-level assertions and generating visual evidence.
- For conjunction/multiline-label scenarios: implementation remains on the factual probabilistic path, but verification must assert that label density does not collapse the review artifact.
- Strategy column "Copy legacy rendering behavior into PlotSpec renderer" for triangular/global is now SUPERSEDED — the copy was already executed prior to this red-team. The strategy for these families is now "verify via tests and visual evidence".

### Primary touchpoints
- `src/calibrated_explanations/plotting.py`
- `src/calibrated_explanations/viz/builders.py`
- `src/calibrated_explanations/viz/matplotlib_adapter.py`
- `src/calibrated_explanations/viz/plots.py`
- `tests/unit/viz/test_plot_parity_adapter.py`
- `tests/unit/viz/test_plot_parity_fixtures.py`
- `tests/unit/viz/test_alternative_regression_parity.py`
- `tests/integration/test_plot_repr_consistency.py`
- `tests/unit/viz/test_plotspec_mvp.py`
- `docs/api/visualization.md`
- `docs/improvement/plot_spec/` (analysis/report artifact)

### Semantic mending scope (binding)
The task must evaluate and mend, where needed, the following user-facing semantics for PlotSpec-rendered plots on supported opt-in paths:
1. Baseline/reference interval meaning.
2. Positive vs negative contribution directionality and salience.
3. Uncertainty/interval presentation semantics.
4. Feature/rule ordering and ranking meaning.
5. Alternative/counterfactual progression semantics.
6. Required labels/annotations needed for interpretation.

### Ordered implementation steps
1. For each generated plot pair above, generate deterministic side-by-side outputs (`*_legacy.png`, `*_plotspec.png`) using identical inputs and fixed fixtures.
2. Record discrepancy findings per family under the six semantic scope items (baseline meaning, directionality, interval semantics, ordering, progression semantics, labels/annotations).
3. Apply the family strategy:
  - factual/alternative families: mend only verified semantic mismatches;
  - triangular/global/conjunction families: verify landed implementation, then mend only if human review identifies a real semantic miss.
4. Regenerate the final side-by-side outputs and append delta notes per family when any code change was required. If no code change was required, the final `*_plotspec.png` artifact is the accepted review target.
5. Add/update automated parity tests for semantics that can be machine-asserted; keep visual judgment items in human-evaluation artifacts.
6. Produce a human evaluation packet (side-by-side gallery plus scoring rubric) and require explicit evaluator sign-off for each family.
7. Update API/release docs to reflect improved opt-in PlotSpec quality, continued non-default posture, and remaining blockers (if any) for v0.11.3 re-evaluation.

### Exact tests to add or update
- Extend `tests/unit/viz/test_plot_parity_adapter.py` with semantics-oriented assertions for factual/alternative families (directionality role, interval overlays, header captions, base interval semantics).
- Extend `tests/unit/viz/test_plot_parity_fixtures.py` with deterministic fixtures that cover edge cases required for side-by-side review (thresholded, multiclass, zero-crossing intervals, unbounded intervals).
- Extend `tests/unit/viz/test_alternative_regression_parity.py` as the reference parity pattern and replicate that approach for factual probabilistic and factual regression parity comparisons.
- Add targeted parity tests for triangular/global that assert equivalent primitive semantics between legacy and mended PlotSpec renderer/plugin outputs (not just route invocation).
- Add a non-viz invariant test file for default-path and opt-in routing invariants so parity governance remains covered in `make local-checks-pr`.
- Run `tests/unit/viz/test_plotspec_mvp.py` explicitly if renderer changes touch viz-only assertions.
- Run serializer/builder regression tests for triangular/global metadata preservation (`tests/unit/viz/test_viz_serializers.py`, `tests/unit/viz/test_viz_builders.py`, `tests/unit/viz/test_viz_builders_extended.py`, `tests/unit/viz/test_plotspec_roundtrip_and_headless.py`).

### Exact validation commands
- `pytest -q tests/unit/viz/test_plot_parity_adapter.py -o addopts= --no-cov`
- `pytest -q tests/unit/viz/test_plot_parity_fixtures.py -o addopts= --no-cov`
- `pytest -q tests/unit/viz/test_alternative_regression_parity.py -o addopts= --no-cov`
- `pytest -q tests/integration/test_plot_repr_consistency.py -o addopts= --no-cov`
- `pytest -q -m viz tests/unit/viz/test_plotspec_mvp.py -o addopts= --no-cov` (if touched)
- `pytest -q tests/unit/test_plot_default_promotion.py -o addopts= --no-cov`
- `pytest -q tests/unit/viz/test_viz_serializers.py tests/unit/viz/test_viz_builders.py tests/unit/viz/test_viz_builders_extended.py tests/unit/viz/test_plotspec_roundtrip_and_headless.py -o addopts= --no-cov`
- If `--no-cov` is unavailable, omit it and keep `-o addopts=`; if the system temp directory is inaccessible, add a workspace-local `--basetemp=.tmp_codex_task9_pytest`.
- `make local-checks-pr`

### Exact release artifacts / docs to produce
- `docs/improvement/plot_spec/v0.11.2_plot_mending_report.md`
- `docs/improvement/plot_spec/v0.11.2_plot_side_by_side_gallery.md` (legacy vs plotspec_current vs plotspec_mended)
- `reports/plot_parity/v0.11.2_side_by_side/` (image artifacts grouped by plot family and scenario)
- `docs/improvement/plot_spec/v0.11.2_human_evaluation_sheet.md` (rubric + per-family sign-off)
- Updated `docs/api/visualization.md` describing the improved but still non-default PlotSpec path
- Updated `docs/improvement/RELEASE_PLAN_v1.md` v0.11.2 / v0.11.3 milestone notes referencing the mending task and re-evaluation dependency

### Detailed verification checklist
- [x] PlotSpec remains non-default across all public plotting entrypoints in v0.11.2.
- [x] Strategy application is followed per family: factual/alternative families are mended only where a verified semantic mismatch exists; triangular/global/conjunction families are treated as verify-first paths unless review finds a real semantic miss.
- [x] Triangular/global renderer branches consume canonical dataclass PlotSpecs; serialized dict envelopes remain boundary artifacts only.
- [x] Global PlotSpec semantics retain threshold/class-label metadata required for thresholded and class-conditioned labels.
- [x] Side-by-side outputs exist for each generated review target as `*_legacy.png` and `*_plotspec.png`; any post-mend delta is explained in the mending report rather than requiring a third filename class.
- [x] Supported opt-in PlotSpec plots preserve legacy-equivalent explanatory meaning for the semantic scope items listed above.
- [x] Conjunction/multiline-label artifacts remain legible and receive explicit verification rather than implicit acceptance.
- [x] Tests cover the semantic deltas that motivated this task, not only payload correctness.
- [x] A human evaluator has reviewed side-by-side outputs and signed off each family as either `accepted` or `needs more mending`.
- [x] Mending report documents remaining open gaps, if any, that still block future default promotion.
- [x] Visualization docs state both the improved opt-in posture and the still-deferred default decision.
- [x] `make local-checks-pr` passes.

### Status-integrity rule
Task 9 cannot be marked complete based only on payload correctness, schema validity, or passing routing tests. Completion requires: (1) side-by-side legacy vs mended PlotSpec visual evidence for every plot family, (2) automated semantic parity checks for machine-verifiable dimensions, and (3) explicit human evaluator sign-off that each family is "good enough" for legacy-equivalent meaning while default behavior remains unchanged.

### Task 9 implementation checkpoint
**Status**: ✅ **Complete** (2026-05-11)

**Evidence:**
- Factual/alternative bar families have semantics-oriented primitive assertions in `tests/unit/viz/test_plot_parity_adapter.py`.
- Triangular/global PlotSpec dataclasses render through concrete adapter branches with exported quiver/scatter/axis primitives.
- `GlobalSpec` now retains threshold/class-label metadata required for threshold and class-conditioned labels.
- Verification packet created under `docs/improvement/plot_spec/` and `reports/plot_parity/v0.11.2_side_by_side/`.
- Side-by-side PNG artifacts now exist in-repo for factual, alternative, triangular, global classification, global regression, and conjunction label scenarios.
- Maintainer/evaluator decisions in `docs/improvement/plot_spec/v0.11.2_human_evaluation_sheet.md` mark every review target as accepted.
- Task-specific validation passed for `tests/unit/viz/test_plot_parity_adapter.py`, `tests/unit/viz/test_alternative_regression_parity.py`, `tests/integration/test_plot_repr_consistency.py`, and `tests/unit/test_plot_default_promotion.py`; `tests/unit/viz/test_plot_parity_fixtures.py` is a fixture module and currently collects no tests.
- `make local-checks-pr` passes (2026-05-11).

---

## 10) Packaging metadata maturity correction

### Goal
Correct stale packaging metadata so `pyproject.toml` no longer advertises CE as Alpha.

### Why this task exists
The current classifier is stale and contradicts the current pre-v1 release posture. The correction should be explicit and traceable, not a silent metadata edit.

### Primary touchpoints
- `pyproject.toml`
- `docs/improvement/RELEASE_PLAN_v1.md`
- `CHANGELOG.md`
- `CHANGELOG.md` / release notes for the next pre-v1 release

### Implementation steps
1. Replace `Development Status :: 3 - Alpha` with `Development Status :: 4 - Beta`.
2. Add packaging metadata maturity control policy to `RELEASE_PLAN_v1.md` (RC stays Beta; GA promotion gate controls the Stable transition).
3. Add a changelog entry under Packaging / Release metadata.
4. Confirm package metadata with build inspection:
   - build wheel/sdist;
   - inspect generated metadata;
   - verify the Beta classifier appears exactly once;
   - verify Alpha no longer appears in generated package metadata.
5. Confirm no docs claim GA/stable before v1.0.0.


### Task 10 verification status
**Status**: ✅ **Complete** (2026-05-11)

- **Implemented evidence (2026-05-11):** source metadata updated to Beta in `pyproject.toml`; RC/GA classifier governance documented in `RELEASE_PLAN_v1.md`; changelog note added under Packaging / Release metadata; No new wording introduced by this task claims that the current pre-v1 package is GA/stable.
- **Verification evidence (2026-05-11):** `python -m build` produced wheel/sdist artifacts; generated metadata inspection passed for 3 metadata files with the Beta classifier exactly once in each file and the Alpha classifier absent; `tests/test_project_metadata.py` passed; `make local-checks-pr` and `make local-checks` passed.

### Verification checklist
- [x] `pyproject.toml` uses `Development Status :: 4 - Beta`.
- [x] Generated wheel metadata contains the Beta classifier exactly once per metadata file.
- [x] No generated package metadata file contains Alpha.
- [x] Release plan documents RC and GA classifier policy.
- [x] Changelog explains this as metadata correction, not a behavior change.
- [x] No docs claim GA/stable before v1.0.0.
- [x] `make local-checks-pr` passes.

---

## Consolidated release gate checklist (v0.11.2)

- [x] **Task 1:** four-module ConfigManager migration complete and allowlist reaches zero.
- [x] **Task 1:** `check_config_manager_usage.py` targeted scan reports zero violations.
- [x] **Task 2:** ADR-034 accepted-state implementation summary inserted and synchronized across release docs.
- [x] **Task 3:** every open appendix gap row has explicit milestone ownership or closed/superseded state.
- [x] **Task 3:** v0.11.2 quick wins (ADR-026 gaps 2/3, ADR-004 naming drift) are closed or explicitly deferred with blocker text.
- [x] **Task 4:** governance status artifact schema + producer + local_checks.py integration + Makefile target + retrieval docs + 15 tests complete. CI workflow upload-artifact wiring deferred: documented as manual step in `docs/improvement/governance_status_artifact.md`; requires ADR-035-compliant PR to `.github/workflows/`.
- [x] **Task 5:** post-5A CI hygiene verified (zero workflow references to removed symbols); `deprecation-check.yml` behavioral verification passed; `remediation_api_changes.md` removal note added; list-path "complete migration" interpreted and recorded.
- [x] **Task 5A:** v0.11.2 LIME/SHAP core/wrapper removal inventory deleted with fail-closed tests.
- [x] **Task 5A:** migration deprecations table moved from Active to Removed history for removed rows, while legacy list-path APIs remain on v0.11.3 timeline.
- [x] **Task 6:** reset/close retention semantics implemented with numeric RSS stabilization proof.
- [x] **Task 7:** PlotSpec default-promotion deferral recorded with synchronized docs/plan text and explicit v0.11.3 follow-up.
- [x] **Task 8:** CODEOWNERS covers `scripts/local_checks.py`; validator allow-list exempts pre-reusable inline workflows; ADR-035 §2 wording clarified.
- [x] **Task 9:** PlotSpec semantic/visual mending completed on opt-in paths while default behavior remains unchanged.
- [x] **Task 10:** packaging metadata maturity correction completed (Beta classifier in source and generated package metadata; Alpha absent from generated package metadata; RC/GA policy documented; changelog note added).
- [x] `make local-checks-pr` passes for each merged task PR.
- [x] `make local-checks` passes for any PR that changes branch-gate behavior.

---

## Recommended validation command set

```bash
make local-checks-pr
python scripts/quality/check_config_manager_usage.py --scope targeted --report reports/config_manager_usage_report.json
pytest -q tests/scripts/test_check_config_manager_usage.py
pytest -q tests/unit/core/test_config_manager.py
pytest -q tests/unit/core/test_lime_shap_core_deprecation.py
pytest -q tests/unit/plugins/test_legacy_api_deprecation.py
pytest -q tests/test_reject_memory.py
pytest -q tests/unit/core/test_memory_retention.py
pytest -q tests/unit/viz/test_plot_parity_adapter.py
pytest -q tests/unit/viz/test_plot_parity_fixtures.py
pytest -q tests/unit/viz/test_alternative_regression_parity.py
pytest -q tests/integration/test_plot_repr_consistency.py
pytest -q tests/test_project_metadata.py
python -m build
python - <<'PY'
from pathlib import Path
import tarfile
import zipfile

beta = "Classifier: Development Status :: 4 - Beta"
alpha = "Classifier: Development Status :: 3 - Alpha"
metadata_blobs = []

for wheel in Path("dist").glob("*.whl"):
    with zipfile.ZipFile(wheel) as zf:
        for name in zf.namelist():
            if name.endswith("METADATA"):
                metadata_blobs.append((str(wheel), name, zf.read(name).decode()))

for sdist in Path("dist").glob("*.tar.gz"):
    with tarfile.open(sdist) as tf:
        for member in tf.getmembers():
            if member.name.endswith("PKG-INFO"):
                f = tf.extractfile(member)
                if f is not None:
                    metadata_blobs.append((str(sdist), member.name, f.read().decode()))

assert metadata_blobs, "No generated package metadata found"
for artifact, name, blob in metadata_blobs:
    assert blob.count(beta) == 1, f"{artifact}:{name} missing single Beta classifier"
    assert alpha not in blob, f"{artifact}:{name} still contains Alpha classifier"
print("Generated package metadata classifier check passed")
PY
make local-checks
```
