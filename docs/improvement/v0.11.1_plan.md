# v0.11.1 Release Task Implementation Plan

This document is the execution plan for all `v0.11.1 (hardening)` tasks listed in `docs/improvement/RELEASE_PLAN_v1.md`.


## Milestone control posture

- This is the active detailed milestone execution plan.
- Keep this file prescriptive and execution-oriented until the v0.11.1 boundary is closed.
- Update status in milestone-boundary batches (not continuously after every PR).

## Planning standard used in this file

Each task now contains:

1. Why the task exists (ADR/standard alignment).
2. Concrete code and doc touchpoints.
3. Ordered implementation steps with dependency order.
4. Required tests/checks and release artifacts.
5. A detailed verification checklist that can be executed as-is.

## Global execution constraints

- Keep CE-first and ADR-first behavior.
- Preserve public API compatibility unless the release task explicitly requires additive behavior.
- Any behavior change must include docs + tests in same PR.
- Validation path for each task PR:
  1. `make local-checks-pr`
  2. Task-targeted pytest runs
  3. `make local-checks` if task changes branch-gate behavior

---

## Recommended implementation sequence

The 16 pending v0.11.1 tasks are grouped into implementation waves by technical concern. Tasks within the same wave can proceed in parallel; tasks in a later wave must wait for their prerequisite waves to complete. This ordering maximises parallel throughput while respecting hard inter-task dependencies.

| Wave | Tasks | Theme | Prerequisites |
| --- | --- | --- | --- |
| 1 | 1, 6, 8, 11, 14, 15, 22 | Parallel foundation work | None |
| 2 | 2, 4 | ADR-006 trust hardening | Wave 1: Task 1 complete |
| 3 | 3, 5, 12, 13 | Governance events + ADR-033 docs | Wave 2: Task 2 complete; Wave 1: Task 11 complete |
| 4 | 7, 20 | Observability enforcement closure | Wave 3: Task 3 complete; Wave 1: Task 15 complete |

### Cluster legend

| Cluster | Tasks | Theme |
| --- | --- | --- |
| ADR-006 Plugin Authority Chain | 1, 2, 4, 5 | `PluginManager` becomes the single resolver authority; trust state is made atomic; test-helper relocation and list-deprecation follow once the new surface is stable. |
| ADR-028 Governance Observability Chain | 3, 7, 20 | Plugin governance-event schema established (Task 3), enforcement tooling built (Task 7), and a separate `governance.config` lifecycle-event schema wired through `ConfigManager` (Task 20). |
| ADR-033 Modality Extension Cluster | 11, 12, 13 | CLI entry-point shims created (Task 11); RTD contributor and practitioner docs written (Task 12); packaging smoke test confirms entry-point discovery contract (Task 13). |
| ADR-034 ConfigManager Completion | 15 | ConfigManager Phase A establishes the authoritative config path and preparatory lifecycle hook points consumed by governance observability closure (Task 20). |
| PlotSpec + ADR Revisioning | 22 | Harden PlotSpec canonical IR boundaries and make ADR-036/ADR-037 the authoritative visualization governance source while preserving legacy-default plotting in v0.11.1. |
| Independent Work | 6, 8, 14 | ADR-012 notebook-execution CI (Task 6), Standard-001 double-underscore cleanup (Task 8), and reject-framework expansion (Task 14) have no intra-milestone hard dependencies. |

### Hard dependency map

| Task | Hard depends on | Reason |
| --- | --- | --- |
| 2 | 1 | Trust atomicity requires `PluginManager` to be the single mutation entry point before internal trust-id sets can be unified. |
| 3 | 1, 2 | `GovernanceEvent` hooks into the unified trust/registration path established by Tasks 1 and 2. |
| 4 | 1 | Test-helper relocation requires knowing the final production surface after the Task 1 refactor. |
| 5 | 1, 2 | Keyed-descriptor API (Task 2 trust unification) must exist before the old list path can be deprecated. |
| 7 | 3 | Observability enforcement tooling verifies `GovernanceEvent` fields established by Task 3. |
| 12 | 11 | RTD docs reference the CLI shims and entry points created in Task 11. |
| 13 | 11 | Packaging smoke test exercises the entry-point discovery contract created in Task 11. |
| 20 | 3, 15 | Reuses the Task 3 governance event builder/validation pattern, but adds a separate `governance.config` lifecycle-event schema and emitter; requires the `ConfigManager` lifecycle hooks from Task 15. |
| 22 | None (must preserve Tasks 9/10 relocation decisions) | PlotSpec hardening is self-contained for v0.11.1 but must keep legacy plotting default and keep runtime kind-extension deferred to future ADR work. |

### Gate coverage map

| Release gate condition | Satisfied by |
| --- | --- |
| `PluginManager` is the single resolver authority for all plugin selection | Task 1 |
| Trust state (descriptor flags + trusted-id sets) is atomic; cannot diverge | Task 2 |
| `GovernanceEvent` schema covers every plugin registration/discovery decision audit fields | Task 3 |
| `governance.config` lifecycle events covered in a separate schema-backed governance contract | Task 20 |
| Test-helper bodies reside in test fixtures, not production modules | Task 4 |
| Legacy list-based plugin API emits deprecation warnings; CI enforces `CE_DEPRECATIONS=error` | Task 5 |
| ADR-012 notebook execution CI gate green | Task 6 |
| ADR-028 observability enforcement tooling green | Task 7 |
| Standard-001 double-underscore cleanup + naming-enforcement gate green | Task 8 |
| ADR-033 modality shims and CLI entry points gate green | Task 11 |
| ADR-033 modality RTD docs gate green | Task 12 |
| ADR-033 packaging smoke test gate green | Task 13 |
| Reject framework expanded with uncertainty strategy | Task 14 |
| `ConfigManager` is authoritative; snapshot semantics, strict validation posture, and migration tests green | Task 15 |
| PlotSpec canonical IR hardening + ADR-036/ADR-037 supersession governance delivered (legacy `.plot()` remains default; runtime kind extension disabled) | Task 22 |
| Migration/release docs reflect deprecations, modality timeline, reject strategy, `ConfigManager` | Tasks 5, 11, 12, 14, 15 |
| ADR-020 and ADR-028 promoted to Accepted | Tasks 16–17 (Done 2026-03-03) |

---

## 1) PluginManager-authoritative resolution (ADR-006)

Goal: make `PluginManager` the single resolver authority for explanation/interval/plot plugin selection.

Primary touchpoints:

- `src/calibrated_explanations/plugins/manager.py`
- `src/calibrated_explanations/core/calibrated_explainer.py`
- `src/calibrated_explanations/plugins/registry.py`
- `tests/unit/plugins/*`
- `tests/integration/*plugin*`

Step-by-step implementation:

1. Freeze current behavior before refactor.
   1. Capture baseline resolver behavior with characterization tests for factual/alternative/FAST/plot routes.
   2. Assert current precedence order (kwargs > env > pyproject > fallback/default).
2. Introduce explicit manager resolver interfaces.
   1. Add/confirm `resolve_explanation_plugin(...)` with mode/task/override parameters.
   2. Add/confirm `resolve_interval_plugin(...)` with fast-mode and fallback handling.
   3. Add/confirm `resolve_plot_plugin(...)` with style/kind selection.
3. Move decision logic out of `CalibratedExplainer`.
   1. Port private resolver logic from `CalibratedExplainer` methods into manager methods.
   2. Keep logic structure parallel to reduce regression risk.
4. Rewire runtime call paths.
   1. Update all explanation invocation paths to call manager resolver methods.
   2. Update interval resolution path used by prediction/explanation orchestration.
   3. Update plot resolution path to use manager instead of core-local logic.
5. Remove duplicate resolver logic from core.
   1. Delete/retire obsolete core resolver methods.
   2. Keep only orchestration and invariant enforcement in core.
6. Finalize compatibility.
   1. Confirm no public symbols are removed unexpectedly.
   2. Confirm env/pyproject overrides still work unchanged.

### Task 1A — CalibratedExplainer surface deprecation alignment (ADR-011 + ADR-020)

Goal: deprecate non-essential `CalibratedExplainer` compatibility delegators that are no longer
required to live on the explainer surface now that `PluginManager` is authoritative.

Deprecation criteria:
1. Method/property is primarily a pass-through to `PluginManager` or orchestrators.
2. Symbol is not documented as required user API in `docs/improvement/legacy_user_api_contract.md`.
3. Symbol exists mainly for backward/internal compatibility during architecture transition.

Candidate groups (deprecate now, do not remove in v0.11.x):
1. Delegator methods:
   - `build_plot_style_chain`
   - `instantiate_plugin`
   - `invoke_explanation_plugin`
   - `ensure_interval_runtime_state`
   - `gather_interval_hints`
2. Plugin-state alias properties exposed on explainer:
   - `interval_plugin_hints`, `interval_plugin_fallbacks`, `interval_preferred_identifier`
   - `telemetry_interval_sources`, `interval_plugin_identifiers`, `interval_context_metadata`
   - `explanation_plugin_overrides`, `interval_plugin_override`,
     `fast_interval_plugin_override`, `plot_style_override`

Timeline policy:
1. `v0.11.x`: emit `deprecate()` warnings with canonical migration targets.
2. `v0.11.2`–`v0.11.3`: remove deprecated wrappers/aliases in the pre-v1.0 finalization window; no carryover into v1.0.0.

Migration mapping requirement:
1. Every deprecated explainer symbol must be mapped in `docs/migration/deprecations.md` to a
   canonical target (`plugin_manager.*`, orchestrator API, or wrapper-level API).

ADR alignment note:
1. ADR-011 governs warning semantics and CI strict mode (`CE_DEPRECATIONS=error`).
2. ADR-020 keeps legacy user API stability major-gated; this deprecation set is limited to
   non-contract delegators and state aliases.

Closure requirements:
1. Plot resolver must fail closed when `PluginManager` style resolution fails; no silent fallback
   to local env/pyproject chain logic.
2. Global plot plugin selection must route through `PluginManager.resolve_plot_plugin(...)` for
   runtime explainer paths; no direct registry resolution fallback in `plotting.py`.
3. Explicit object overrides in explanation resolver must be validated by the same metadata
   validator path used for chain-resolved plugins (schema/tasks/modes/capabilities).

Closure status:
- [x] Requirement 1 implemented in `plotting._resolve_plot_style_chain`.
- [x] Requirement 2 implemented in `plotting.plot_global`.
- [x] Requirement 3 implemented in `PluginManager.resolve_explanation_plugin_for_mode`.

Verification checklist:

- [x] Characterization tests for old/new resolver behavior pass with identical outcomes.
- [x] `CalibratedExplainer` no longer contains plugin-selection decision logic.
- [x] Manager resolver methods are the only runtime selection entry points.
- [x] Precedence tests pass for all resolver types.
- [x] Factual/alternative/FAST integration tests show no behavior regressions.
- [x] `make local-checks-pr` passes.

---

## 2) Atomic trust-state unification (ADR-006)

Goal: ensure descriptor trust flags and trusted-id sets cannot diverge.

Primary touchpoints:
- [src/calibrated_explanations/plugins/registry.py](src/calibrated_explanations/plugins/registry.py)
- [src/calibrated_explanations/plugins/manager.py](src/calibrated_explanations/plugins/manager.py)
- New: `src/calibrated_explanations/plugins/_trust.py`
- [tests/unit/plugins/test_*trust*]
- New: `scripts/quality/check_trust_mutation_primitive.py`

Hardening notes (applied):
1. Clarify atomicity scope.
   - Required atomicity is thread-level within a single Python process (shared in-memory registry state).
   - Cross-process synchronization is out of scope for v0.11.1 because plugin trust state is process-local.
2. Define the exact invariant to enforce after every mutation.
   - For each plugin kind and identifier, descriptor `.trusted` must equal trusted-set membership.
   - No identifier may exist in trusted-id sets without a corresponding descriptor.
3. Remove dependency ambiguity with Task 3.
   - Task 2 must emit a structured trust-mutation payload through a helper that can be consumed by Task 3 schema enforcement; Task 2 does not block on final governance-event schema wiring.
4. Define lock ownership and boundary.
   - All read-modify-write trust mutations must pass through one primitive protected by a single shared lock.
   - Direct writes to `_TRUSTED_*` collections or descriptor `.trusted` outside the primitive are prohibited.

Non-goals:
1. Re-architecting plugin storage format.
2. Cross-process/global distributed trust coordination.
3. Expanding governance schema beyond trust-mutation payload shape required by this task.

Step-by-step implementation:
1. Inventory all trust mutation locations.
   - Programmatically scan `src/` for `_TRUSTED_` symbols and descriptor `trusted` writes.
   - Record findings as `reports/trust_mutation_inventory.json` with fields: `file`, `line`, `symbol`, `mutation_kind`, `is_allowed`.
   - Mark allowed mutation sites as only the new primitive module.
2. Create one trust mutation primitive.
   - Implement `plugins/_trust.py::mutate_trust_atomic(identifier: str, trusted: bool, *, actor: Optional[str]=None)` which:
     - acquires a module-level `threading.RLock` to guarantee thread-safe atomicity;
     - updates descriptor `trusted` flag and `_TRUSTED_*` sets in one critical section;
     - validates post-mutation invariant before releasing lock (in debug/test mode);
     - emits a structured trust-mutation event payload (Task 3 consumes this path for schema hardening).
   - Keep mutation idempotent: repeated calls with the same `(identifier, trusted)` state are no-op except for optional observability emission policy (must be documented).
3. Replace all direct mutations.
   - Route `mark_*_trusted` / `mark_*_untrusted` to `mutate_trust_atomic`.
   - Update registration-time trust propagation to use the helper.
   - Remove/forbid any remaining direct set add/remove and direct descriptor trust writes.
4. Add invariant guardrails and concurrency tests.
   - Add `tests/unit/plugins/test_trust_atomic.py` that runs concurrent toggles with threads and asserts invariant holds after each operation batch.
   - Add deterministic race tests for competing trust/untrust calls on the same identifier and different identifiers.
   - If process-based tests are kept, scope them to per-process invariant checks only (not cross-process lock guarantees).
   - Add a debug-only invariant checker invoked at end of mutation in CI test mode.
5. Add static check and CI enforcement.
   - `scripts/quality/check_trust_mutation_primitive.py` fails PRs if raw `_TRUSTED_*` mutations or direct descriptor `.trusted` writes are present outside the primitive.
   - Wire the check into `make local-checks-pr`.

Verification checklist:
- [x] `src/calibrated_explanations/plugins/_trust.py::mutate_trust_atomic` is the only allowed mutation path for trust state.
- [x] Post-mutation invariant is machine-checked in tests: descriptor trust flag equals trusted-set membership for all plugin kinds.
- [x] `tests/unit/plugins/test_trust_atomic.py` covers concurrent threaded toggles, same-identifier races, and idempotency.
- [x] `reports/trust_mutation_inventory.json` produced with all mutation sites and only primitive-module sites marked allowed.
- [x] `scripts/quality/check_trust_mutation_primitive.py` blocks direct trusted-set/descriptor-trust writes outside the primitive and is wired into `make local-checks-pr`.
- [x] Structured trust-mutation event payload emitted on each mutation path (schema validation completion tracked by Task 3).

---

## 3) Governance audit event contract for plugin decisions (ADR-028 + ADR-006 + STD-005)

Goal: enforce one machine-checked governance event contract for every plugin registration/discovery decision while preserving OSS runtime behavior.

Hardening findings to close:
1. Current event coverage is branch-incomplete: accepted, denied, untrusted-skip, and checksum-failure paths are not guaranteed to emit one canonical schema.
2. Contract location is inconsistent (`schema.py` vs `events.py`) and conflicts with Task 20’s planned extension path.
3. The original plan assumes a runtime `pydantic` model, but this repository’s runtime stack is JSON-schema oriented; the contract must follow existing schema validation patterns.
4. Add governance auditable `who/what/when/why` fields and side-effect-only observability (no prediction/explanation behavior drift).
5. Data-minimization risk: plugin metadata and event reasons can leak unnecessary sensitive detail unless explicitly bounded.

ADR/STD alignment requirements:
1. ADR-006: every plugin activation/rejection outcome must emit a structured audit event.
2. ADR-028 + STD-005: events must emit under `calibrated_explanations.governance.*` with structured fields and logging-context propagation.
3. STD-003: tests must be deterministic and assert schema validity for each decision branch.
4. STD-004: governance-event docs must be published as Tier-3 governance reference docs with explicit tier labeling.
5. Governance events must support parity-safe, side-effect-only hooks and provide evidence-ready fields (`who`, `what`, `when`, `why`, correlation IDs).

Primary touchpoints:
- [src/calibrated_explanations/plugins/registry.py](src/calibrated_explanations/plugins/registry.py)
- [src/calibrated_explanations/logging.py](src/calibrated_explanations/logging.py)
- New: [src/calibrated_explanations/governance/events.py](src/calibrated_explanations/governance/events.py)
- New: [src/calibrated_explanations/schemas/governance_event_schema_v1.json](src/calibrated_explanations/schemas/governance_event_schema_v1.json)
- New: `scripts/quality/check_governance_event_schema.py`
- Tests: `tests/observability/test_governance_events.py`
- Docs: `docs/foundations/governance/governance_events.md`

Implementation steps:
1. Standardize one event envelope implementation path.
   1. Use `src/calibrated_explanations/governance/events.py` (not `schema.py`) so Task 20 can extend the same contract.
   2. Define a typed event envelope plus JSON-schema-backed validation (`schema_version`, `event_name`, `decision`, fields map).
2. Define canonical plugin decision vocabulary and required fields.
   1. Decisions must cover: `accepted_registration`, `skipped_untrusted`, `skipped_denied`, `checksum_failure`, `denied_registration`.
   2. Required fields must include: `identifier`, `provider`, `source`, `trusted`, `reason_code`, `reason`, `actor`, `timestamp`, optional `invocation_id`, plus propagated context fields (`request_id`, `tenant_id`, `plugin_identifier`).
   3. Add `event_id` + `schema_version` for evidence-pack traceability and forward-compatible schema upgrades.
3. Validate before emit and route to governance logger domains.
   1. Emit through `calibrated_explanations.governance.plugins` (and `governance.registry` where task-scoped) with `ensure_logging_context_filter(...)`.
   2. Keep human-readable message concise; carry machine fields via `extra`.
   3. Enforce data minimization: no raw feature vectors, labels, predictions, or full plugin metadata blobs in governance event payloads.
4. Wire every decision path.
   1. Registration/discovery accepted path.
   2. Untrusted skip path.
   3. Denylist skip path.
   4. Checksum failure path.
   5. Explicit register-call denial/error path.
5. Add deterministic test coverage.
   1. Caplog tests assert one valid event per decision path and correct logger domain.
   2. Schema tests validate emitted payloads against `governance_event_schema_v1.json`.
   3. Side-effect parity test asserts enabling governance logging does not change registry acceptance/rejection outcomes.
6. Add CI/schema enforcement.
   1. Add `governance-event-schema` check/job running schema and caplog tests.
   2. Wire the check into `scripts/local_checks.py` and `make local-checks-pr`.
   3. Path-gate CI job to registry/governance/schema/logging touchpoints.
7. Document contract mapping.
   1. Document field dictionary, decision taxonomy, and schema evolution policy in `docs/foundations/governance/governance_events.md`.
   2. Include mapping snippet from OSS governance fields with `ce.*` namespaced telemetry.
   3. Add `Entry-point tier: Tier 3` footer and cross-links from governance index pages.

Verification checklist:
 - [x] `src/calibrated_explanations/governance/events.py` is the single plugin-governance event construction path used by registry decision branches.
 - [x] `src/calibrated_explanations/schemas/governance_event_schema_v1.json` exists, is versioned, and validates emitted payloads.
- [x] Every plugin decision branch emits one schema-valid event (`accepted_registration`, `skipped_untrusted`, `skipped_denied`, `checksum_failure`, `denied_registration`).
- [x] Event payloads include `who/what/when/why` + correlation fields and exclude raw sensitive payloads.
- [x] Deterministic caplog + schema tests pass and include side-effect parity assertions.
- [x] `governance-event-schema` CI check is path-gated and green; `make local-checks-pr` includes this guard.
 - [x] `docs/foundations/governance/governance_events.md` is published with decision taxonomy, ingestion mapping, and Tier-3 label.

Red-team hardening note (2026-03-20):
1. `_warn_untrusted_plugin` now emits `skipped_untrusted` governance events on every decision occurrence while preserving warning de-duplication, closing an audit-coverage gap for repeated untrusted discovery attempts.

---

## 4) Relocate test helper implementations from production module

Goal: remove test-helper scaffolding from production registry module.

Hardening analysis:

Threat model:
1. A consumer or internal runtime path can still reach test-only behavior through production imports (direct export, alias, or lazy/dynamic forwarder).
2. Test helper logic can leak into packaged runtime artifacts, creating an accidental supported surface.
3. Future contributors can silently reintroduce test-helper bridges in `src/` while tests still pass.

High-risk bypass paths identified:
1. Re-export bypass: helper wrappers removed from `registry.py` but re-exposed via `plugins/__init__.py` or module-level `__getattr__` forwarding.
2. Dynamic import bypass: runtime code calling `importlib.import_module("tests.support...")`, evading simple static `from ... import ...` checks.
3. Packaging bleed-through: wheel/sdist unintentionally includes test helper modules that runtime code can import.
4. Shadow helper drift: tests continue importing production-private internals directly after relocation, preserving anti-pattern under a new name.

Required hardening outcomes:
1. No production module in `src/` can import or forward names from `tests/` (static or dynamic).
2. No production export surface (`__all__`, package `__init__`, `__getattr__`) exposes test helper symbols.
3. Test helper modules are explicitly treated as non-runtime artifacts in packaging and import-policy checks.
4. CI must fail on reintroduction attempts, not only on current known helper names.

Primary touchpoints:

- `src/calibrated_explanations/plugins/registry.py`
- `tests/support/registry_helpers.py` (or approved testing module)
- Test files importing old helper locations
- Quality script enforcing no test-helper exports from `src/`

Step-by-step implementation:

1. Enumerate production-exposed testing helpers.
   1. Identify helper wrappers used only by tests.
   2. Categorize by trust, discovery, plot, validation helper groups.
2. Build dedicated test helper surface.
   1. Add `tests/support/registry_helpers.py` wrappers referencing internal runtime functions where needed.
   2. Keep naming stable for easy migration.
3. Migrate tests.
   1. Update test imports from `src/.../registry.py` to `tests/support/registry_helpers.py`.
   2. Ensure no runtime code imports `tests.support`.
4. Remove production wrapper bodies.
   1. Delete test-only wrapper functions from registry module.
   2. Retain private runtime internals required by real code.
5. Enforce policy.
   1. Confirm no test-helper exports in production modules.
   2. Keep/extend quality check script that blocks reintroduction.
   3. Add static checks for dynamic-import bypass patterns (e.g., `importlib.import_module("tests.`) in `src/`.
   4. Add packaging checks proving test helper files are not part of runtime wheel import surface.
6. Add regression tests/checks.
   1. Add a negative test/check that fails if any `src/` file imports from `tests.`.
   2. Add a negative test/check that fails if production package exports include known helper names.
   3. Add a smoke check that importing the installed runtime package does not resolve `tests.support` symbols through production namespaces.

Verification checklist:

- [x] All test-only helpers are served from `tests/support`.
- [x] No tests import test-only helpers from production registry module.
- [x] Registry production module no longer contains test-helper wrapper bodies. (`clear()` docstring "(testing convenience)" removed; `clear_legacy_registry()` added to `_testing.py` + `registry_helpers.py` as the idiomatic test path.)
- [x] Runtime import graph does not include `tests.support`.
- [x] Quality check for test-helper exports remains green.
- [x] Static check fails on `src/` dynamic-import patterns targeting `tests.` namespaces.
- [x] Production package export surface (`__all__`, package `__init__`, `__getattr__`) contains no test-helper aliases/forwarders.
- [x] Packaging/import smoke checks confirm runtime install cannot reach `tests.support` via production modules. (`test_production_package_does_not_expose_tests_support` added to `tests/integration/test_import_smoke.py`; 2 passed.)
- [x] Full test suite passes after migration. (86 plugin/registry/governance tests passed 2026-04-02.)

---

## 5) Legacy list-path deprecation closure in v0.11.x (ADR-011)

Goal: deprecate legacy `_REGISTRY`/`_TRUSTED` list path APIs with explicit migration path and enforced policy checks.

Primary touchpoints:

- `src/calibrated_explanations/plugins/registry.py`
- `src/calibrated_explanations/utils/deprecations.py` (existing helper usage)
- `docs/migration/deprecations.md`
- `tests/unit/plugins/test_legacy_api_deprecation.py` (new file)

Step-by-step implementation:

1. Audit internal callers before touching any public surface.
   1. Grep `src/` for every call site of `register(`, `trust_plugin(`, `find_for(`, and `find_for_trusted(` — scope to `src/calibrated_explanations/plugins/` and `src/external_plugins/`.
   2. Replace every in-tree caller with the keyed equivalent (step 2 below) before adding deprecation warnings, so that normal CI paths never emit legacy warnings.
2. Establish the exact replacement API mapping.
   1. `register(plugin)` → `register_explanation_plugin(identifier, plugin, metadata)`. The `identifier` is `plugin.plugin_meta["name"]`, which `register()` already derives internally.
   2. `trust_plugin(plugin)` — no keyed public replacement exists yet. Deprecation message must direct callers to pass `trusted=True` in the `metadata` dict at registration time via `register_explanation_plugin`. Do not create a new public keyed `trust_*` API in this task; record "future plugin-descriptor trust API" as a post-v0.11.1 backlog item.
   3. `find_for(model)` → `find_explanation_plugin_for(model)` (`trusted_only=False`).
   4. `find_for_trusted(model)` → `find_explanation_plugin_for(model, trusted_only=True)`.
3. Add `deprecate()` calls with canonical keys and sunset messages.
   1. `register()` — `key="legacy.plugin:register"`.
   2. `trust_plugin()` — `key="legacy.plugin:trust_plugin"`.
   3. `find_for()` — `key="legacy.plugin:find_for"`.
   4. `find_for_trusted()` — `key="legacy.plugin:find_for_trusted"`.
   5. Each message must include the v0.11.3 closure milestone and name the replacement API from step 2.
   6. Stable `key=` values are required for per-test deduplication in `deprecations.py`; do not leave any key implicit.
4. Update migration docs (`docs/migration/deprecations.md`).
   1. Add a section "Plugin registration list-path API (closed by v0.11.3)".
   2. Include a table: `register` → `register_explanation_plugin`, `trust_plugin` → registration-time `trusted=True`, `find_for` → `find_explanation_plugin_for`, `find_for_trusted` → `find_explanation_plugin_for(..., trusted_only=True)`.
   3. Include a short before/after code snippet for each row.
5. Add policy tests in `tests/unit/plugins/test_legacy_api_deprecation.py`.
   1. Reuse the `reset_deprecation_state` fixture from `tests/unit/utils/test_deprecations_helper.py`.
   2. One `pytest.warns(DeprecationWarning)` test per deprecated function (four tests).
   3. One `CE_DEPRECATIONS=error` block via `monkeypatch.setenv("CE_DEPRECATIONS", "error")` that asserts each of the four calls raises `DeprecationWarning` as an exception.

Verification checklist:

- [ ] `register`, `trust_plugin`, `find_for`, and `find_for_trusted` each call `deprecate()` with the canonical key listed in step 3 and a message containing both the sunset milestone and the replacement API name.
- [ ] All in-tree callers of the four functions have been migrated to keyed equivalents; `grep src/ 'register(\|trust_plugin(\|find_for('` returns only the definition sites and the new `deprecate()` call inside each function body.
- [ ] Normal runtime paths (no test patching) emit zero `DeprecationWarning` entries for legacy keys.
- [ ] `tests/unit/plugins/test_legacy_api_deprecation.py` exists with four warning-mode tests and one `CE_DEPRECATIONS=error` block.
- [ ] `docs/migration/deprecations.md` contains the "Plugin registration list-path API" table with replacement names and before/after examples.
- [ ] `make local-checks-pr` passes.

---

## 6) ADR-012 notebook execution and runtime ceilings in docs CI

Goal: close the ADR-012 notebook-execution implementation gap only: add deterministic notebook execution with runtime ceilings and auditable artifacts, while preserving existing docs-build/linkcheck architecture and branch-gate ownership.

ADR alignment hardening findings (red-team):
1. The true gap is notebook execution/runtime-ceiling enforcement and reporting; docs build/linkcheck workflows already exist and must not be redefined in this task.
2. Branch-policy wiring for notebook execution (advisory vs release-blocking) must be explicit and testable.
3. Skip-tag handling (`noexec`, `slow`) needs auditable output to avoid silent coverage gaps.
4. ADR-010 boundary risk: notebook capabilities must remain optional (`[notebooks,viz]` in CI only) and must not expand core dependencies.
5. ADR-035/CI-governance risk: avoid ad-hoc CI sprawl; integrate through existing modular workflows/policy checks and keep heavy jobs path-gated/advisory outside release boundaries.

Primary touchpoints:
- Execution driver: `scripts/docs/run_notebooks.py`
- CI wiring (within existing workflow architecture): `.github/workflows/ci-nightly.yml` and/or approved modular CI entrypoints
- Artifacts: `reports/docs/notebook_execution_report.json`
- Tests: `tests/docs/test_notebook_driver.py`
- Docs policy: `docs/contributor/notebook_policy.md`

Implementation steps:
1. Limit scope to notebook execution hardening.
   1. Do not redesign existing docs build/linkcheck jobs in this task.
   2. Do not introduce parallel CI governance paths that bypass current modular workflow policy.
2. Implement deterministic notebook execution driver contract.
   1. Add `scripts/docs/run_notebooks.py` using `nbconvert.preprocessors.ExecutePreprocessor` with `--cell-timeout` and `--notebook-timeout`.
   2. Enforce deterministic notebook traversal and stable report ordering.
   3. Emit `reports/docs/notebook_execution_report.json` with per-notebook records and summary.
   4. Required per-notebook fields: `notebook`, `status`, `elapsed_seconds`, `errors`, `invocation_id`, `skip_reason` (nullable), `mode` (`advisory` or `blocking`).
   5. Closed status vocabulary: `passed`, `failed`, `timed_out`, `skipped_noexec`, `skipped_slow`.
3. Define runtime-ceiling and skip policy with auditability.
   1. Default ceilings: `--cell-timeout 30`, `--notebook-timeout 300`.
   2. `noexec` metadata emits `skipped_noexec` with reason in report.
   3. `slow` metadata emits `skipped_slow` with reason in report; release-blocking inclusion/exclusion must follow documented branch policy.
   4. Unknown/undocumented skip tags are policy violations in blocking mode.
4. Wire CI behavior without violating ADR-035 constraints.
   1. Integrate into existing modular workflows (or approved reusable entrypoint pattern), not a one-off legacy-style wrapper.
   2. Keep notebook execution advisory outside release-boundary contexts and blocking on release/stable branches.
   3. Keep heavy-job gating/path filters consistent with CI governance rules.
5. Preserve ADR-010 optional-dependency boundary.
   1. CI notebook execution installs `[notebooks,viz]` extras only for the notebook job.
   2. No new core/runtime dependency additions are introduced by this task.
6. Add tests and policy checks.
   1. `tests/docs/test_notebook_driver.py` validates malformed notebook failure, timeout classification, skip-tag behavior, and report schema/order determinism.
   2. Add CI/policy assertions proving notebook mode resolution is advisory on mainline and blocking on release/stable.
   3. Add schema/validation checks that fail on unknown status values or missing required fields.
7. Update contributor policy only where notebook-specific behavior is missing.
   1. Document tags, timeout ceilings, seed/headless requirements, and blocking/advisory semantics for notebook execution.
   2. Cross-link to existing docs/release checklist locations rather than redefining their ownership.

Verification checklist:
- [x] `scripts/docs/run_notebooks.py` exists and emits deterministic `reports/docs/notebook_execution_report.json` with required fields.
- [x] Notebook execution mode is advisory outside release-boundary contexts and blocking on release/stable branches.
- [x] Per-cell and per-notebook timeouts are enforced and reported as `timed_out`.
- [x] `noexec` and `slow` handling is documented, auditable in report output, and not silently ignored.
- [x] CI integration follows existing modular workflow/policy governance (ADR-035-compatible) and keeps heavy-job gating behavior.
- [x] Notebook execution uses optional extras (`[notebooks,viz]`) without changing core dependency boundaries (ADR-010).
- [x] Driver/policy tests cover malformed notebooks, skip tags, timeout paths, schema validation, and branch-mode behavior.

---

## 7) Observability enforcement closure (ADR-027 + ADR-028 + STD-005)

Goal: make observability requirements enforceable via docs, tests, and policy checks.

Primary touchpoints:

- `src/calibrated_explanations/core/explain/_feature_filter.py`
- `src/calibrated_explanations/logging.py`
- `tests/observability/*`
- `scripts/quality/*` logger-domain checks
- Governance/observability docs

Step-by-step implementation:

1. Define required observability fields by event type.
   1. Feature-filter decisions.
   2. Governance trust/registration decisions.
2. Add docs examples.
   1. Text logging configuration example.
   2. JSON logging configuration example.
   3. Governance logger routing example.
3. Add tests.
   1. Caplog tests for domain names.
   2. Caplog tests for required structured fields.
4. Add enforcement script/check.
   1. Add or extend quality script to enforce logger domain patterns in touched modules.
   2. Hook script into local checks.
5. Confirm fallback visibility policy compliance.
   1. Warnings remain visible.
   2. Logging remains visible for fallback/governance branches.

Verification checklist:

- [x] Observability docs contain maintained, runnable logging examples.
- [x] Caplog tests assert logger domain + required field presence.
- [x] Logger domain quality script passes.
- [x] Fallback/governance paths continue emitting warning/log signals.
- [x] `make local-checks-pr` passes with observability checks enabled.

---

## 8) Standard-001 completion tasks (nomenclature cleanup)

Goal: close the remaining v0.11.1 Standard-001 gaps end-to-end by removing non-legacy
double-underscore runtime patterns, adding blocking automated enforcement, and completing the
in-scope utility/shim cleanup without violating ADR-020 compatibility expectations.

Governing sources and scope:
1. `docs/standards/STD-001-nomenclature-standardization.md` is the binding naming authority.
2. `docs/improvement/Standard-001_nomenclature_remediation.md` defines the broader remediation
   workstreams that remain in scope for this task: helper-module split and shim confinement.
3. `docs/improvement/RELEASE_PLAN_status_appendix.md` currently records two open STD-001 gaps:
   non-legacy double-underscore usage and missing automated enforcement. Task 8 must close those
   gaps with evidence.
4. ADR-020 governs compatibility posture for any retained bridge surface. ADR-027 is not a
   separate scope driver for this task; it matters only where FAST-mode internals still rely on
   remaining compatibility bridges.

Hardening notes:
1. Ruff naming checks are already blocking; they are necessary but not sufficient. This task must
   add a dedicated detector for non-legacy double-underscore runtime usage.
2. A temporary compatibility bridge is allowed only when full removal in v0.11.1 would violate
   current compatibility expectations. Any retained bridge must be thin, documented, excluded from
   new internal call paths, and assigned an explicit removal milestone no later than v0.11.3.
3. Utility split and shim confinement remain in scope for this task, but they must be executed as
   bounded workstreams rather than left as open-ended cleanup.

Non-goals:
1. Renaming or deprecating the stable public API beyond what ADR-020 already permits.
2. Broad namespace cleanup outside the evidenced STD-001 workstreams named below.
3. Claiming closure based only on lint success without a machine-checkable dunder detector report.

Primary touchpoints:

- `src/calibrated_explanations/core/calibrated_explainer.py`
- `src/calibrated_explanations/calibration/state.py`
- `src/calibrated_explanations/explanations/explanation.py`
- `src/calibrated_explanations/utils/helper.py`
- `src/calibrated_explanations/plotting.py`
- `src/calibrated_explanations/serialization.py`
- `src/calibrated_explanations/viz/builders.py`
- `src/calibrated_explanations/legacy/**`
- `scripts/quality/check_std001_nomenclature.py` (new)
- `scripts/local_checks.py`
- `.github/workflows/ci-pr.yml`
- `docs/improvement/Standard-001_nomenclature_remediation.md`
- `docs/improvement/RELEASE_PLAN_status_appendix.md`
- `CHANGELOG.md`

Step-by-step implementation:

1. Inventory remaining naming violations.
   1. Add a deterministic inventory pass for `src/` and write
      `reports/nomenclature_violation_inventory.json` with fields: `file`, `line`, `symbol`,
      `violation_kind`, `allowed_reason`, `workstream`.
   2. Required violation kinds are: `non_legacy_dunder_definition`,
      `non_legacy_dunder_access`, `non_legacy_dunder_mutation`, `compatibility_bridge`,
      `non_legacy_transitional_shim`, and `utility_import_bridge`.
   3. Allowed exceptions must be explicitly coded into the detector: Python protocol dunder names,
      legacy namespace files, and any approved temporary compatibility bridge documented by this
      task. Anything else in `src/` is a violation.
2. Refactor violations safely.
   1. Start with the current hotspot files named above; do not leave discovery implicit.
   2. Replace active non-legacy double-underscore definitions, accesses, and mutations with
      canonical single-underscore state or explicit accessor/property methods.
   3. Update call sites so active runtime paths no longer depend on mangled names.
   4. If a temporary compatibility bridge must remain, keep it as a thin delegator only, document
      it inline and in the plan/changelog, and ensure no new internal caller uses it.
3. Utility split completion.
   1. Split `src/calibrated_explanations/utils/helper.py` into topical modules per the remediation
      plan, keeping a bounded compatibility import bridge only where required.
   2. Update imports to canonical module layout and add import-parity coverage so the split is not
      behaviorally lossy.
   3. Completion is not “module created”; completion means canonical imports are used internally
      and compatibility imports remain stable where still supported.
4. Legacy shim confinement.
   1. Evaluate and classify the known shim-like surfaces in `plotting.py`, `serialization.py`, and
      `viz/builders.py`.
   2. Move transitional shims under `legacy/` where possible in v0.11.1.
   3. Where movement is not yet compatible, retain only a documented transitional bridge with a
      named removal milestone and no new internal dependency on that bridge.
5. Final lint hardening.
   1. Implement `scripts/quality/check_std001_nomenclature.py` as the blocking detector for
      non-legacy dunder usage and the inventory generator for this task.
   2. Wire the check into `scripts/local_checks.py` and the PR CI path so the gate is blocking for
      `src/` changes.
   3. Keep Ruff naming enforcement in place, but do not treat Ruff alone as closure evidence.
6. Tests and status-artifact closure.
   1. Add targeted regression coverage for the cleaned runtime state paths, utility import parity,
      and any retained shimmed import path.
   2. Update `docs/improvement/RELEASE_PLAN_status_appendix.md` only after detector output and
      tests demonstrate the remaining STD-001 gaps are resolved.
   3. Add a short `CHANGELOG.md` note if any temporary compatibility bridge or migration-visible
      shim decision remains after implementation.

Verification checklist:

- [ ] `reports/nomenclature_violation_inventory.json` exists, is deterministic, and records only
      explicitly allowed exceptions after remediation.
- [ ] No active non-legacy double-underscore runtime definitions, accesses, or mutations remain in
      `src/` outside explicitly documented compatibility bridges.
- [ ] Any retained compatibility bridge is thin, documented, unused by new internal call paths,
      and assigned a removal milestone no later than v0.11.3.
- [ ] `scripts/quality/check_std001_nomenclature.py` blocks non-legacy dunder regressions and is
      wired into `scripts/local_checks.py` and PR CI.
- [ ] Utility split produces stable canonical imports and import-parity tests cover the supported
      compatibility bridge.
- [ ] Transitional shim decisions for `plotting.py`, `serialization.py`, and `viz/builders.py` are
      explicit: moved to `legacy/`, retained as documented bridges, or deferred with named
      ownership.
- [ ] Targeted regression tests pass for FAST/runtime compatibility paths, calibration-state
      accessors, explanation-state cleanup, and shimmed import compatibility where retained.
- [ ] Ruff naming checks remain green.
- [ ] `docs/improvement/RELEASE_PLAN_status_appendix.md` and `CHANGELOG.md` are updated as needed
      with evidence-backed closure notes.

Task-8 implementation evidence snapshot (2026-04-14):

1. Automated enforcement + inventory delivered.
   1. `scripts/quality/check_std001_nomenclature.py` added and wired into local + CI PR checks.
   2. `reports/nomenclature_violation_inventory.json` generated with zero disallowed violations.
2. Utility/shim closure evidence made explicit.
   1. `docs/improvement/Standard-001_nomenclature_remediation.md` now includes explicit shim
      decisions for `plotting.py`, `serialization.py`, and `viz/builders.py`.
   2. Targeted parity/bridge regression tests added in
      `tests/unit/test_std001_task8_closure.py` and
      `tests/scripts/test_check_std001_nomenclature.py`.

---

## 9) Governance dashboard extension (lint + preprocessing/domain telemetry)  → Moved to v0.11.2

This task was relocated to v0.11.2. See `docs/improvement/v0.11.2_plan.md` Task 4 for the full implementation plan.

Primary motivation for relocation: depends on gate-locked Tasks 3 (plugin governance-event schema), 7 (observability enforcement), and 20 (config lifecycle governance schema) completing in v0.11.1, and fits the v0.11.2 governance sweep theme as a derived reporting artifact rather than a runtime governance contract.

---

## 10) CI workflow decommissioning (legacy wrappers)  → Completed in v0.11.1

This task is now completed in v0.11.1. Duplicate legacy CI wrappers were removed after replacement modular workflows were established and documented in `docs/improvement/CI-upgrade.md`.

Completed decommission scope:

- `.github/workflows/test.yml`
- `.github/workflows/coverage.yml`
- `.github/workflows/examples.yml`
- `.github/workflows/scan-private-members.yml`
- `.github/workflows/notebook-audit.yml`
- `.github/workflows/docs.yml`
- `.github/workflows/lint.yml`
- `.github/workflows/mypy.yml`
- `.github/workflows/dependency-audit.yml`

---

## 11) ADR-033 additive UX/migration gates

Goal: deliver additive modality UX and migration gates (`--modality` on CLI `list`, top-level
`vision`/`audio` shim modules, deprecation warning for plugins without explicit `data_modalities`).

**Canonical modality convention:** `base.py` uses `"vision"` as the canonical string (aliases: `"image"`,
`"images"`, `"img"` all normalise to `"vision"`; `"multi-modal"` and `"multi_modal"` normalise to
`"multimodal"`). ADR-033 §1.v is already corrected to match this convention.

**`find_explanation_plugin_for` normalises its `modality` input** through the same alias map — callers
may pass `"image"` and it resolves to `"vision"` candidates. A test must cover this.

**`MissingExtensionError` must be created first** — it does not yet exist in `utils/exceptions.py`. It must
inherit from both `CalibratedError` and `ImportError` so it can be caught as either. All shim modules
depend on it; implement it before the shim files.

**Shim module location:** `src/calibrated_explanations/vision.py` and `src/calibrated_explanations/audio.py`
at the package root (not under `plugins/`). This is the only location that makes
`import calibrated_explanations.vision` resolve correctly.

**`--modality` scope:** add it only to the `list` subcommand. All other subcommands (`show`, `trust`,
`untrust`, `validate-plot`, `validate-interval`) address plugins by identifier; modality filtering
is not applicable to them.

**Deprecation warning target:** no pre-existing legacy modality path exists. The warning fires when
`load_entrypoint_plugins` processes a plugin whose raw metadata dict does NOT contain an explicit
`data_modalities` key before `validate_plugin_meta` applies the default. It is directed at plugin
authors. Warning message: `"Plugin '{identifier}' does not declare 'data_modalities'; defaulting to
('tabular',). Explicit declaration will be required in v0.11.3."` Do not emit for plugins registered
directly via `register_explanation_plugin` (builtins and tests use that path).

Primary touchpoints:

- `src/calibrated_explanations/utils/exceptions.py` — add `MissingExtensionError`
- `src/calibrated_explanations/vision.py` — new top-level shim module
- `src/calibrated_explanations/audio.py` — new top-level shim module
- `src/calibrated_explanations/__init__.py` — do NOT auto-import shims (lazy-only)
- `src/calibrated_explanations/plugins/cli.py` — add `--modality` to `list` subcommand only
- `src/calibrated_explanations/plugins/registry.py` — emit `DeprecationWarning` in `load_entrypoint_plugins` for plugins without explicit `data_modalities`
- `tests/unit/plugins/test_adr033_contract.py` — extend with shim and CLI filter tests
- `CHANGELOG.md` — release note entry

Step-by-step implementation:

1. Add `MissingExtensionError` to `src/calibrated_explanations/utils/exceptions.py`.
   1. Add class `MissingExtensionError(CalibratedError, ImportError)` with docstring referencing ADR-033.
   2. Add `"MissingExtensionError"` to `__all__`.
   3. No other code changes in this step — do not reference it yet.
2. Add top-level shim modules (ADR-033 §4.3 — try-import pattern required).
   1. Create `src/calibrated_explanations/vision.py`:
      ```python
      """Shim for the calibrated_explanations.vision modality namespace (ADR-033 §4)."""
      from .utils.exceptions import MissingExtensionError
      try:
          from ce_vision import *  # noqa: F401, F403
          from ce_vision import __all__  # noqa: F401
      except ImportError:
          raise MissingExtensionError(
              "calibrated_explanations.vision requires the 'ce-vision' package. "
              "Install with: pip install calibrated_explanations[vision]"
          ) from None
      ```
   2. Create `src/calibrated_explanations/audio.py` with the same structure, importing from
      `ce_audio` and referencing `calibrated_explanations[audio]`.
   3. Do not add these modules to `src/calibrated_explanations/__init__.py` — shims are
      import-triggered only. The `try/except ImportError` pattern works for callers because
      `MissingExtensionError` is a subclass of `ImportError`.
3. Add `--modality` to the `list` subcommand in `cli.py`.
   1. Add `--modality` argument to `list_parser` accepting a comma-separated list of canonical modality names (e.g., `--modality vision,tabular`).
   2. In `_cmd_list`, after collecting descriptors for each kind, filter out descriptors whose `data_modalities` do not overlap with the requested modalities. Apply filter only when `--modality` is explicitly provided.
   3. Normalise the user-supplied modality tokens using `_normalise_modality` from `plugins/base.py` before comparing. Raise `SystemExit(1)` with a user-facing message on unrecognised modality token.
   4. `--modality` is additive: do NOT change the existing `--trusted-only` or `--verbose` behavior.
4. Add `DeprecationWarning` for plugins without explicit `data_modalities` (ADR-033 §6.1).
   1. In `registry.py`, inside `load_entrypoint_plugins`, snapshot whether `"data_modalities"` is
      present in the raw `plugin_meta` dict BEFORE calling `validate_plugin_meta` (which applies
      the `("tabular",)` default).
   2. If absent, emit:
      `warnings.warn(f"Plugin '{identifier}' does not declare 'data_modalities'; defaulting to ('tabular',). Explicit declaration will be required in v0.11.3.", DeprecationWarning, stacklevel=2)`.
   3. Do not emit this warning for plugins registered directly via `register_explanation_plugin`
      (builtins and tests use that path; the warning is directed at external plugin authors only).
5. Add tests in `tests/unit/plugins/test_adr033_contract.py`.
   1. `test_missing_extension_error_is_importerror`: `import calibrated_explanations.vision` raises
      `MissingExtensionError`; verify `isinstance(exc, ImportError)` is `True`.
   2. `test_missing_extension_error_audio`: same for `calibrated_explanations.audio`.
   3. `test_cli_list_modality_filter`: register two plugins with different `data_modalities`, run
      `_cmd_list` with `--modality` for only one; assert only matching plugin appears in output.
   4. `test_cli_list_modality_filter_invalid_token`: verify `SystemExit` with code 1 on
      unrecognised modality token.
   5. `test_deprecation_warning_on_plugin_without_modality`: use `unittest.mock.patch` on
      `importlib.metadata.entry_points` to present a plugin whose raw `plugin_meta` omits
      `data_modalities`; call `load_entrypoint_plugins`; assert `DeprecationWarning` is emitted.
   6. `test_find_explanation_plugin_for_accepts_alias` (ADR-033 §1.v.4): register a plugin with
      `data_modalities: ("vision",)`; call `find_explanation_plugin_for("image", ...)` (alias);
      assert it resolves to the registered plugin without raising. This covers the normalization
      added to `registry.py:find_explanation_plugin_for`.
6. Update CHANGELOG with short descriptions..
   1. `[Unreleased] > Added`: "ADR-033: `vision`/`audio` shim modules — delegates to `ce_vision`/`ce_audio` when installed, raises `MissingExtensionError` otherwise."
   2. `[Unreleased] > Added`: "ADR-033: `--modality` filter on `ce plugins list`."
   3. `[Unreleased] > Added`: "ADR-033: `find_explanation_plugin_for` now normalises modality aliases so callers may pass `'image'` to match `'vision'` plugins."
   4. `[Unreleased] > Deprecated`: "Plugins without explicit `data_modalities` in entry-point metadata; default-fallback behaviour removed in v0.11.3."

Verification checklist:

- [x] `MissingExtensionError` in `utils/exceptions.py`, inherits both `CalibratedError` and `ImportError`, listed in `__all__`.
- [x] `import calibrated_explanations.vision` without `ce_vision` installed raises `MissingExtensionError` with install guidance.
- [x] `import calibrated_explanations.audio` without `ce_audio` installed raises `MissingExtensionError` with install guidance.
- [x] Neither shim is auto-imported in `calibrated_explanations/__init__.py`.
- [x] `ce plugins list --modality vision` filters to only `vision`-modality plugins; other kinds unchanged.
- [x] `ce plugins list --modality image` resolves alias and filters the same as `--modality vision`.
- [x] Invalid modality token on `--modality` exits with code 1 and a diagnostic message.
- [x] `DeprecationWarning` emitted exactly once per entry-point plugin missing explicit `data_modalities`; message names the plugin and cites `v0.11.3`.
- [x] `find_explanation_plugin_for("image", ...)` resolves to a `vision`-tagged plugin (alias normalisation).
- [x] All six new tests pass under `pytest -q tests/unit/plugins/test_adr033_contract.py`.
- [x] `make local-checks-pr` stays green.

---

## 12) ADR-033 follow-through documentation

Goal: publish complete contributor + practitioner docs for modality plugin rollout and migration.
Must run after Task 11 — docs reference the shim modules and `--modality` CLI flag created there.

**Concrete file targets** (replaces the earlier vague "docs/contributor/\*plugins\*" placeholder):
- `docs/contributor/plugin-contract.md` — existing; add **Modality contract** section
- `docs/contributor/extending/plugin-advanced-contract.md` — existing; add **Extension packaging** section
- `docs/practitioner/advanced/use_plugins.md` — existing; add cross-reference
- `docs/practitioner/advanced/modality-plugins.md` — new file; modality selection guide
- `docs/practitioner/advanced/index.md` — add link to the new page
- `docs/improvement/adrs/ADR-033-modality-extension-plugin-contract-and-packaging.md` — correct §1.v text

**ADR §1.v is already corrected** — canonical = `vision`; aliases include `image`, `images`, `img`,
`multi-modal`, `multi_modal`. Verify the text before writing docs; do not re-apply the old text.

**`find_explanation_plugin_for` is undocumented:** `registry.py:find_explanation_plugin_for` (ADR-033 §3)
is the primary public API for modality-based plugin resolution. It must appear in the new
`modality-plugins.md` with a runnable example.

**Verification command:** run `python -m sphinx docs docs/_build/html -W --keep-going -q`
(or `make docs`) — zero warnings/errors is the pass criterion. "Docs checks pass" alone is not sufficient.

Primary touchpoints:

- `docs/contributor/plugin-contract.md`
- `docs/contributor/extending/plugin-advanced-contract.md`
- `docs/practitioner/advanced/use_plugins.md`
- `docs/practitioner/advanced/modality-plugins.md` (new)
- `docs/practitioner/advanced/index.md` (add modality-plugins link)
- `docs/improvement/adrs/ADR-033-modality-extension-plugin-contract-and-packaging.md` (text correction §1.v)

Step-by-step implementation (must run after Task 11 is complete):

1. Verify ADR-033 §1.v text matches code convention.
   1. Confirm canonical modalities list reads: `tabular`, `vision`, `audio`, `text`, `multimodal`.
   2. Confirm aliases include `image -> vision`, `images -> vision`, `img -> vision`,
      `multi-modal -> multimodal`, `multi_modal -> multimodal`.
   3. Confirm §1.v.4 states that `find_explanation_plugin_for` normalises its input through the alias map.
   4. If any of the above differs, correct the ADR text before proceeding; do not write docs against a wrong spec.
2. Update `docs/contributor/plugin-contract.md`.
   1. Add section **Modality contract** covering: canonical modality strings and their aliases;
      `data_modalities` metadata key (required from `v0.11.3`; defaults to `("tabular",)`
      until then); `plugin_api_version` format; major-hard/minor-soft compatibility policy.
   2. Note that omitting `data_modalities` emits `DeprecationWarning` in v0.11.1 (message names the
      plugin and cites `v0.11.3`) and becomes mandatory in `v0.11.3`.
3. Update `docs/contributor/extending/plugin-advanced-contract.md`.
   1. Add section **Extension packaging** covering: entry-point group name
      (`calibrated_explanations.plugins`); the entry point must resolve to an object with a
      `plugin_meta` attribute; `plugin_api_version` field placement in `pyproject.toml`;
      version compatibility smoke test reference (Task 13 at
      `tests/unit/plugins/test_adr033_packaging_smoke.py`).
4. Create `docs/practitioner/advanced/modality-plugins.md`.
   1. Explain canonical modalities and the full alias map (include `multi-modal`, `multi_modal`).
   2. State the CE-first invariant (ADR-033 §10): existing `CalibratedExplainer` tabular workflows
      are unaffected; `find_explanation_plugin_for` is an opt-in helper and is not called by
      `CalibratedExplainer` itself.
   3. Show `find_explanation_plugin_for(modality="vision", mode="factual", task="classification", model=clf)`
      as the primary runnable example. Add a note that aliases work: passing `modality="image"` is
      equivalent and resolves to `vision`-tagged plugins.
   4. Show `ce plugins list --modality vision` CLI output example; note `--modality image` gives
      the same result via alias resolution.
   5. Explain `MissingExtensionError` and install guidance for `ce-vision` / `ce-audio`; show the
      `try/except ImportError` availability-check pattern.
   6. Include a **Migration timeline** table:
      | Version | Behaviour |
      |---------|-----------|
      | v0.11.0 | `data_modalities` defaults to `("tabular",)`; no warning |
      | v0.11.1 | `DeprecationWarning` if `data_modalities` absent from entry-point plugin metadata |
      | v0.11.3 | Explicit `data_modalities` required; default-fallback removed |
5. Update `docs/practitioner/advanced/use_plugins.md`.
   1. Add cross-reference to `modality-plugins.md` in the plugin selection section.
6. Add `modality-plugins` link to `docs/practitioner/advanced/index.md`.
7. Update `CHANGELOG.md` with short descriptions.
   1. Add entry under `[Unreleased] > Added`: "ADR-033: practitioner modality guide and contributor contract docs."

Verification checklist:

- [x] ADR-033 §1.v confirmed: canonical = `vision`; aliases include `image`, `multi-modal`, `multi_modal`; §1.v.4 states alias normalisation in `find_explanation_plugin_for`.
- [x] `docs/contributor/plugin-contract.md` contains **Modality contract** section with `data_modalities` deprecation timeline citing `v0.11.3`.
- [x] `docs/contributor/extending/plugin-advanced-contract.md` contains **Extension packaging** section with entry-point group name `calibrated_explanations.plugins`.
- [x] `docs/practitioner/advanced/modality-plugins.md` exists with: CE-first invariant statement, `find_explanation_plugin_for` example, alias-works note, `MissingExtensionError` pattern, migration table.
- [x] Migration table deadline reads `v0.11.3` (not plain `v0.11.3`).
- [x] `docs/practitioner/advanced/index.md` links to the new page.
- [ ] Sphinx build (`make docs` or `python -m sphinx docs docs/_build/html -W --keep-going -q`) produces zero warnings/errors on changed files. *(Blocked in this container: `sphinx` is unavailable; must be verified in CI/release environment.)*
- [x] All cross-links between the new page and existing plugin docs resolve.

---

## 13) ADR-033 packaging smoke test

Goal: add one packaging smoke test that validates entry-point discovery and `plugin_api_version`
contract checks without requiring a subprocess install or network access.
Must run after Task 11 — the smoke test exercises the full registry discovery path that Task 11
completes.

**Discovery approach:** use `unittest.mock.patch` on `importlib.metadata.entry_points` in-process.
This avoids subprocess overhead, network dependency, and venv complexity while still exercising
the full `load_entrypoint_plugins` code path. No actual pip install is required and no timeout
marker is needed — the approach has no I/O.

**Test file placement:** `tests/unit/plugins/test_adr033_packaging_smoke.py` (co-located with
`test_adr033_contract.py`). Do not create a new top-level `tests/packaging/` directory.

**Fixture file tree** (required, fully specified):
```
tests/fixtures/ce_mock_modality_plugin/
    ce_mock_modality_plugin/
        __init__.py      # defines MinimalModalityPlugin
    pyproject.toml       # documents entry-point group; NOT installed at test time
```
`MinimalModalityPlugin` must satisfy the `ExplainerPlugin` protocol: `plugin_meta` dict with all
required keys including `data_modalities: ("vision",)` and `plugin_api_version: "1.0"`, plus
`supports(model)` and `explain(model, x, **kw)` methods.

**`plugin_api_version` major-mismatch test is required:** ADR-033 §1.iv.a mandates rejection on
major version mismatch. Include a test case where a fixture plugin declares
`plugin_api_version: "2.0"` and verify the plugin is NOT registered. The registry swallows
`ValidationError` during discovery by emitting a `UserWarning` and skipping the plugin, but does
NOT add any record to `PluginDiscoveryReport` (`checksum_failures` is only populated by
`_verify_plugin_checksum`, not by `validate_plugin_meta` failures). Use `pytest.warns(UserWarning)`
to assert rejection — do NOT assert on `get_last_discovery_report()`.

Primary touchpoints:

- `tests/fixtures/ce_mock_modality_plugin/ce_mock_modality_plugin/__init__.py` (new)
- `tests/fixtures/ce_mock_modality_plugin/pyproject.toml` (new, documents entry-point group)
- `tests/unit/plugins/test_adr033_packaging_smoke.py` (new)
- `tests/unit/plugins/test_adr033_contract.py` (possibly extend with shared fixture)

Step-by-step implementation (must run after Task 11 is complete):

1. Create the mock extension fixture package.
   1. Create `tests/fixtures/ce_mock_modality_plugin/ce_mock_modality_plugin/__init__.py`:
      ```python
      class MinimalModalityPlugin:
          plugin_meta = {
              "schema_version": 1,
              "name": "ce-mock-modality-plugin",
              "version": "0.1.0",
              "provider": "tests.fixtures",
              "capabilities": ("explain",),
              "data_modalities": ("vision",),
              "plugin_api_version": "1.0",
              "trusted": False,
          }
          def supports(self, model): return True
          def explain(self, model, x, **kw): raise NotImplementedError
      ```
   2. Create `tests/fixtures/ce_mock_modality_plugin/pyproject.toml`:
      ```toml
      [project]
      name = "ce-mock-modality-plugin"
      version = "0.1.0"
      [project.entry-points."calibrated_explanations.plugins"]
      "tests.mock.modality" = "ce_mock_modality_plugin:MinimalModalityPlugin"
      ```
      This file documents the expected entry-point group name. It is NOT installed; it serves as
      the reference for what the monkeypatched entry-point simulates.
2. Create `tests/unit/plugins/test_adr033_packaging_smoke.py`.
   1. Import `importlib.metadata`, `unittest.mock`, and `calibrated_explanations.plugins.registry`.
   2. Write a helper `_fake_entry_point(name, value_str)` that returns a mock object mimicking
      `importlib.metadata.EntryPoint` with `.name`, `.group`, and `.load()` returning the
      `MinimalModalityPlugin` class (import it from the fixture path using `importlib.import_module`
      after inserting `tests/fixtures/ce_mock_modality_plugin` into `sys.path`).
   3. Test cases (all use `unittest.mock.patch` on `importlib.metadata.entry_points`):
      - `test_entry_point_discovery_registers_valid_plugin`: patch entry_points to return the
        `MinimalModalityPlugin` fixture; call `load_entrypoint_plugins(include_untrusted=True)`;
        assert `find_explanation_descriptor("tests.mock.modality")` is not None.
      - `test_entry_point_discovery_validates_modality_field`: after registration, retrieve descriptor
        and assert `descriptor.metadata["data_modalities"] == ("vision",)`.
      - `test_entry_point_discovery_normalises_modality_alias` (ADR-033 §8.2): create a fixture
        class whose raw `plugin_meta` declares `data_modalities: ("image",)` (an alias); after
        `load_entrypoint_plugins`, assert `descriptor.metadata["data_modalities"] == ("vision",)`.
        This confirms `validate_plugin_meta` normalises aliases at registration time.
      - `test_entry_point_discovery_rejects_major_version_mismatch`: create a fixture class with
        `plugin_api_version: "2.0"` inline; patch entry_points; assert `pytest.warns(UserWarning,
        match="Invalid metadata")` is raised during `load_entrypoint_plugins`; assert
        `find_explanation_descriptor("tests.mock.modality.bad")` is None. Do NOT assert on
        `get_last_discovery_report()` — `validate_plugin_meta` failures emit `UserWarning` and are
        skipped without adding any record to `PluginDiscoveryReport` (ADR-033 §8.3).
      - `test_entry_point_contract_plugin_api_version_default`: create a fixture class with no
        `plugin_api_version` key but with explicit `data_modalities: ("vision",)` (to avoid also
        triggering the `DeprecationWarning` from Task 11); after registration, assert
        `descriptor.metadata["plugin_api_version"] == "1.0"`.
   4. Each test must clear the explanation registry via `clear_explanation_plugins()` in setup/teardown.
   5. All tests use `tests.support.registry_helpers.clear_explanation_plugins` (existing public helper).
3. Verify test isolation.
   1. Tests must not leave any registered plugins after completion.
   2. Tests must not write to disk.
   3. No subprocess calls, no network calls.
4. Update CHANGELOG with short descriptions.
   1. Add entry under `[Unreleased] > Added`: "ADR-033: packaging smoke test (`test_adr033_packaging_smoke.py`) validates entry-point discovery, modality contract, and `plugin_api_version` compatibility."

Verification checklist:

- [x] `tests/fixtures/ce_mock_modality_plugin/` directory exists with `__init__.py` and `pyproject.toml`.
- [x] `MinimalModalityPlugin` satisfies the `ExplainerPlugin` protocol minimally (has `plugin_meta`, `supports`, `explain`).
- [x] `test_entry_point_discovery_registers_valid_plugin` passes: discovery registers the fixture and descriptor is not None.
- [x] `test_entry_point_discovery_validates_modality_field` passes: `descriptor.metadata["data_modalities"] == ("vision",)`.
- [x] `test_entry_point_discovery_normalises_modality_alias` passes: raw `("image",)` stored as `("vision",)` after registration.
- [x] `test_entry_point_discovery_rejects_major_version_mismatch` passes: `UserWarning` emitted, descriptor is None; no assertion on `PluginDiscoveryReport` (ADR-033 §8.3).
- [x] `test_entry_point_contract_plugin_api_version_default` passes: missing key defaults to `"1.0"`.
- [x] All five tests pass under `pytest -q tests/unit/plugins/test_adr033_packaging_smoke.py`.
- [x] Registry is clean after each test (no leaked descriptors).
- [x] No subprocess calls or network I/O in any test.
- [x] `pyproject.toml` in fixture documents the entry-point group name `calibrated_explanations.plugins`.

---

## 14) Reject framework expansion beyond binary conformal rejectors

Goal: expand reject framework with additional strategy types (including uncertainty-based rejectors) and document implementation/usage.

Primary touchpoints:

- `src/calibrated_explanations/core/reject/*`
- `src/calibrated_explanations/explanations/reject.py`
- Reject orchestrator/strategy registry implementation
- `tests/unit/*reject*`
- `tests/integration/*reject*`
- Reject policy docs and ADR-029 linked guidance

Step-by-step implementation:

1. Define strategy extension contract.
   1. Formalize strategy interface (input payload, score/decision output, metadata keys, errors).
   2. Ensure compatibility with existing `RejectResult` envelope.
2. Extend strategy registry.
   1. Support multiple registered strategy identifiers.
   2. Keep default strategy behavior unchanged for backward compatibility.
3. Implement uncertainty-based strategy.
   1. Use calibrated uncertainty signals (interval width/probability uncertainty or approved metric).
   2. Define configurable threshold semantics.
   3. Emit strategy metadata in envelope for auditability.
4. Wire strategy selection.
   1. Add strategy selection hooks in orchestrator path.
   2. Validate fallback/default behavior when strategy not explicitly set.
5. Add tests.
   1. Unit tests for strategy registry registration/resolution.
   2. Unit tests for uncertainty strategy decisions around threshold boundaries.
   3. Integration tests for policy + strategy interactions.
   4. Regression tests proving default binary conformal strategy remains unchanged when selected/default.
6. Document usage.
   1. Add practitioner examples for uncertainty-based rejection.
   2. Add contributor example for implementing custom strategy.
   3. Add migration note that this is additive and backward compatible.

Verification checklist:

- [x] Strategy interface contract is documented and implemented.
- [x] Registry supports multiple strategy identifiers.
- [x] Uncertainty-based strategy is implemented and test-covered.
- [x] `RejectResult` compatibility is preserved for all strategies.
- [x] Default reject behavior is unchanged when legacy/default strategy path is used.
- [x] Strategy metadata is present for governance/audit inspection.
- [x] Practitioner + contributor docs include concrete strategy examples.

---

## 15) Centralized configuration resolution via `ConfigManager` (ADR-034)

Goal: establish a single configuration authority (`ConfigManager`) with deterministic precedence and migrate runtime configuration access to it.

Planning basis (workspace evidence as of 2026-04-07):

- ADR exists: `docs/improvement/adrs/ADR-034-centralized-configuration-management.md` (`Accepted`, 2026-04-07).
- The current workspace does **not** contain the Phase A runtime artifacts previously implied by older plan text:
  - `src/calibrated_explanations/core/config_manager.py`
  - `scripts/quality/check_config_manager_usage.py`
  - `tests/unit/core/test_config_manager.py`
  - `tests/scripts/test_check_config_manager_usage.py`
- No current implementation evidence was found for `ce config show` / `ce config export` in runtime source.
- Direct runtime configuration reads still exist in current code:
  - `src/calibrated_explanations/plugins/manager.py`
  - `src/calibrated_explanations/plugins/registry.py`
  - `src/calibrated_explanations/logging.py`
  - `src/calibrated_explanations/plotting.py`
  - `src/calibrated_explanations/plugins/cli.py`
  - `src/calibrated_explanations/core/config_helpers.py`
- Generated documentation under `docs/_build/` references `ConfigManager`, but generated HTML is not evidence that the runtime implementation exists in the workspace.
- ADR-034 now defines the normative contract for this task:
  - `ConfigManager` is snapshot-based.
  - Environment and `pyproject.toml` are captured at construction time.
  - Existing runtime objects do not observe later env changes.
  - Task 15 establishes authority and migration scope; Task 20 alone closes `governance.config` lifecycle-event coverage.

Primary touchpoints:

- `docs/improvement/adrs/ADR-034-centralized-configuration-management.md`
- `src/calibrated_explanations/core/config_manager.py`
- `src/calibrated_explanations/plugins/manager.py`
- `src/calibrated_explanations/plugins/registry.py`
- `src/calibrated_explanations/logging.py`
- `src/calibrated_explanations/plugins/cli.py`
- `scripts/quality/check_config_manager_usage.py`
- `scripts/local_checks.py`
- `tests/unit/core/test_config_manager.py`
- `tests/scripts/test_check_config_manager_usage.py`
- `tests/unit/plugins/test_cli_coverage.py`
- `docs/improvement/RELEASE_PLAN_v1.md`
- `docs/migration/deprecations.md`

Step-by-step implementation:

1. Implement the ADR-defined contract exactly.
   1. Keep precedence contract fixed: `override > env > pyproject > default_profile`.
   2. `ConfigManager.from_sources()` must capture env and `pyproject.toml` once at construction time.
   3. `ConfigManager.env(key)` must return the captured snapshot value, not the live process environment.
   4. Mid-process env changes are picked up only by reconstructing the manager or reconstructing the owning runtime object.
2. Establish ownership and lifecycle.
   1. Migrated runtime consumers must either accept an injected `ConfigManager` or construct one once and retain it for their own lifetime.
   2. Fresh manager construction on every lookup is non-compliant because it defeats snapshot semantics.
3. Recreate the missing runtime manager and checker/test artifacts.
   1. Implement `src/calibrated_explanations/core/config_manager.py`.
   2. Expose the public surface only after the module exists and import paths are verified.
   3. Keep strict validation default (`ConfigurationError` on unknown keys/type mismatches).
   4. Define a stable snapshot/export contract (`ResolvedConfigSnapshot`, source map, profile id, schema version) in code and tests, not just docs.
   5. Add `tests/unit/core/test_config_manager.py`.
   6. Add `scripts/quality/check_config_manager_usage.py` and `tests/scripts/test_check_config_manager_usage.py`.
4. Migrate the v0.11.1 Phase A runtime surfaces named by ADR-034.
   1. Replace direct env/pyproject reads in `plugins/manager.py`.
   2. Replace direct env/pyproject reads in `plugins/registry.py`.
   3. Replace direct env/pyproject reads in `logging.py`.
   4. Replace CLI trust/read/export config access paths in `plugins/cli.py`.
   5. Replace direct env/pyproject reads in `plotting.py`.
   6. Keep `core/config_helpers.py` as a sanctioned low-level ingestion boundary only; migrated runtime consumer modules must not call it directly.
5. Add CLI and diagnostic surfaces only after manager-backed resolution works.
   1. Implement and test `ce config show`.
   2. Implement and test `ce config export`.
   3. CLI commands must report the effective snapshot built for that CLI invocation; they do not introspect already-instantiated in-memory runtime objects.
6. Add enforcement after the migration path exists.
   1. Implement `scripts/quality/check_config_manager_usage.py`.
   2. Wire it into `scripts/local_checks.py`.
   3. Use an explicit temporary allowlist with documented justifications, release ownership, and expiry rather than implicit drift.
7. Keep observability boundaries clear.
   1. Task 15 may add private hook points needed by later config lifecycle emission.
   2. Task 15 must not claim `config.resolve`, `config.export`, or `config.validation_failure` lifecycle-event closure.
   3. Task 20 remains the closure task for schema-backed `governance.config` lifecycle events.
   4. Redaction-sensitive export/logging behavior remains a documented open item until separately implemented.
8. Tests + gates.
   1. Add precedence, strict-validation, snapshot/export, and CLI coverage in unit/tests scripts once the code exists.
   2. Add migration tests covering plugin manager, plugin registry, logging, plotting, and CLI behavior through manager-backed config resolution.
   3. Run the config-usage enforcement check and targeted pytest coverage before claiming migration complete.
   4. Keep `make local-checks-pr` as the release-task gate once the local enforcement path is wired.
9. Docs synchronization.
   1. Update migration guidance only after the live implementation exists.
   2. Treat generated docs in `docs/_build/` as build artifacts, not planning evidence.
   3. Record the compatibility impact explicitly: ad hoc direct env reads previously behaved like live reads in some paths; centralized snapshot-based config intentionally changes that behavior.
   4. Keep ADR/release-plan linkage current after the recovered implementation is verified.

Verification checklist:

### Completed in Task 15 (v0.11.1 Phase A)

- [x] ADR-034 is Accepted and is the binding authority for centralized config management scope.
- [x] `ConfigManager` implementation exists with strict validation, precedence resolution, snapshot, and export support.
- [x] CLI export/show config commands are implemented and test-covered.
- [x] Plugin manager/registry and telemetry logging runtime paths are migrated to manager-based config access.
- [x] `plugins/cli.py` trust/read/export paths are migrated to manager-based config access.
- [x] `plotting.py` runtime config reads are migrated to manager-based config access.
- [x] `core/config_helpers.py` is used only as a sanctioned low-level ingestion boundary, not by migrated runtime consumer modules.
- [x] Governing snapshot semantics are test-covered: existing managers do not observe later env changes, and reconstruction is required to pick them up.
- [x] Enforcement script exists and is wired into local checks to block new direct env/pyproject runtime reads.
- [x] Migration/release docs include centralized config migration guidance.
- [x] `python scripts/local_checks.py --skip-main` passes after integration.

### Deferred to Task 20 (out of Task 15 closure)

- [x] Schema-backed `governance.config` lifecycle-event closure (`config.resolve`, `config.export`, `config.validation_failure`) is complete. *(Owned by Task 20; completed 2026-04-08)*
- [x] Governance config lifecycle event schema hardening and closure evidence are complete. *(Owned by Task 20; completed 2026-04-08)*

### Deferred to v0.11.2 (ADR-034 Phase B)

- [ ] Remaining allowlisted runtime modules are migrated to `ConfigManager` ownership patterns and removed from the CI allowlist. *(Owned by v0.11.2 Task 1)*
- [ ] Config-usage enforcement reaches zero-runtime-allowlist closure, or any remaining sanctioned boundary is explicitly documented with release ownership. *(Owned by v0.11.2 Tasks 1-2)*

### Status integrity rule

- [x] Task 15 is treated as Phase A closure only; it must not claim Task 20 lifecycle-event closure.
- [x] If transitional allowlist entries remain, status wording must stay partial and point to explicit v0.11.2 ownership.
- [x] ADR-034 status is `Accepted`.

Red-team findings incorporated into the task design (2026-04-02):
1. Stale implementation claims removed.
   1. The plan now treats Task 15 as a real implementation task, not as validation of supposedly existing artifacts.
2. Snapshot semantics locked.
   1. Task 15 now follows ADR-034’s snapshot semantics exactly and no longer leaves room for live env-read compatibility drift.
3. Ownership and lifecycle defined.
   1. The task now requires injected or retained manager ownership and forbids per-lookup reconstruction.
4. `plotting.py` ambiguity removed.
   1. `plotting.py` is explicitly in Phase A scope for Task 15.
5. Boundary policy clarified.
   1. `core/config_helpers.py` remains sanctioned only as a low-level ingestion boundary.
6. Task 15 / Task 20 overlap removed.
   1. Task 15 may prepare hooks, but Task 20 alone closes `governance.config` lifecycle-event coverage.
7. Generated-doc drift explicitly discounted.
   1. `docs/_build/` output is treated as non-authoritative and cannot be used as implementation evidence.

---

## 16) Promote ADR-020 to Accepted  *(Done 2026-03-03)*

Goal: advance ADR-020 from Draft to Accepted now that the legacy API contract is current for v0.11.0 removals.

**Status:** Complete.

Delivered:
- ADR-020 `Status` field set to `Accepted (2026-03-03)`.
- `docs/improvement/legacy_user_api_contract.md` updated: status note date refreshed, `.get_explanation(i)` note corrected, new `## Removed in v0.11.0` section listing all v0.11.0 removals with replacements.
- `## Implementation Status` in ADR-020 extended with a 2026-03-03 promotion note.

Verification checklist:
- [x] ADR-020 `Status:` line reads `Accepted`.
- [x] `legacy_user_api_contract.md` contains a `## Removed in v0.11.0` section.
- [x] All v0.11.0 removals documented with replacement guidance.

---

## 17) Promote ADR-028 to Accepted  *(Done 2026-03-03)*

Goal: advance ADR-028 from Draft to Accepted before the enforcement tooling task (Task 7) merges, so that task enforces an authoritative policy record.

**Status:** Complete.

Delivered:
- ADR-028 `Status` field set to `Accepted (2026-03-03)`.
- Status note date refreshed.

Verification checklist:
- [x] ADR-028 `Status:` line reads `Accepted`.

---

## 18) Update legacy_user_api_contract.md for v0.11.0 removals  *(Done 2026-03-03)*

See Task 16 — delivered as part of the ADR-020 promotion package.

---

## 19) Add Standard-005 to RELEASE_PLAN_v1.md roadmap  *(Done 2026-03-03)*

Goal: add Standard-005 row to the ADR/Standards roadmap summary and Standards appendix in `RELEASE_PLAN_v1.md` so gap-analysis history is traceable.

**Status:** Complete.

Delivered:
- Standard-005 roadmap summary row added after Standard-004.
- Standard-005 appendix section added with two open gap entries (both targeting Task 7).
- ADR-028 and ADR-020 roadmap summary rows updated to reflect `Accepted` status.
- ADR-029 through ADR-034 appendix entries added.

Verification checklist:
- [x] `RELEASE_PLAN_v1.md` roadmap contains `Standard-005` row.
- [x] Appendix contains `### Standard-005` section.
- [x] Appendix contains `### ADR-029` through `### ADR-034` sections.

---

## 20) Add governance.config lifecycle event schema and emission (ADR-034 + ADR-028)

Goal: introduce a separate `governance_config_event_schema_v1.json` for `calibrated_explanations.governance.config` lifecycle events and wire emission into `ConfigManager` at exactly the three snapshot-lifecycle boundaries defined by ADR-034.

Why: ADR-034 §5 and ADR-028 require structured observability for configuration lifecycle decisions. Task 15 (Phase A) deliberately deferred this closure. The existing `governance_event_schema_v1.json` cannot be extended to cover config events — its `event_name: const` (`"plugin.registration.decision"`) and `decision: enum` (plugin-specific values) would reject any config event at validation time. A separate schema file and separate logger domain are therefore required.

Prerequisites: Task 3 (plugin `GovernanceEvent` schema and `governance/events.py` pattern established) and Task 15 (`ConfigManager` implemented with `from_sources()`, `export_effective()`, `_validate_sections()`, and snapshot semantics).

Primary touchpoints:

- `src/calibrated_explanations/schemas/governance_config_event_schema_v1.json` (new)
- `src/calibrated_explanations/governance/events.py`
- `src/calibrated_explanations/core/config_manager.py`
- `tests/observability/test_config_governance_events.py` (new preferred) or extend `tests/unit/core/test_config_manager.py`
- `scripts/quality/check_governance_event_schema.py` (extend, do not create new)

Step-by-step implementation:

1. Add `governance_config_event_schema_v1.json`.
   1. Do NOT modify `governance_event_schema_v1.json`. Its `event_name: const` and `decision: enum` constraints are plugin-specific; changing them would be a breaking schema revision.
   2. Create `src/calibrated_explanations/schemas/governance_config_event_schema_v1.json` with the following required fields:
      - `schema_version` (string, const `"1.0"`)
      - `event_id` (string, uuid format)
      - `event_name` (string, const `"config.lifecycle"`)
      - `event_type` (string, enum: `"resolve"` | `"export"` | `"validation_failure"`)
      - `profile_id` (string)
      - `config_schema_version` (string)
      - `strict` (boolean)
      - `source_count` (integer ≥ 0)
      - `validation_issue_count` (integer ≥ 0)
      - `timestamp` (string, date-time format)
   3. Set `additionalProperties: false`. Config lifecycle events are governance artefacts; adding a field must require a deliberate schema revision. This makes the schema gate meaningful: any accidental field addition — including sensitive-value leakage — is caught at validation time.
   4. Document alongside the schema as a data-minimization rule: raw config values, resolved plugin identifiers, raw env strings, and raw pyproject payloads MUST NOT appear in any payload field, per ADR-034 Open Item 1 (full redaction deferred to v1.0, but leakage prevented by construction and test).

2. Add `validate_config_governance_event`, `build_config_governance_event`, and `emit_config_governance_event`.
   1. Add to `governance/events.py` or a new `governance/config_events.py`. Follow the existing builder/emitter/validator pattern from Task 3.
   2. Add `_config_schema_json()` using `importlib.resources.files("calibrated_explanations.schemas").joinpath("governance_config_event_schema_v1.json")` — same pattern as the existing `_schema_json()`. Do NOT use relative file paths: they break in installed packages.
   3. Add `validate_config_governance_event(payload)` that validates against `governance_config_event_schema_v1.json`. This is separate from and MUST NOT be confused with the existing `validate_governance_event`, which enforces plugin-event constraints (`PLUGIN_GOVERNANCE_DECISIONS` enum, `trusted: bool`) and would reject every config event.
   4. `build_config_governance_event(*, event_type, profile_id, config_schema_version, strict, source_count, validation_issue_count, details=None) -> dict` — constructs the payload, stamps `event_id`, `event_name`, `timestamp`; calls `validate_config_governance_event` before returning.
   5. `emit_config_governance_event(...)` — calls the builder, then:
      - Calls `ensure_logging_context_filter("calibrated_explanations.governance.config")` for filter installation (ADR-028 §3).
      - Does NOT wrap `logger.info(...)` in a `logging_context(...)` call. Config lifecycle events have no applicable context field from the ADR-028 §3 set (`request_id`, `tenant_id`, `explainer_id`, `checkpoint_id`, `plugin_identifier`). Emit using whatever ambient context is already present. Do not fabricate a `plugin_identifier`.
      - Logs via `logging.getLogger("calibrated_explanations.governance.config")`, separate from `calibrated_explanations.governance.plugins`.
   6. Add `CONFIG_GOVERNANCE_EVENT_TYPES = ("resolve", "export", "validation_failure")` as a module-level constant parallel to `PLUGIN_GOVERNANCE_DECISIONS`.
   7. Export `validate_config_governance_event`, `build_config_governance_event`, `emit_config_governance_event`, and `CONFIG_GOVERNANCE_EVENT_TYPES` in `__all__`.

3. Compute `source_count` cheaply and cache it.

   Computing `source_count` via `_resolve_effective_key()` for all 24 `_KNOWN_ENV_KEYS` (the `export_effective()` approach) is O(n) and would penalise every `ConfigManager` construction, including test constructions. Instead:
   - Define `source_count = len(self._env_snapshot) + sum(len(v) for v in self._pyproject_snapshot.values())`. This counts all contributing non-default entries and is O(1) from already-stored dicts.
   - Compute and store it as `self._source_count` at the end of `__init__` (after `_validate_sections()` sets `self._validation_report`). Reuse `self._source_count` in both the `resolve` event (step 4a) and the `export` event (step 4b).

4. Wire emission into `ConfigManager` at exactly three sites.

   ADR-034 §2 (snapshot semantics) dictates that resolution is a one-time construction event. Emission from per-accessor methods (`env()`, `pyproject_section()`) is non-compliant: it conflates construction-time resolution with call-time access and produces repeated noisy events. Direct `__init__` construction with injected snapshots is the injection/testing pattern; emitting from `__init__` would fire on every test-constructed manager, polluting test log output and potentially interfering with existing task 15 tests. The `resolve` event is semantically about reading live sources, which only `from_sources()` does.

   Emit only at:
   1. End of `ConfigManager.from_sources()`, after the manager is successfully constructed — `event_type="resolve"`. Use `manager._source_count`. Set `validation_issue_count=0` (construction succeeded without raising). This is the only real source-resolution path per ADR-034 §2.
   2. End of `ConfigManager.export_effective()`, after the `ResolvedConfigSnapshot` is computed — `event_type="export"`. Use `self._source_count` (already cached). Set `details={"diagnostic_only": True}` to honour ADR-034 Open Item 2 (export schema contract not yet versioned for external consumers).
   3. Inside `_validate_sections()`, after `issues` list is fully collected, in BOTH strict and non-strict paths — `event_type="validation_failure"`. ADR-034 §4 states "observability is not suppressed" in `strict=False` mode. Set `validation_issue_count=len(issues)`. `details` MUST contain ONLY `{"location": issues[0].location, "issue_count": len(issues)}` — never include `issue.message`, which always contains `{value!r}` (raw config value). In strict mode: fire before raising; in non-strict mode: fire before the warning is emitted.

   Do NOT emit from `__init__` directly or from per-accessor methods (`env()`, `pyproject_section()`).

5. Add tests.
   1. Three log-capture tests using `pytest`'s `caplog` fixture with `calibrated_explanations.governance.config` as the logger name — one per emission site:
      - `ConfigManager.from_sources()` (with patched env) → exactly one `resolve` event; assert `event_type`, `source_count`, `validation_issue_count=0`, logger name. In the same test, construct `ConfigManager(env_snapshot={}, pyproject_snapshot={})` directly and assert that NO governance event is emitted (injection path is silent).
      - `export_effective()` call → exactly one `export` event; assert `details == {"diagnostic_only": True}`; assert `source_count` matches `_source_count`.
      - Invalid pyproject entry with strict=True → one `validation_failure` event before `ConfigurationError`; with strict=False → one `validation_failure` event before the warning; assert `details` contains exactly `{"location": ..., "issue_count": ...}` and no other keys.
   2. Schema-gate tests: call `validate_config_governance_event` for each of the three event types and assert no exception is raised. Also assert that calling `validate_governance_event` (plugin validator) on a config event payload raises `ValidationError` — this documents the incompatibility explicitly and guards against future confusion.
   3. Data-minimization tests: for each emitted payload assert (a) no field named `env_snapshot`, `pyproject_snapshot`, `values`, `message`, or `_env_snapshot` exists at any nesting level, and (b) `details` keys are exactly the allowed set for that event type (`{"diagnostic_only"}` for export; `{"location", "issue_count"}` for validation_failure; `None` for resolve).
   4. Multi-instance test: construct two `ConfigManager` instances via `from_sources()` with different patched env snapshots; assert two independent `resolve` events with distinct, correct `source_count` values are emitted.
   5. Extend `scripts/quality/check_governance_event_schema.py` (the existing Task 3 gate script) to validate config-event fixtures against `governance_config_event_schema_v1.json`. Do NOT create a separate script; wire the extension into the same `make local-checks-pr` path.

6. Docs.
   1. Update `docs/foundations/governance/governance_events.md` (established in Task 3) with a new section for config lifecycle events: logger name, event types, required fields, exact payload examples for each event type, and the strict/non-strict distinction for `validation_failure`.
   2. State explicitly that the `export` event is diagnostic-only until ADR-034 Open Item 2 is resolved in v1.0.
   3. State explicitly that raw config values, env strings, and pyproject payloads are prohibited in all payload fields, with a cross-link to ADR-034 Open Item 1.
   4. Do not alter ADR-034 or ADR-028 normative text without an ADR process.
7. Contract boundary for future follow-on work.
   1. Task 20 defines the runtime, schema-validated governance event contract for configuration lifecycle events.
   2. Later governance status or dashboard artifacts may aggregate or summarize Task 3 and Task 20 outputs, but must not introduce a parallel runtime event vocabulary for config lifecycle state.
   3. Any new runtime governance event type or schema must extend the governance event docs and schema gates introduced here, not bypass them through CI-only artifact formats.

Red-team findings incorporated into this plan (2026-04-07):

1. Emission site `__init__` vs `from_sources()` — reasoning was backwards.
   - Direct `__init__` construction is the injection/testing path and must not emit. `from_sources()` is the only real source-resolution path per ADR-034 §2. Emitting from `__init__` would pollute task 15 test logs.
2. `validation_failure` must fire in both strict and non-strict modes.
   - ADR-034 §4 says "observability is not suppressed" in `strict=False`. Both paths now emit the event.
3. `source_count` O(n) computation removed from construction path.
   - Replaced with O(1) dict-length proxy cached as `self._source_count` in `__init__`.
4. `export` event fires at end of `export_effective()`, not at entry.
   - Entry placement cannot supply accurate `source_count`. Event fires after computation; `_source_count` is reused from the cache.
5. `additionalProperties: false` chosen over `true`.
   - Governance schema gate only catches leakage if extra fields are rejected. `additionalProperties: false` makes sensitive-value leakage detectable at schema-validation time.
6. `details` leak path made unconditional and field-enumerated.
   - `issue.message` always contains `{value!r}`. Prohibition is now unconditional. Allowed `details` keys are enumerated exactly per event type and asserted in data-minimization tests.
7. `emit_config_governance_event` logging context specified.
   - `ensure_logging_context_filter` installed; no `logging_context(...)` wrapper (no applicable ADR-028 §3 context fields for config events; fabricating `plugin_identifier` is wrong).
8. `validate_config_governance_event` named explicitly; incompatibility with plugin validator tested.
   - Calling the existing `validate_governance_event` on config events is explicitly guarded against by a schema-gate test that asserts it raises.
9. Phase B stability test respecified as a testable multi-instance assertion.
   - "Without schema revision" (untestable process claim) replaced with two-instance `source_count` correctness assertion.
10. CI gate extension strategy specified.
    - Extends existing `check_governance_event_schema.py` rather than creating a new script.

Verification checklist:

- [x] `governance_config_event_schema_v1.json` exists with `additionalProperties: false`; `governance_event_schema_v1.json` is unchanged.
- [x] Schema requires `event_name: "config.lifecycle"` and `event_type` enum; no plugin-domain fields (`trusted`, `decision`) present.
- [x] `validate_config_governance_event`, `build_config_governance_event`, `emit_config_governance_event`, and `CONFIG_GOVERNANCE_EVENT_TYPES` exist and are exported; config schema loaded via `importlib.resources`.
- [x] `source_count` is `len(env_snapshot) + sum(len(v) for v in pyproject_snapshot.values())`, cached as `self._source_count` in `__init__`.
- [x] `resolve` event fires from `from_sources()` only; direct `__init__` construction emits nothing (tested explicitly).
- [x] `export` event fires at end of `export_effective()`; `details == {"diagnostic_only": True}`.
- [x] `validation_failure` event fires in both strict=True and strict=False paths; `details` contains exactly `{"location": ..., "issue_count": ...}` and no other keys.
- [x] `emit_config_governance_event` calls `ensure_logging_context_filter` but has no `logging_context(...)` wrapper.
- [x] Logger name is `calibrated_explanations.governance.config` in all emitted events.
- [x] Schema-gate test asserts `validate_governance_event` (plugin validator) raises on a config event payload.
- [x] Data-minimization tests assert exact allowed key sets for `details` per event type and absence of raw value fields at all nesting levels.
- [x] Multi-instance test asserts two independent `resolve` events with distinct `source_count` values.
- [x] `check_governance_event_schema.py` extended to cover config-event fixtures; `make local-checks-pr` green.
- [x] `docs/foundations/governance/governance_events.md` updated with config-lifecycle section.
- [x] `pytest tests/observability` or `pytest tests/unit -k "config_governance or config_event"` is green.

---

## Consolidated release gate checklist (v0.11.1)

- [ ] Remaining open milestone tasks are 5, 11, 12, and 13. Tasks 1-4, 6-8, 10, 14-20, and 22 are complete or relocated as noted. (Task 9 remains relocated to v0.11.2.)
- [x] Tasks 16-19 governance doc updates delivered (done 2026-03-03).
- [x] Task 20 `GovernanceEvent` config lifecycle extension delivered and schema gate green. *(Completed 2026-04-08: schema hardening + fixture-gated validation + targeted observability/script tests green.)*
- [ ] `make local-checks-pr` passes for every merged task PR.
- [ ] `make local-checks` passes for tasks touching branch-gate behavior.
- [ ] Migration and release docs reflect the still-open deprecation and modality timeline work, plus completed reject strategy expansion, `ConfigManager` precedence/migration guidance, and PlotSpec ADR supersession routing (ADR-036/ADR-037 authoritative).
- [x] ADR/release-plan status text is synchronized with delivered behavior as of 2026-04-09 for Tasks 14, 15, and 20.
- [x] ADR-020 and ADR-028 promoted to Accepted (done 2026-03-03).

## Recommended validation command set

```powershell
make local-checks-pr
make local-checks
pytest -q tests/unit/plugins
pytest -q tests/unit/viz
pytest -q tests/unit -k "plotspec or plot_kind"
pytest -q tests/observability
pytest -q tests/packaging
pytest -q tests/unit -k reject
pytest -q tests/integration -k reject
pytest -q tests/unit -k config
pytest -q tests/integration -k "pyproject or config"
```

## 21) API-bloat removal program (v0.11.x zero-deprecation closure, plugin-only LIME/SHAP)

Goal: complete the LIME/SHAP core-surface retirement inside the `v0.11.x` line so **no LIME/SHAP deprecations survive into v1.0.0**.

Binding policy alignment:
1. ADR-011 default two-minor policy remains the normal baseline.
2. ADR-011 v1.0 finalization exception overrides that baseline for this task.
3. Therefore this task is **not** “deprecate now, remove at v1.0.0”; it is a bounded multi-milestone closure plan executed entirely in `v0.11.1` → `v0.11.3`.

Milestone ownership (mandatory):
1. **v0.11.1 (this milestone):** inventory, warning coverage, migration mapping, and CI strict-mode enforcement for all core LIME/SHAP entry points.
2. **v0.11.2:** remove core exports/wrapper hooks and stop routing runtime behavior through deprecated core entry points.
3. **v0.11.3:** delete residual compatibility stubs, finalize migration docs as removed-history items, and enforce an empty active-deprecation ledger for this workstream.

Primary touchpoints:

- `src/calibrated_explanations/core/calibrated_explainer.py`
- `src/calibrated_explanations/explanations/explanations.py`
- `src/calibrated_explanations/integrations/lime.py` and `integrations/shap.py`
- `pyproject.toml`
- `docs/migration/deprecations.md`
- `tests/unit/core/test_lime_shap_core_deprecation.py`
- `docs/improvement/v0.11.2_plan.md` (removal follow-through checkpoint)
- `docs/improvement/v0.11.3_plan.md` (final deletion/ledger checkpoint)

Step-by-step implementation (v0.11.1 scope, concrete and verifiable):

1. Inventory the complete set of core LIME/SHAP entry points.
   1. Confirm the ten known methods and detect any additional public hooks in `__init__` exports.
   2. Record the definitive inventory in this task and in `docs/migration/deprecations.md`.
2. Add mandatory deprecation warnings and stable keys.
   1. Add `deprecate()` calls for every inventoried method.
   2. Messages must include milestone ownership: deprecated in v0.11.1, removed by v0.11.3 under the v1.0 zero-deprecation rule.
3. Enforce warning coverage.
   1. Add one warning assertion per deprecated entry point.
   2. Add one `CE_DEPRECATIONS=error` test block covering the full set.
4. Publish migration mapping.
   1. Add direct replacement mapping to plugin-only adapters in `docs/migration/deprecations.md`.
   2. Mark each mapped symbol with explicit removal milestone `v0.11.2` or `v0.11.3`.
5. Prepare v0.11.2 removal patch set.
   1. Create a removal checklist that v0.11.2 must execute (core export deletion + wrapper hook removal + import-path cleanup).
   2. Link that checklist to v0.11.2 release-gate text.
6. Define v0.11.3 final closure check.
   1. Require migration table movement from Active → Removed history for this surface.
   2. Require zero active deprecations for LIME/SHAP core paths at v0.11.3 close.

Verification checklist:

- [ ] All inventoried core LIME/SHAP entry points emit `deprecate()` warnings with stable keys and v0.11.x closure language.
- [ ] `tests/unit/core/test_lime_shap_core_deprecation.py` covers every entry point plus a `CE_DEPRECATIONS=error` aggregate check.
- [ ] `docs/migration/deprecations.md` includes a replacement table with explicit v0.11.2/v0.11.3 removal ownership.
- [ ] v0.11.2 plan contains the concrete removal follow-through checklist for these symbols.
- [ ] v0.11.3 plan contains the final deletion + deprecation-ledger-empty closure criteria.
- [ ] `make local-checks-pr` passes.


## 22) PlotSpec hardening + ADR revisioning (ADR-036/ADR-037)

Goal: implement the v0.11.1 PlotSpec hardening workstream as one bounded delivery that (a) hardens PlotSpec into an enforceable canonical IR contract and (b) replaces fragmented visualization ADR authority with ADR-036/ADR-037 while preserving legacy-default plotting behavior in v0.11.1.

Primary touchpoints:

- `docs/improvement/adrs/ADR-036-plot-spec-canonical-contract-and-validation-boundary.md`
- `docs/improvement/adrs/ADR-037-visualization-extension-and-rendering-governance.md`
- `docs/improvement/adrs/superseded ADR-007-visualization-abstraction-layer.md`
- `docs/improvement/adrs/superseded ADR-014-plot-plugin-strategy.md`
- `docs/improvement/adrs/superseded ADR-016-plot-spec-separation.md`
- `src/calibrated_explanations/viz/plotspec.py`
- `src/calibrated_explanations/viz/serializers.py`
- `src/calibrated_explanations/viz/builders.py`
- `src/calibrated_explanations/viz/matplotlib_adapter.py`
- `src/calibrated_explanations/plotting.py`
- `tests/unit/viz/*`
- `tests/unit/plugins/*plot*`
- `docs/api/visualization.md`, `docs/api/serialization.md`, `docs/api/plugins.md`, `docs/plugins.md`

Step-by-step implementation:

1. Author and ratify the two-ADR authority split.
   1. Confirm ADR-036 is the sole authority for canonical PlotSpec contract, dataclass-only in-memory form, validation boundary, forbidden backend leakage, and compatibility-boundary rules.
   2. Confirm ADR-037 is the sole authority for builder/renderer governance, strict-minimal metadata, legacy-default policy, and runtime kind-extension deferral.
   3. Keep ADR-023 separate and unchanged except optional cross-reference text.
2. Supersede ADR-007/ADR-014/ADR-016 without deleting historical context.
   1. Ensure each superseded ADR has explicit supersession note + `Superseded-by` metadata pointing to ADR-036/ADR-037.
   2. Ensure active docs route architectural authority to ADR-036/ADR-037 and do not treat superseded ADRs as current policy.
3. Harden the canonical PlotSpec contract in implementation.
   1. Ensure canonical in-memory PlotSpec uses dataclass models; boundary dict/JSON forms are serializer artifacts only.
   2. Enforce canonical required semantic fields (`kind`, `mode`, required semantic payload, stable semantic identity metadata).
   3. Forbid renderer-owned formatting fields in canonical objects; allow semantic roles/tokens only.
4. Strengthen validator and boundary sequencing.
   1. Enforce `builder -> canonical validation -> renderer` ordering in runtime code paths.
   2. Ensure validators reject backend-specific leakage and malformed canonical payloads before renderer entry.
   3. Keep compatibility translation strictly in serializer/translator boundaries (not canonical dataclasses, validators, or renderer contracts).
5. Unify builder and renderer contracts.
   1. Builders MUST return canonical PlotSpec objects only.
   2. Renderers MUST consume validated canonical PlotSpec only.
   3. Remove or fail-closed any builder/renderer path that accepts raw dicts as contract input.
6. Decompose matplotlib-adapter boundary responsibilities.
   1. Keep renderer responsibility limited to rendering semantic input.
   2. Move normalization/export/compat translation into explicit pre-render boundary layers where needed.
   3. Keep test instrumentation hooks outside canonical semantic model objects.
7. Preserve v0.11.1 public behavior constraints.
   1. Keep legacy plotting as the default `.plot()` path in v0.11.1.
   2. Keep PlotSpec path opt-in/non-default in v0.11.1.
   3. Keep runtime plot-kind extension disabled in v0.11.1; plot kinds remain core-defined only.
8. Add regression tests for contract hardening.
   1. Validator behavior: required-field checks, backend-leakage rejection, malformed canonical payload rejection.
   2. Builder contract: unit tests asserting builders return canonical dataclass PlotSpec instances only.
   3. Serializer round-trips: boundary dict/JSON conversions preserve canonical meaning without promoting dicts to canonical in-memory authority.
   4. Renderer boundary expectations: renderer tests assert validated canonical input requirement and fail on boundary artifacts/unvalidated objects.
   5. Public behavior: regression tests verify `.plot()` still defaults to legacy path in v0.11.1.

Red-team findings (2026-03-20):

1. Canonical/boundary drift existed for non-panel builders.
   1. `build_triangular_plotspec(...)` and `build_global_plotspec(...)` returned dict-like wrappers instead of canonical dataclasses.
   2. Risk: builders leaked boundary serialization artifacts as in-memory authority.
2. Renderer boundary sequencing was weak in the default PlotSpec renderer.
   1. `PlotSpecDefaultRenderer` accepted mapping artifacts directly and delegated without canonical dataclass validation.
   2. Risk: malformed mapping payloads could bypass explicit canonical-contract checks.
3. Triangular plotting path relied on dict mutation for save behavior.
   1. `plot_triangular(...)` merged save metadata into dict envelopes instead of keeping canonical object flow.
   2. Risk: compatibility logic spread outside serializer/translator boundaries.

Implementation updates (2026-03-20):

1. Canonical dataclass returns for non-panel builders.
   1. `build_triangular_plotspec(...)` now returns `TriangularPlotSpec`.
   2. `build_global_plotspec(...)` now returns `GlobalPlotSpec`.
   3. Boundary dict helpers remain explicit: `build_*_plotspec_dict(...)`.
2. Canonical validation helpers added to serializer boundary.
   1. Added `canonical_plotspec_to_dict(...)` and `validate_canonical_plotspec(...)` in `viz/serializers.py`.
3. PlotSpec default renderer hardened.
   1. Renderer now canonicalizes mapping input via serializer boundaries (`*_from_dict(...)`), validates canonical dataclass, then renders.
   2. Renderer wraps canonicalization/validation failures in `ConfigurationError` for stable plugin error semantics.
   3. Renderer returns serialized envelope in `PlotRenderResult.artifact` for compatibility.
4. Triangular runtime path moved to canonical builder output.
   1. `plot_triangular(...)` now uses `build_triangular_plotspec(...)` and passes canonical spec to renderer.
   2. Per-extension save loop now uses renderer `save_path` directly.
5. Regression tests updated for hardened contract behavior.
   1. Updated plugin builtins test to assert canonical `GlobalPlotSpec` output.
   2. Targeted viz/plugin/plotting suites executed with `pytest -o addopts=` to avoid unrelated global coverage gate failures in focused runs.
6. Legacy-default behavior restored in practice for v0.11.1 public `.plot()` paths.
   1. Builtin plot-style registration now marks `legacy` as default and keeps `plot_spec.default` as explicit fallback.
   2. Plot style chain resolvers preserve legacy-first default ordering while still retaining PlotSpec fallback.
   3. Added regression coverage asserting default legacy selection when `use_legacy=None`.

Verification checklist:

- [x] ADR-036/ADR-037 text is authoritative and internally coherent; ADR-023 remains separate.
- [x] ADR-007/ADR-014/ADR-016 are explicitly superseded and retained as historical records.
- [x] Canonical PlotSpec in-memory contract is dataclass-based; dict/JSON are boundary artifacts only.
- [x] Builders return canonical PlotSpec only; renderers accept validated canonical PlotSpec only.
- [x] Compatibility translation occurs only at serializer/translator boundaries.
- [x] Matplotlib adapter boundary is decomposed so rendering does not redefine canonical semantics. Require validated canonical input before rendering.
- [x] Runtime plot-kind extension remains disabled in v0.11.1 with core-defined kinds only.
- [x] Legacy plotting remains default public `.plot()` behavior in practice for v0.11.1 (`use_legacy=None` resolves legacy-first unless explicitly overridden).
- [x] Regression tests exist for validator behavior, builder contract, serializer round-trip boundaries, renderer boundary enforcement, and legacy-default plotting behavior.
- [x] `make local-checks-pr` passes.
