# v0.11.0 Release Task Implementation Plan

This plan expands the v0.11.0 tasks from `docs/improvement/RELEASE_PLAN_v1.md`
with implementation details grounded in ADRs, engineering standards, and the
current code layout. Each section lists concrete work items, impacted modules,
verification steps, and open questions needed to satisfy the release gate.

v0.11.0 is an **ADR gap closure milestone**. The authoritative task list is the
"v0.11.0 (domain model & preprocessing finalisation)" section in
`docs/improvement/RELEASE_PLAN_v1.md`.

## Source references reviewed

- Release task list + appendix references: `RELEASE_PLAN_v1.md`.
- ADRs: all files in `adrs/` (including superseded ADRs).
  - Primary drivers for v0.11.0 are ADR-004/005/008/009/010/012/020/026/027/028/030/031.
- Standards: all files in `../standards/` (STD-001..STD-005).
- Standard-001 execution roadmap: `Standard-001_nomenclature_remediation.md`.
- Test standards and enforcement guidance:
  - `../../.github/tests-guidance.md` (authoritative test-writing rubric)
  - `../standards/STD-003-test-coverage-standard.md`
  - `ANTI_PATTERN_REMEDIATION_PLAN.md`
- Existing tooling/scripts referenced by release tasks:
  - `scripts/audit_notebook_api.py` (ADR-020)
  - `scripts/detect_test_anti_patterns.py` (ADR-030)
  - `scripts/snapshot_public_api.py` (API surface snapshot)
  - `../../.github/private_member_allowlist.json` (Pattern 1 remediation)

---

## Release tasks covered (from `RELEASE_PLAN_v1.md`)

The v0.11.0 milestone contains 16 tasks. This plan has matching sections for
each of them, in order.

---

## Global rules (deterministic reproducibility + security)

These rules apply across all v0.11.0 work items (domain model, preprocessing,
serialization, persistence, plugin telemetry).

1. **Canonical hashing of manifests.**
      - Any manifest used for artifact identity or reproducibility MUST be
         canonicalized deterministically and hashed. Canonicalization rules
         are strict and MUST be followed verbatim when producing the bytes to
         hash:

         - Numbers must be rendered in shortest decimal form (no gratuitous
           exponential notation) where feasible.
         - Datetimes must be serialized as ISO-8601 UTC (`YYYY-MM-DDThh:mm:ssZ`).
         - `null` must use JSON `null`.
         - Canonical JSON MUST be produced by:

           ```py
           canonical = json.dumps(
                 manifest,
                 sort_keys=True,
                 separators=(",",":"),
                 ensure_ascii=False,
           ).encode("utf-8")
           parameters_hash = "sha256:" + sha256(canonical).hexdigest()
           ```

      - Manifests MUST NOT include raw training data or PII. If sensitive keys
         are detected, a `ValidationError` MUST be raised with a machine-readable
         error code (e.g. `CE_VALIDATION_PII`) so automation can react.
      - PII/sensitive key handling (normative):
         - A configurable denylist and allowlist of payload keys (strings or
           regex patterns) is REQUIRED. If any denylisted key is present in a
           manifest, `ValidationError` is raised unless the caller explicitly
           requests redaction.
         - Provide an explicit redaction helper that replaces sensitive values
           with the literal string `'<redacted>'` before persistence when
           redaction mode is requested. Record `redaction_applied: true` in the
           manifest when redaction occurred.
         - Provide an allowlist override API for known-safe fields; overrides
           must be auditable and recorded in the manifest metadata.
      - Persist the canonical manifest as an immutable artifact and, where
         permitted, reference it from `metadata.audit.config_artifact` (or
         `metadata.audit.run_id` when artifact storage is unavailable). The
         manifest MUST include `access_control` and `retention_policy` fields
         (see artifact manifest schema below) and documentation must explain
         how artifact storage enforces RBAC and how to revoke access.

      - Minimize runtime and build dependencies: prefer Python stdlib code for
         canonicalization, hashing, and validation. Introduce third-party
         libraries only when the functionality cannot be implemented safely and
         deterministically with the stdlib.

2. **Artifact manifest schema.**
      - All persisted artifacts (parameter manifest, calibrator primitives,
         mappings, saved state) MUST include:
         - `schema_version`
         - `tool_name` or `component_id`
         - `created_by`
         - `created_at`
         - `checksums` (file → `sha256:<hex>`)
         - `access_control` (e.g., `{"read": ["role:maintainer"], "write": ["role:ci"]}`)
         - `retention_policy` (e.g., `{"class": "ephemeral|standard|regulated", "days": 30}`)
      - `save_state()` MUST write a top-level manifest and the files listed
         therein.
      - `load_state()` MUST validate checksums and `schema_version` before
         rehydration.

      - Secrets MUST NOT be stored in manifests. Add a CI lint that scans
         artifacts for credential-like strings and fails loudly. Document
         secrets handling rules for integrators.

3. **No opaque blobs by default.**
      - Default persistence/export APIs MUST be JSON-safe and portable.
      - Opaque binary exports (e.g., pickle) are allowed only behind an explicit
         opt-in API and MUST be documented as non-portable / potentially insecure.
      - Provide an advanced plugin hook for tightly controlled binary exchange,
         but keep it non-default.

---

## 1) ADR-009: Preprocessing automation + mapping persistence

**Goal:** Complete preprocessing automation with `auto_encode='auto'`, enforce
unseen-category policy, provide mapping export/import helpers, improve dtype
diagnostics, and align telemetry/docs.

### 1.0 Relevant references

- **[RELEASE_PLAN_v1.md](RELEASE_PLAN_v1.md)** — v0.11.0 task definition and release gates.
- **[adrs/ADR-009-input-preprocessing-and-mapping-policy.md](adrs/ADR-009-input-preprocessing-and-mapping-policy.md)** — preprocessing automation requirements and JSON-safe mapping persistence contract.
- [adrs/ADR-002-validation-and-exception-design.md](adrs/ADR-002-validation-and-exception-design.md) — validation error expectations for dtype/unseen-category failures.
- [adrs/ADR-031-calibrator-serialization-and-state-persistence.md](adrs/ADR-031-calibrator-serialization-and-state-persistence.md) — persistence patterns that may need to include mapping snapshots.
- [../standards/STD-003-test-coverage-standard.md](../standards/STD-003-test-coverage-standard.md) — required test coverage and determinism.
- [tests-guidance.md](https://github.com/Moffran/calibrated_explanations/blob/main/.github/tests-guidance.md) — test-writing rubric.

### 1.1 Current anchors in code

- Wrapper: `src/calibrated_explanations/core/wrap_explainer.py`.
- Config: `src/calibrated_explanations/api/config.py` (`auto_encode`, `unseen_category_policy`).
- Preprocessor metadata snapshot utilities already exist:
  - `WrapCalibratedExplainer.get_preprocessor_metadata()`
  - `WrapCalibratedExplainer.set_preprocessor_metadata()`

### 1.2 Key gaps (from ADR-009 status + release plan)

- `auto_encode='auto'` does not yet activate a built-in encoder deterministically.
- Mapping export/import contract (JSON-safe primitives) is not available.
- Unseen-category policy must be enforced consistently in inference.
- Dtype diagnostics for DataFrames/non-numeric inputs must be actionable.

### 1.3 Implementation steps

1. **Define and publish the mapping snapshot protocol.**
      - Add `HasMappingSnapshot` protocol in
         `src/calibrated_explanations/preprocessing/protocols.py`:

         ```py
         class HasMappingSnapshot(Protocol):
               schema_version: str
               def get_mapping_snapshot(self) -> Dict[str, Any]: ...
               def load_mapping_snapshot(self, snapshot: Dict[str, Any]) -> None: ...
         ```

      - Snapshot requirements:
         - JSON-safe primitives only.
         - Includes: `schema_version`, `preprocessor_type`, `stable_seed`,
            `column_order`, per-column category order, and a per-column
            `encoding_spec`.
         - `mapping_hash` MUST be computed deterministically as:

            ```py
            canonical = json.dumps(
                  snapshot,
                  sort_keys=True,
                  separators=(",",":"),
                  ensure_ascii=False,
            ).encode("utf-8")
            mapping_hash = "sha256:" + sha256(canonical).hexdigest()
            ```

           Canonicalization rules for `mapping_hash` are the same as the global
           manifest canonicalization rules (shortest-decimal numbers,
           ISO-8601 UTC datetimes, JSON `null`, `json.dumps(..., sort_keys=True,
           separators=(",",":"), ensure_ascii=False)`).

         - `column_order` MUST be treated as authoritative during transform;
            `import_mapping()` MUST validate incoming `column_order` against the
            fitted input schema and raise `MappingImportError` with a clear,
            actionable message if the orders differ (suggesting re-order or
            re-fit instructions).
         - All snapshots MUST include `snapshot_schema_version`. Minor versions
            are additive; importers SHOULD accept older minor versions. A major
            version mismatch MUST raise `IncompatibleSnapshotError`.
      - Document versioning: additive changes only; break only on major.

2. **Implement a deterministic built-in encoder.**
      - Implement a built-in encoder with `fit_transform` / `transform`,
         `get_mapping_snapshot()` and `load_mapping_snapshot()`.
         - Determinism rules:
             - `stable_seed` controls all deterministic behaviors (category
                ordering, binning decisions, any randomized hashing) and MUST be
                recorded in the snapshot metadata. Document seed semantics across
                processes; tests must verify reproducible outputs across supported
                OS and Python versions. If a dependency introduces non-determinism,
                document required dependency versions.
             - Category ordering MUST be deterministic and stable. Use priority:
                user-provided ordering > alphabetical (Unicode codepoint) > numeric.
                Record the applied rule in each column's `encoding_spec`.
             - Column ordering MUST be stable and recorded in `column_order`.
      - Enforce `unseen_category_policy` at transform time; default remains
         `"error"`. Provide explicit behavior for `"ignore"`.

3. **Activate `auto_encode='auto'` in fit paths.**
    - In `wrap_explainer.py`, extend `_pre_fit_preprocess()`:
       - if user provided a preprocessor: use it (existing behavior).
       - else if `auto_encode` resolves to `"auto"`: build+fit the built-in encoder.
       - else: no preprocessing.
    - Ensure the result is deterministic across runs (seeded, stable category ordering).

4. **Implement mapping export/import helpers (protocol-based).**
      - Add public wrapper-layer methods consistent with ADR-009:
         - `WrapCalibratedExplainer.export_mapping() -> dict`
         - `WrapCalibratedExplainer.import_mapping(mapping: dict) -> None`
      - Export rules:
         - If `_preprocessor` implements `HasMappingSnapshot`, return the snapshot
            plus `schema_version` and a deterministic `mapping_hash`.
         - If `_preprocessor` does not implement the protocol, raise
            `MappingExportNotSupportedError` with actionable guidance.
      - Import rules:
         - Accept only JSON-safe snapshots conforming to the protocol.
         - Reject opaque blobs by default.
         - Validate `schema_version` and `mapping_hash` (where present).
      - Ensure mapping artifacts align with the global artifact manifest rules.

5. **Advanced extension (explicit opt-in).**
      - Provide an opt-in plugin hook (e.g.,
         `register_mapping_serializer(name, serializer)`) to enable controlled
         binary exchange for specialized tooling.
      - Keep this non-default and document portability/security implications.

6. **Improve dtype diagnostics.**
    - Identify where the library currently fails on non-numeric input and ensure
       the raised error includes:
       - offending column(s),
       - suggested fix (`auto_encode='auto'` or user preprocessor),
       - policy note about unseen categories.

7. **Telemetry/doc alignment.**
    - Ensure `WrapCalibratedExplainer._build_preprocessor_metadata()` is emitted
       consistently, and docs mention the meaning of `mapping_snapshot`.

### 1.4 Decision (v0.11.0)

- Adopt a small public protocol (`HasMappingSnapshot`) for mapping export/import.
- Built-in encoder implements the protocol.
- User preprocessors participate only by implementing the protocol; otherwise
   export/import raises `MappingExportNotSupportedError`.
- Opaque blob export/import is disallowed by default; a separate opt-in hook is
  available for advanced tooling.

### 1.5 Verification checklist

- Unit tests:
  - `auto_encode='auto'` produces identical transformed feature layout across
    fit+predict in the same run.
  - `export_mapping()` + `import_mapping()` reproduces identical predictions.
   - Built-in encoder is deterministic across runs given the same `stable_seed`.
  - unseen-category policy:
    - default `"error"` fails on unseen category,
    - `"ignore"` does not fail and produces deterministic output.
   - Export/import for a user preprocessor that does not implement
      `HasMappingSnapshot` raises `MappingExportNotSupportedError` with clear
      guidance.
- Run `pytest -q`.

---

## 2) ADR-030: Test quality tooling upgrades (determinism + assertions)

**Goal:** Upgrade test-quality tooling and wire it into CI.

### 2.0 Relevant references

- **[RELEASE_PLAN_v1.md](RELEASE_PLAN_v1.md)** — v0.11.0 task definition and release gates.
- **[adrs/ADR-030-test-quality-priorities-and-enforcement.md](adrs/ADR-030-test-quality-priorities-and-enforcement.md)** — enforcement posture and incremental rollout expectations.
- [ANTI_PATTERN_REMEDIATION_PLAN.md](ANTI_PATTERN_REMEDIATION_PLAN.md) — existing anti-pattern taxonomy and remediation context.
- **[../standards/STD-003-test-coverage-standard.md](../standards/STD-003-test-coverage-standard.md)** — baseline quality/coverage expectations.
- **[tests-guidance.md](https://github.com/Moffran/calibrated_explanations/blob/main/.github/tests-guidance.md)** — authoritative test-writing rubric used by the detector.

### 2.1 Current anchors in repo

- Anti-pattern scanner entrypoint: `scripts/detect_test_anti_patterns.py`.
- Scanner implementation: `scripts/anti-pattern-analysis/detect_test_anti_patterns.py`.
- Authoritative guidance: `.github/tests-guidance.md`.

### 2.2 Key gaps (from ADR-030 + release plan)

- Existing detector focuses on a small set of patterns; v0.11.0 requires
   assertion and determinism checks.

### 2.3 Implementation steps

1. **Extend the anti-pattern detector.**
    - Add new findings for:
       - tests without assertions (treat `pytest.raises` and `pytest.warns` as assertions),
       - use of `random.*` without explicit seeding,
       - use of time/network primitives without patching.
    - Keep the detector behavior-first: avoid brittle AST rules that false
       positive heavily.

2. **Add a CI enforcement mode.**
    - Introduce a `--check` mode (or equivalent) that returns non-zero when
       violations exist.
      - Decision (v0.11.0): enforce **no new violations** using a committed
         baseline file.
      - Add flags:
         - `--check` (CI mode): exit non-zero on new violations
         - `--report <path>`: write full report
         - `--rebaseline`: regenerate baseline locally (PR-reviewed)

3. **Wire into CI.**
    - Add the script to the core CI workflow so that it runs alongside tests.
    - Ensure it does not require heavy optional dependencies.
      - Commit a baseline file (e.g., `.github/test-quality-baseline.json`) and
         make CI compare findings against it. Baseline files MUST include file
         hashes and a short `rationale` field for each entry. The
         `--rebaseline` flow MUST print a diff and require a PR with at least
         one reviewer sign-off to accept baseline updates.
      - Publish the report as a CI artifact.

### 2.4 Decision (v0.11.0)

- Enforce “no new violations” immediately using a committed baseline file.
- Baseline updates happen via `--rebaseline` and must be PR-reviewed.

Performance job environment:
   - Define the canonical CI environment for performance benchmarking (CPU
      count, memory limits, container/VM image, fixed dataset path) and
      document reproduction steps. Publish expected P95 and throughput
      interpretation guidance in the perf job README.

### 2.5 Verification checklist

- Running `python scripts/detect_test_anti_patterns.py` generates a report.
- Running the new check mode fails when violations exist.
- CI fails on *new* violations compared to baseline, but allows existing
   baseline entries.
- CI shows the detector as a distinct step with actionable output.

---

## 3) ADR-031: Calibrator primitives + explainer state persistence

**Goal:** Implement versioned `to_primitive`/`from_primitive` contracts for
calibrators and add explainer `save_state()`/`load_state()`.

### 3.0 Relevant references

- **[RELEASE_PLAN_v1.md](RELEASE_PLAN_v1.md)** — v0.11.0 task definition and release gates.
- **[adrs/ADR-031-calibrator-serialization-and-state-persistence.md](adrs/ADR-031-calibrator-serialization-and-state-persistence.md)** — serialization/persistence contract and versioning expectations.
- [adrs/ADR-009-input-preprocessing-and-mapping-policy.md](adrs/ADR-009-input-preprocessing-and-mapping-policy.md) — mapping snapshots that should round-trip through persistence.
- [adrs/ADR-005-explanation-json-schema-versioning.md](adrs/ADR-005-explanation-json-schema-versioning.md) — versioning principles and JSON-safe constraints.
- [adrs/ADR-002-validation-and-exception-design.md](adrs/ADR-002-validation-and-exception-design.md) — failure modes and exception types.
- [../standards/STD-003-test-coverage-standard.md](../standards/STD-003-test-coverage-standard.md) — persistence round-trip testing expectations.

### 3.1 Current anchors in code

- Calibration modules live under `src/calibrated_explanations/core/` and
   `src/calibrated_explanations/calibration/`.
- Export/import is already a public-facing theme in docs (see foundations).

### 3.2 Implementation steps

1. **Define primitive schema and version policy.**
    - Use JSON-safe primitives only.
    - Require `schema_version` and a calibrator type discriminator.
      - Every calibrator primitive includes: `schema_version`, `calibrator_type`,
         `parameters`, and `checksums`.
      - Validate `schema_version` and fail with actionable errors when unsupported.

2. **Implement calibrator primitives.**
    - For each built-in calibrator, add:
       - `to_primitive() -> dict`
       - `from_primitive(payload: Mapping[str, object]) -> <Calibrator>`
    - Validate schema version range and fail fast with actionable errors.

3. **Implement explainer persistence API.**
    - Add `save_state(path_or_fileobj)` and `load_state(path_or_fileobj)` on the
       wrapper entry point (recommended) so preprocessing (ADR-009) is included.
      - Persist as a directory or archive containing:
         - `manifest.json` (top-level manifest with checksums and schema version)
         - calibrator primitive JSON
         - preprocessing mapping snapshot(s)
         - `explainer_config.json` (plugin identifiers and versions)
         - optional `audit.log`
      - Atomicity/durability requirements:
         - Write to a temp location and finalize using atomic rename/move.
         - Never partially overwrite an existing artifact; on failure, leave the
            previous artifact intact.
         - Include checksums for every file and validate them on load.
      - Implement `load_state()` to validate checksums and `schema_version` before
         rehydrating; on mismatch raise `IncompatibleStateError`.
      - Remote storage upload robustness (where implemented):
         - Prefer a two-phase pattern (upload to staging + checksum verify +
            promote/commit).
         - Enforce RBAC/credential checks before upload; on denial fail fast with
            actionable guidance.

4. **Round-trip tests.**
    - Ensure saved state rehydrates to identical predictions on the same input.
    - Ensure incompatible schema versions raise a documented exception.
      - Add a negative test that mutates a checksum and asserts `load_state()`
         rejects the artifact.

### 3.3 Verification checklist

- Unit tests cover save/load for both classification and regression flows.
- State artifacts are JSON-safe and stable.
- Run `pytest -q`.

---

## 4) ADR-012: Notebook/gallery execution + time ceilings

**Goal:** Document the tooling choice and enforce notebook execution/time
ceilings in docs CI.

### 4.0 Relevant references

- **[RELEASE_PLAN_v1.md](RELEASE_PLAN_v1.md)** — v0.11.0 task definition and release gates.
- **[adrs/ADR-012-documentation-and-gallery-build-policy.md](adrs/ADR-012-documentation-and-gallery-build-policy.md)** — notebook/gallery execution policy and time ceilings.
- [adrs/ADR-010-core-vs-evaluation-split-and-distribution.md](adrs/ADR-010-core-vs-evaluation-split-and-distribution.md) — keeps heavy/optional deps out of core installs.
- [../standards/STD-004-documentation-audience-standard.md](../standards/STD-004-documentation-audience-standard.md) — doc structure and audience expectations.

### 4.1 Implementation steps

1. **Execution engine (`nbconvert` MVP).**
      - Use `nbconvert` invoked from docs CI to execute notebooks.
      - Wrap execution in a stable driver script that:
         - sets per-notebook and per-cell timeouts via env vars
            (`DOCS_NOTEBOOK_TIMEOUT_SECS`, `DOCS_CELL_TIMEOUT_SECS`),
         - captures execution output,
         - emits a JSON execution report (success/failure, timing, cell-level logs).
      - Reproducibility requirements:
         - Ensure executed notebooks record the environment (Python version and
            package set) in the execution report or as a separate artifact.
         - Pin the docs execution environment (constraints/lock) for release
            branches.
        - Include a pinned notebook environment file (`docs/notebook_environment.yml`
          or a `Pipfile.lock`) and ensure the notebook kernel is explicitly set
          in the notebook metadata so CI uses the pinned kernel.
      - Document a migration path to `sphinx-gallery` in a later release.

2. **Enforce time ceilings.**
    - Configure per-notebook and/or per-cell timeouts.
    - Ensure failures are blocking on release branches per ADR-012.
      - Document how to mark long-running examples as offline/manual and skipped
         in CI.
      - Define a standard metadata tag (e.g., `tags: ["ci-skip", "slow"]`) and a
         deterministic rule for skipping them in CI.

3. **Make optional dependencies explicit.**
    - Ensure docs execution jobs install the right extras (viz/notebooks).
    - Ensure core unit tests do not require these extras (ADR-010).

### 4.2 Decision (v0.11.0)

- Use `nbconvert` as the MVP execution engine with explicit timeouts.
- Document a later migration path to `sphinx-gallery`.

### 4.3 Verification checklist

- Docs CI runs notebook execution with explicit timeouts.
- A deliberately failing example causes the docs job to fail.
- A smoke test covers the notebook execution driver behavior.

---

## 5) ADR-026: Plugin semantics hardening (invariants + immutability + telemetry)

**Goal:** Enforce strict invariants and immutability, and ensure telemetry
completeness for explanation plugin execution.

### 5.0 Relevant references

- **[RELEASE_PLAN_v1.md](RELEASE_PLAN_v1.md)** — v0.11.0 task definition and release gates.
- **[adrs/ADR-026-explanation-plugin-semantics.md](adrs/ADR-026-explanation-plugin-semantics.md)** — plugin invariants, immutability expectations, and telemetry requirements.
- [adrs/ADR-002-validation-and-exception-design.md](adrs/ADR-002-validation-and-exception-design.md) — exception types for invariant violations.
- [adrs/ADR-028-logging-and-governance-observability.md](adrs/ADR-028-logging-and-governance-observability.md) — governance logging/telemetry expectations.
- [../standards/STD-005-logging-and-observability-standard.md](../standards/STD-005-logging-and-observability-standard.md) — logging expectations for telemetry.
- [../standards/STD-003-test-coverage-standard.md](../standards/STD-003-test-coverage-standard.md) — tests for “bad plugin” invariants.

### 5.1 Current anchors in code

- Prediction bridge + monitor is described in ADR-026 and implemented in core.
- Feature filter and orchestrator already attach logging context.

### 5.2 Implementation steps

1. **Immutability enforcement.**
      - Pass immutable contexts to plugins (frozen dataclasses and
         `types.MappingProxyType` for dict-like payloads).
      - At plugin boundary, perform a shallow immutability check and capture a
         copy for telemetry.
      - If a plugin attempts to mutate the provided context, raise
         `ConfigurationError` including the plugin id.

2. **Invariant enforcement.**
      - Ensure invariants are enforced for plugin outputs, not only for
         serialization:
         - `low <= predict <= high` for all intervals
         - required calibrated bridge call occurs per invocation
      - Fail as `PluginInvariantError` (subclass of `ConfigurationError`) and
         include `plugin_id`, `call_id`, and the failing invariant message.

3. **Telemetry completeness.**
      - Per-invocation telemetry must include:
         - `plugin_id`, `plugin_version`
         - `invocation_id` (UUID)
         - `start_ts`, `end_ts`
         - `input_hash`, `output_hash` (sha256 of canonicalized JSON)
         - `invariant_status` (pass/fail)
      - Ensure governance logger uses `calibrated_explanations.governance.*` and
         telemetry sinks are configurable.

### 5.3 Verification checklist

- Unit tests include a minimal “bad plugin” that violates invariants and
   asserts the correct exception type and message.
- Tests assert that telemetry is emitted for failure cases.

---

## 6) ADR-027 + ADR-028: Observability enforcement (examples + lint/tests)

**Goal:** Close observability enforcement by adding logging/observability
examples and tests, aligned with Standard-005.

### 6.0 Relevant references

- **[RELEASE_PLAN_v1.md](RELEASE_PLAN_v1.md)** — v0.11.0 task definition and release gates.
- **[adrs/ADR-027-fast-feature-filtering.md](adrs/ADR-027-fast-feature-filtering.md)** — feature filtering behavior that must emit governance logs.
- **[adrs/ADR-028-logging-and-governance-observability.md](adrs/ADR-028-logging-and-governance-observability.md)** — logging domains/fields and governance observability expectations.
- **[../standards/STD-005-logging-and-observability-standard.md](../standards/STD-005-logging-and-observability-standard.md)** — the enforcement standard for observability.
- [../standards/STD-003-test-coverage-standard.md](../standards/STD-003-test-coverage-standard.md) — tests for logging behavior.
- [tests-guidance.md](https://github.com/Moffran/calibrated_explanations/blob/main/.github/tests-guidance.md) — caplog assertion patterns.

### 6.1 Current anchors in code

- Logging context helper exists: `src/calibrated_explanations/logging.py` (`ensure_logging_context_filter`).
- Feature filter already uses governance logger name and context filter:
  `src/calibrated_explanations/core/explain/_feature_filter.py`.

### 6.2 Implementation steps

1. **Examples (docs).**
    - Add examples showing how to configure logging in a host application:
       - text formatter
       - JSON formatter
       - governance logger routing
    - Place them in the governance/observability documentation area.

2. **Tests (caplog).**
    - Add unit tests that assert:
       - governance events use `calibrated_explanations.governance.*` loggers,
       - expected structured fields appear when set.

3. **Enforcement tooling.**
    - Add a lightweight lint check (script) or extend existing scripts to
       validate logger domain usage in touched modules.

### 6.3 Verification checklist

- Tests validate logger names/domains.
- Docs contain runnable snippets (kept lightweight).

---

## 7) Standard-001: Nomenclature cleanup completion tasks

**Goal:** Eliminate double-underscore mutations, split utilities, report lint
telemetry, and confine transitional shims to `legacy/`.

### 7.0 Relevant references

- **[RELEASE_PLAN_v1.md](RELEASE_PLAN_v1.md)** — v0.11.0 task definition and release gates.
- **[../standards/STD-001-nomenclature-standardization.md](../standards/STD-001-nomenclature-standardization.md)** — canonical naming rules and cleanup targets.
- **[Standard-001_nomenclature_remediation.md](Standard-001_nomenclature_remediation.md)** — step-by-step remediation roadmap.
- [PATTERN_1_REMEDIATION_PLAN.md](PATTERN_1_REMEDIATION_PLAN.md) — related private-member remediation context.
- [../standards/STD-002-code-documentation-standard.md](../standards/STD-002-code-documentation-standard.md) — docstring/code documentation expectations while refactoring.

### 7.1 Implementation steps

1. **Double-underscore removal.**
    - Identify remaining accesses like `_Class__attr` and replace with accessors
       or protected fields.
2. **Utility splitting.**
    - Split `utils/helper.py` responsibilities into focused modules (see
       `docs/improvement/Standard-001_nomenclature_remediation.md`).
3. **Lint/telemetry reporting.**
    - Ensure naming-lint tooling exists and emit a governance/telemetry summary
       suitable for dashboards.
4. **Legacy confinement.**
    - Ensure all transitional shims live under `src/calibrated_explanations/legacy/`.

### 7.2 Verification checklist

- No new double-underscore accesses.
- Internal imports point to canonical modules.

---

## 8) Governance dashboards: surface lint status + preprocessing/domain telemetry

**Goal:** Extend dashboards/telemetry documentation so maintainers can monitor
lint status alongside preprocessing/domain-model telemetry.

### 8.0 Relevant references

- **[RELEASE_PLAN_v1.md](RELEASE_PLAN_v1.md)** — v0.11.0 task definition and release gates.
- **[adrs/ADR-028-logging-and-governance-observability.md](adrs/ADR-028-logging-and-governance-observability.md)** — governance telemetry/logging requirements.
- [adrs/ADR-009-input-preprocessing-and-mapping-policy.md](adrs/ADR-009-input-preprocessing-and-mapping-policy.md) — preprocessing telemetry/mapping snapshot signals.
- **[../standards/STD-005-logging-and-observability-standard.md](../standards/STD-005-logging-and-observability-standard.md)** — what “observability compliant” means.
- [GOVERNANCE.md](https://github.com/Moffran/calibrated_explanations/blob/main/.github/GOVERNANCE.md) — governance reporting conventions and where dashboards belong.

### 8.1 Implementation steps

1. Define the “lint status” signal:
    - source (script output, CI job status, generated report)
    - format (JSON report artifact, log line, badge)
      - Minimal JSON artifact format includes: `component`, `check_name`,
         `status`, `details_url`, `timestamp`.
2. Document how to publish/consume it in governance docs.

3. Telemetry privacy and reliability (minimums):
      - Make sampling rate and enable/disable configurable.
      - Document retention expectations for telemetry artifacts.
      - Ensure failures still emit a minimal event (with `invocation_id`) and
         increment a failure counter metric.

4. Governance export format.
         - Publish a machine-readable governance export schema that bundles
            `manifest.json`, `audit.log`, and associated telemetry entries into a
            single export artifact. Provide an example ingestion script that
            demonstrates consuming the export and extracting metrics for a
            governance dashboard.
         - Document the minimal fields required for governance ingestion and the
            JSON schema for the export artifact.

### 8.2 Verification checklist

- A single documented workflow exists for maintainers to retrieve lint status
   and preprocessing/domain-model signals.

---

## 9) Pattern 1 hardening: empty `.github/private_member_allowlist.json`

**Goal:** Empty the private member allowlist as part of final remediation.

### 9.0 Relevant references

- **[RELEASE_PLAN_v1.md](RELEASE_PLAN_v1.md)** — v0.11.0 task definition and release gates.
- **[PATTERN_1_REMEDIATION_PLAN.md](PATTERN_1_REMEDIATION_PLAN.md)** — remediation scope and definition of “Pattern 1”.
- **[../../.github/private_member_allowlist.json](../../.github/private_member_allowlist.json)** — artifact that must become empty.
- [ANTI_PATTERN_REMEDIATION_PLAN.md](ANTI_PATTERN_REMEDIATION_PLAN.md) — related remediation workflow.
- [../standards/STD-001-nomenclature-standardization.md](../standards/STD-001-nomenclature-standardization.md) — discourages private-member coupling and renames.

### 9.1 Current state

- Allowlist exists: `.github/private_member_allowlist.json`.
- Repo includes helper scripts, but `scripts/prune_allowlist.py` is currently
   hardcoded to a local path and should not be relied upon without fixing.

### 9.2 Implementation steps

1. Refactor remaining allowlisted tests to avoid private member usage.
2. Fix or replace the `scripts/prune_allowlist.py` script so it is portable
   (remove hardcoded paths) and can be used to re-generate the allowlist cleanly.
3. Set `allowlist` to an empty array.
4. Ensure the private-member scan CI gate remains green.
5. Add a CI job that runs the private-member scan on a fresh checkout and
   fails if private members are used.

### 9.3 Verification checklist

- CI private-member scan passes with an empty allowlist.

---

## 10) Final gap sweep: ADRs, standards, improvement docs + appendix update

**Goal:** Perform a final sweep and update any remaining gaps, ensuring the
release appendix reflects reality.

### 10.0 Relevant references

- **[RELEASE_PLAN_v1.md](RELEASE_PLAN_v1.md)** — release gate checklist, v0.11.0 task list, and appendix gap log.
- **[adrs/ADR-004-parallel-backend-abstraction.md](adrs/ADR-004-parallel-backend-abstraction.md)**, **[ADR-005](adrs/ADR-005-explanation-json-schema-versioning.md)**, **[ADR-008](adrs/ADR-008-explanation-domain-model-and-compat.md)**, **[ADR-009](adrs/ADR-009-input-preprocessing-and-mapping-policy.md)**, **[ADR-010](adrs/ADR-010-core-vs-evaluation-split-and-distribution.md)**, **[ADR-012](adrs/ADR-012-documentation-and-gallery-build-policy.md)**, **[ADR-020](adrs/ADR-020-legacy-user-api-stability.md)**, **[ADR-026](adrs/ADR-026-explanation-plugin-semantics.md)**, **[ADR-027](adrs/ADR-027-fast-feature-filtering.md)**, **[ADR-028](adrs/ADR-028-logging-and-governance-observability.md)**, **[ADR-030](adrs/ADR-030-test-quality-priorities-and-enforcement.md)**, **[ADR-031](adrs/ADR-031-calibrator-serialization-and-state-persistence.md)** — v0.11.0 primary ADR gate set.
- **[../standards/STD-001-nomenclature-standardization.md](../standards/STD-001-nomenclature-standardization.md)**, **[STD-002](../standards/STD-002-code-documentation-standard.md)**, **[STD-003](../standards/STD-003-test-coverage-standard.md)**, **[STD-004](../standards/STD-004-documentation-audience-standard.md)**, **[STD-005](../standards/STD-005-logging-and-observability-standard.md)** — standards to re-check at ship time.
- [CHANGELOG.md](https://github.com/Moffran/calibrated_explanations/blob/main/CHANGELOG.md) — ensure shipped items are reflected.
- [legacy_user_api_contract.md](legacy_user_api_contract.md) — validate legacy contract changes are intentional.
- [GOVERNANCE.md](https://github.com/Moffran/calibrated_explanations/blob/main/.github/GOVERNANCE.md) — governance reporting/checklist expectations.

### 10.1 Implementation steps

1. Re-run all “health” scripts relevant to v0.11.0:
    - test suite, coverage gates, anti-pattern scans, notebook audit.
2. Update ADR status notes if implementation changes semantics.
3. Update the release plan appendix gap log for any newly-closed items.

### 10.2 Verification checklist

- All gates for v0.11.0 are green.
- Appendix reflects closed gaps.

---

## Release gate summary

v0.11.0 can ship when the items above are completed or explicitly deferred per
ADR policy, and the following are green:

- `pytest -q`
- ADR-030 test-quality tooling in CI
- Core-only import path verified (no matplotlib required)
- Docs execution job runs notebooks with time ceilings (release-branch blocking)
- Legacy API audit workflow is in place

---

## Next steps (concrete)

1. Copy the precise clarifications (canonicalization, PII redaction, RBAC,
   deterministic seeds, atomic persistence, telemetry rules) into the plan
   sections above where indicated.
2. Implement the minimal tests listed in the "Minimal new tests to add"
   section and wire them into CI (parity, perf, save_state atomic,
   mapping MLflow round-trip, plugin invariant, determinism, PII checks).
3. Add PII redaction helpers, RBAC language, and atomic/transactional
   persistence rules to the `save_state()`/`load_state()` design and
   implementers' guidance.
4. Commit the ADR-004 naming addendum and publish the `condition_source`
   migration guide and compatibility checker (one-time fit-time warning).
5. Ensure canonical exception classes and message templates are documented
   and covered by the new tests.


---

## Minimal new tests to add (explicit)

The following tests are required and must be added to the test suite and CI:

- `tests/integration/test_save_state_atomic.py` — simulate partial remote
   upload and assert `load_state()` fails cleanly and leaves previous artifacts
   intact; exercise `--force-retry` semantics in an integration job.
- `tests/parity/offline_parity.py` and `tests/parity/online_parity.py` — parity
   harnesses that produce JSON reports and are runnable locally and in CI;
   include `--offline`/`--online` flags. Add `tests/parity/run_parity.py` as the
   canonical runner that accepts `--offline`/`--online` and emits a
   machine-readable JSON report.
- `tests/preprocessing/test_mapping_roundtrip_mlflow.py` — export/import mapping
   through an MLflow mock and assert identical predictions and that `mlflow_run_id`
   and `artifact_uri` are recorded in the manifest when `mlflow_upload=True`.
- `tests/plugin/test_bad_plugin_invariant.py` — minimal bad-plugin that violates
   an invariant; assert `PluginInvariantError` is raised and telemetry (governance
   log + metrics counter) is emitted.
- `tests/determinism/test_seed_across_platforms.py` — assert reproducible
   outputs with the same `stable_seed`; when CI cannot run on all OSes, at a
   minimum run deterministic checks on available runners and document coverage.
- `tests/security/test_no_pii_in_manifest.py` — assert `ValidationError` with
   `CE_VALIDATION_PII` or that explicit redaction produces `'<redacted>'` values
   in persisted manifests.

Each new test must follow the repository test guidance and be runnable in CI
with a short runtime (or marked to run in a separate longer-running job where
appropriate).
