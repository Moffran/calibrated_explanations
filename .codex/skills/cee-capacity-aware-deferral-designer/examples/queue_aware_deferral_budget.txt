We currently route a case to manual review whenever the calibrated interval width is above 0.25 or the freshness checker marks the case as degraded. Volume is about 4,000 cases per day. Reviewers can handle 300 cases per day with a 24-hour SLA, and backlog above 600 becomes operationally unacceptable. We also have a `valid`, `degraded`, and `likely_invalid` posture from upstream checks. Design a proceed/review/escalate policy table, state the capacity assumptions, and say when a simpler single-threshold manual-review policy would be better.
