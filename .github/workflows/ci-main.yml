# CI: Main branch gates
# Purpose: Main-branch gating workflow — coverage upload, perf guard, anti-pattern audits.
# Scope: Runs on `main` pushes; protects releases. See docs/improvement/CI-upgrade.md
# for branch-protection flip instructions.
name: CI — Main branch gates
on:
  push:
    branches: [ main ]

concurrency:
  group: ci-main
  cancel-in-progress: true

jobs:
  coverage-and-gates:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: |
            requirements.txt
            constraints.txt
            pyproject.toml
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -c constraints.txt
          pip install -e .[dev,eval] -c constraints.txt
      - name: Agent instruction consistency
        run: |
          python scripts/quality/check_agent_instruction_consistency.py
      - name: Logger domain enforcement (ADR-028 / STD-005)
        run: |
          python scripts/quality/check_logging_domains.py \
            --root src/calibrated_explanations \
            --report reports/quality/logging_domain_report.json
      - name: Generated report local-path guard
        run: |
          python scripts/quality/check_no_local_paths_in_reports.py \
            --check \
            --report reports/quality/no_local_paths_report.json
      - name: Run core tests with coverage (context-aware for over-testing analysis)
        run: |
          pytest -q -o addopts= \
            --cov=src/calibrated_explanations \
            --cov-config=pyproject.toml \
            --cov-report=xml:coverage.xml \
            --cov-context=test \
            --cov-fail-under=90
      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: coverage.xml
      - name: Run per-module coverage gates
        run: python scripts/quality/check_coverage_gates.py coverage.xml

  perf-guard:
    needs: coverage-and-gates
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: |
            requirements.txt
            constraints.txt
            pyproject.toml
      - name: Install deps
        run: |
          pip install -r requirements.txt -c constraints.txt
          pip install . psutil -c constraints.txt
      - name: Run micro benchmark
        run: |
          python scripts/perf/micro_bench_perf.py > tests/benchmarks/micro_current.json
      - name: Compare micro metrics
        run: |
          python scripts/perf/check_perf_micro.py tests/benchmarks/micro_current.json tests/benchmarks/perf_thresholds.json

  anti-pattern-audit:
    needs: coverage-and-gates
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: |
            requirements.txt
            constraints.txt
            pyproject.toml
      - name: Run anti-pattern detector
        run: |
          python scripts/anti-pattern-analysis/detect_test_anti_patterns.py \
            --check \
            --output reports/anti-pattern-analysis/test_anti_pattern_report.csv \
            --report reports/anti-pattern-analysis/test_quality_report.json \
            --baseline .github/test-quality-baseline.json
      - name: Run test-helper export guard
        run: |
          python scripts/quality/check_no_test_helper_exports.py \
            --root src/calibrated_explanations \
            --report reports/anti-pattern-analysis/test_helper_wrapper_report.json
      - name: Run marker hygiene check (ADR-030 Phase 3)
        run: |
          python scripts/quality/check_marker_hygiene.py \
            --check \
            --report reports/marker-hygiene/marker_hygiene_report.json \
            --baseline .github/marker-hygiene-baseline.json
      - name: Run generated report local-path guard
        run: |
          python scripts/quality/check_no_local_paths_in_reports.py \
            --check \
            --report reports/quality/no_local_paths_report.json
      - name: Upload quality reports
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: test-quality-reports
          path: |
            reports/anti-pattern-analysis/test_anti_pattern_report.csv
            reports/anti-pattern-analysis/test_quality_report.json
            reports/anti-pattern-analysis/test_helper_wrapper_report.json
            reports/marker-hygiene/marker_hygiene_report.json
            reports/quality/no_local_paths_report.json

  parity-core-vs-extras:
    needs: coverage-and-gates
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: |
            requirements.txt
            constraints.txt
            pyproject.toml
      - name: Generate core parity snapshots
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -c constraints.txt
          pip install -e . -c constraints.txt
          pip install jsonschema -c constraints.txt
          for dataset in classification regression multiclass probabilistic_regression; do
            python tests/parity_reference/run_parity_reference.py --dataset $dataset --condition-source observed --update
            python tests/parity_reference/run_parity_reference.py --dataset $dataset --condition-source prediction --update
          done
          mkdir -p artifacts/parity/core
          cp tests/parity_reference/*.json artifacts/parity/core/
      - name: Generate extras parity snapshots
        run: |
          pip install -e .[dev,eval,viz] -c constraints.txt
          for dataset in classification regression multiclass probabilistic_regression; do
            python tests/parity_reference/run_parity_reference.py --dataset $dataset --condition-source observed --update
            python tests/parity_reference/run_parity_reference.py --dataset $dataset --condition-source prediction --update
          done
          mkdir -p artifacts/parity/extras
          cp tests/parity_reference/*.json artifacts/parity/extras/
      - name: Compare core vs extras parity snapshots
        run: |
          python scripts/quality/compare_parity_snapshots.py \
            --left artifacts/parity/core \
            --right artifacts/parity/extras
      - name: Upload parity snapshot artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: parity-core-vs-extras
          path: |
            artifacts/parity/core
            artifacts/parity/extras

  over-testing-density:
    name: Over-testing density (ADR-030 Phase 4 — advisory)
    needs: coverage-and-gates
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v6
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: |
            requirements.txt
            constraints.txt
            pyproject.toml
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -c constraints.txt
          pip install -e .[dev,eval] -c constraints.txt
      - name: Run tests with coverage contexts
        run: |
          pytest -q -o addopts= \
            --cov=src/calibrated_explanations \
            --cov-config=pyproject.toml \
            --cov-context=test \
            --no-cov-on-fail
      - name: Generate over-testing density report
        run: |
          python scripts/over_testing/over_testing_report.py \
            --require-multiple-contexts \
            --output-lines reports/over_testing/line_coverage_counts.csv \
            --output-blocks reports/over_testing/block_coverage_counts.csv \
            --output-summary reports/over_testing/summary.json \
            --output-metadata reports/over_testing/metadata.json
      - name: Detect redundant tests (fingerprint analysis)
        run: |
          python scripts/over_testing/detect_redundant_tests.py
      - name: Upload over-testing reports
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: over-testing-reports
          path: |
            reports/over_testing/

  private-member-scan:
    needs: coverage-and-gates
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v6
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: |
            requirements.txt
            constraints.txt
            pyproject.toml
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt -c constraints.txt; fi
      - name: Run private-member scan
        run: |
          python scripts/anti-pattern-analysis/scan_private_usage.py tests --check
