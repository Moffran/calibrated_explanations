We have KS, martingale, and freshness detectors. Adaptive routing wants a shared result type, governance wants stable reason codes for audit records, and UX wants to show a simplified banner. Today we return `is_valid: bool`, `score: float | None`, and a free-form `reason: str`. Design a minimal `ValidityResult`, define the states, and say what must stay out of the shared contract.
