# v1.0.1 Plan

> **Release version:** `1.0.1`
> **Development version:** `1.0.1-dev`
> **Status:** Active
> **Theme:** Post-v1.0 stabilisation

## Outcome

Confirms that the v1.0.0 GA quality and performance baselines remain clean,
completes the release-planning simplification, and delivers narrowly scoped
post-GA correctness fixes for documented wrapper preprocessing and explicitly
requested cache/parallel initialization. The release adds no new public API or
plugin category and does not enable cache or parallel execution by default.

## Included work

| ID | Deliverable | Issue | ADR/Standard | Status |
|---|---|---|---|---|
| T1 | Complete migration to issue- and milestone-based release planning | [#201](https://github.com/Moffran/calibrated_explanations/issues/201) | — | Done |
| T2 | Re-run the Standard-002 docstring coverage gate and the Standard-001 nomenclature gate on the v1.0.1 candidate commit; compare against the v1.0.0 GA baseline (96.64% coverage, zero nomenclature violations) | [#199](https://github.com/Moffran/calibrated_explanations/issues/199) | STD-001, STD-002 | Done |
| T3 | Re-run the caching/parallel staging-equivalent smoke from `development/finished-work/v1.0.0-rc_plan.md` §3 and compare telemetry to the RC baseline (cache `hits=1, misses=1`; parallel `strategy="threads", workers=2, worker_utilisation_pct=100.0`) | [#200](https://github.com/Moffran/calibrated_explanations/issues/200) | ADR-003, ADR-004 | Done |
| T4 | Make wrapper auto-encoding operational and safe for mixed-type data | [#202](https://github.com/Moffran/calibrated_explanations/issues/202) | ADR-009 | Done |
| T5 | Make requested cache and parallel initialization failures explicit | [#209](https://github.com/Moffran/calibrated_explanations/issues/209) | ADR-003, ADR-004, ADR-028 | Done |
| T6 | Publish `1.0.1` following `release.md` | — | — | Not started |

## Excluded

- New public API or plugin categories.
- Caching/parallel on-by-default graduation; both remain explicitly opt-in.
- Deprecation or removal of the root `transform_to_numeric` export; tracked by
  #203 for a future minor-release deprecation cycle.
- Ranking-semantics consolidation and plugin-author diagnostics unless separately
  assigned to this milestone.
- Broad preprocessing, cache, parallel, or plugin architecture redesign beyond
  the defects defined in the included issues.

## T4) Wrapper auto-encoding for mixed-type data

### T4.1 Status assessment

Completed 2026-09-11. The built-in `auto_encode='auto'` path now leaves
numeric columns unchanged, encodes categorical columns deterministically,
supports explicit categorical feature overrides, and applies the configured
missing-value policy for categorical inputs.

Reopened 2026-09-22: the encoder fix did not reach the public entry point.
`WrapCalibratedExplainer.fit()`/`calibrate()` only called `_pre_fit_preprocess`
when a user preprocessor was set, so the default `auto_encode='auto'` wrapper
still failed on mixed-type DataFrames. The prior tests exercised the helper
directly or supplied a preprocessor. Fixed by routing through
`_should_pre_fit_preprocess` (non-numeric input or configured
`categorical_features`), with end-to-end coverage added below.

### T4.2 Verification checklist

- [x] `tests/unit/core/test_builtin_encoder.py` covers mixed-type passthrough,
  explicit categorical overrides, and missing-value policy failures.
- [x] `tests/unit/core/test_wrap_explainer_helpers.py` verifies wrapper-level
  auto-encoding preserves numeric columns and respects the configured
  missing-value policy.
- [x] `tests/unit/core/test_config_management_contract.py` verifies the new
  `ExplainerConfig`/`ExplainerBuilder` preprocessing fields are exposed and
  configurable.
- [x] `tests/unit/core/test_wrap_explainer_persistence.py` verifies the built-in
  preprocessing state round-trips on mixed-type inputs.
- [x] `tests/integration/core/test_preprocessor_wiring.py` verifies the default
  wrapper runs fit → calibrate → explain/predict on a mixed-type DataFrame
  (classification and regression), rejects unseen categories at inference,
  raises `ValidationError` with `auto_encode=False`, and passes all-numeric
  DataFrames through unchanged.

## T5) Explicit cache and parallel initialization failures

### T5.1 Status assessment

Completed 2026-09-21. An explicitly requested cache or parallel executor that
cannot be initialized now fails closed with `ConfigurationError` from
`ExplainerBuilder.build_config()` or `WrapCalibratedExplainer.from_config()`,
with `details` naming the `capability`, failing `source` and `cause`.
Non-numeric `CE_CACHE`/`CE_PARALLEL` values raise `ConfigurationError` on both
the builder and plain constructor paths. The perf factory records activation
per capability (`config`, `env`, `disabled_by_env`, or `None`). Retained ADR-004
fallbacks (pool start-up failure, `force_serial` recovery, joblib requested
but missing) emit `UserWarning` plus INFO and are registered as user-visible in
`check_warning_policy.py`; the DEBUG-only `wrap_from_config_perf_primitives`
exemption was removed. The minimal cache-backend notice now fires once per
process, only when an enabled cache is built (registered exemption).

Maintainer decisions: keep ADR-004 graceful degradation for pool start-up
failure; raise in `build_config()`. Adjacent defects split out: #218 (enabled
parallel with the default `auto` strategy silently auto-selects a backend),
#219 (unrecognised `CE_PARALLEL`/`CE_CACHE` tokens are ignored), #220
(`CE_CACHE` is only honoured on the builder/config path).

### T5.2 Verification checklist

- [x] `tests/unit/api/test_api_builder.py` covers fail-closed builder errors for
  malformed `CE_CACHE`/`CE_PARALLEL`, factory failures, env-over-builder
  precedence, activation recording, and no warnings when nothing is requested.
- [x] `tests/unit/core/test_wrap_explainer_helpers.py` covers `from_config()`
  fail-closed errors per capability and call-site `perf_parallel=` precedence.
- [x] `tests/unit/core/test_calibrated_explainer_parallel_env.py` covers the
  plain constructor path raising for a malformed `CE_PARALLEL`.
- [x] `tests/unit/perf/test_parallel.py`, `tests/unit/test_perf_parallel.py` and
  `tests/unit/core/explain/test_parallel_lifecycle.py` assert `UserWarning` +
  INFO for the retained parallel fallbacks and non-integer env rejection.
- [x] `tests/unit/cache/test_cache_roundtrip_and_basic.py` covers non-numeric
  `CE_CACHE` rejection and the once-per-process, enabled-only backend notice.
- [x] `docs/foundations/how-to/tune_runtime_performance.md` states guarantees
  versus best-effort hints.

## Dependencies

- T4 depends on completion of T1 and the preprocessing replacement defined in #202.
- T6 runs only after T1-T5 are Done or explicitly removed from the milestone by
  maintainer decision.

## Release-specific gates

- Docstring coverage and Standard-001 nomenclature checks pass on the release
  commit (T2).
- Caching/parallel telemetry shows no regression against the RC/GA baseline
  (T3).
- Mixed-type wrapper workflows pass classification and regression integration
  tests without altering purely numeric inputs (T4).
- Explicitly requested cache or parallel initialization cannot silently
  degrade, while sequential defaults and opt-in behavior remain unchanged
  (T5).
- Standard repository gates (tests, lint, docs build, version alignment) are
  owned by `release.md` and `make release-preflight`; not repeated here.

## Release decision

`Not ready` — T1-T5 are Done; T6 is not yet complete.

T5 evidence (2026-09-21, working tree on commit `70b615f9`): `make
local-checks-pr` → exit 0 (non-viz suites 1960 passed, 1 skipped and 4078
passed, 2 skipped; pre-commit all green). `python
scripts/quality/check_warning_policy.py --check` → 10 registry entries, 0
failures; `python scripts/quality/check_adr002_compliance.py` → no violations.
Full `pytest` run without coverage → 4220 passed, 2 skipped, 3 failed: one
order-dependent failure in a new T5 test (fixed; the test now clears
`CE_CACHE`/`CE_PARALLEL`, because `run_notebooks()` in
`tests/docs/test_notebook_driver.py` leaks `CE_PARALLEL=off` into the process
environment), and two viz tests
(`test_plot_parity_adapter.py::testplot_triangular_delegates_to_adapter`,
`test_plotspec_mvp.py::testplot_regression_default_save_paths_include_title`)
that fail identically on `70b615f9` without the T5 changes and are unrelated
to T5.

T3 evidence (2026-09-04, commit `6132e677`): `CE_CACHE=1 python -m pytest
tests/unit/cache tests/unit/perf/test_cache.py -v -o addopts= --no-cov` → 20
passed, matching the RC suite count exactly. `CE_PARALLEL="enable,threads,
workers=2,min_batch=1,min_instances=1,tiny=1" python -m pytest
tests/unit/core/test_parallel_benchmark.py
tests/unit/core/test_parallel_deprecation.py
tests/unit/core/test_parallel_helpers.py -v -o addopts= --no-cov` → 7
passed, matching the RC suite count exactly. A callback smoke script
mirroring RC §3.7 (`CalibratorCache`/`ParallelExecutor` with a telemetry
callback registered) reproduced `cache_miss`, `cache_store`, `cache_hit`
events with metrics snapshot `hits=1, misses=1`, and a `parallel_execution`
event with `strategy="threads"`, `workers=2`, `worker_utilisation_pct=100.0`
for ordered results `[1, 4, 9, 16]` — an exact match to the RC baseline in
every field; no deviation observed, so no classification of environment- or
format-variation was needed. `docs/foundations/how-to/safe-defaults.md`
telemetry field names/descriptions (`cache_hit`/`cache_miss` with
`namespace`/`key`/aggregate `hits`/`misses`; `parallel_execution` with
`strategy`/`workers`/`worker_utilisation_pct`) still match the runtime
implementation; no documentation drift found. Environment: Windows
10.0.26200, PowerShell/Git Bash, Python 3.11.9, pytest 9.1.1 (RC baseline
used pytest 9.0.3 — a harmless dependency-version variation with no effect
on pass/fail or telemetry), branch `main`, commit `6132e677`. No corrective
action required; caching and parallel execution remain opt-in-only (no
default-enablement change made).

T2 evidence (2026-09-02, commit `bdb68093`): `python
scripts/quality/check_docstring_coverage.py --fail-under 94.0` → overall
96.66% (Modules 99.17%, Classes 100.00%, Functions 95.09%, Methods 96.85%),
at or above the v1.0.0 GA baseline of 96.64% — no regression. `python
scripts/quality/check_std001_nomenclature.py --root src/calibrated_explanations
--check` → 0 violations (`reports/nomenclature_violation_inventory.json`:
`total_violations: 0`; 6 pre-approved `utility_import_bridge` records, all
`is_allowed: true`), matching the v1.0.0 GA baseline of zero violations
exactly. No corrective action required.

T1 evidence (2026-07-22): `CONTRIBUTING.md`, `.github/CONTRIBUTING.md`,
`.github/pull_request_template.md`, the `ce-release-planner`/`ce-release-task`/
`ce-release-check` skills and `version_plan_reference.md`, and
`scripts/local_checks.py` all updated to the target operating model in #201
(fork-friendly upstream authority, ordinary-vs-release-scoped PR rules,
compact `ID | Deliverable | Issue | ADR/Standard | Status` table, and a
non-authoritative `## Release decision` no longer parsed as a readiness
gate). `pytest tests/scripts/test_local_checks_release_workflow.py -q` → 34
passed; `make capability-chain-check` → 0 errors (8 warnings); `make
local-checks-pr` → 4023 passed, 1 skipped, pre-commit all green; `python -m
sphinx -W --keep-going docs docs/_build/html` → build succeeded. Corrected
workflow exercised end-to-end via T2's linked-issue commands (read from
issue #199, not duplicated in this plan): docstring coverage 96.64% and zero
STD-001 nomenclature violations, matching the v1.0.0 GA baseline exactly.
Repository-wide search confirms no file outside `development/finished-work/`
and `evaluation/` references `kristinebergs` except the two reviewed
`.codex/skills/` files, which name an unrelated local workspace-folder
convention (no change needed).

`kristinebergs/calibrated_explanations` disposition (2026-07-22): 0 open
issues, 0 open PRs. One stray unmerged branch, `difficulty_reject` (last
pushed 2026-07-15, no associated PR, no unique feature work — commit history
is generic release/bump commits), is abandoned/superseded per maintainer
decision and may be deleted from the mirror at the maintainer's convenience;
nothing there needs migrating to `Moffran/calibrated_explanations`.
